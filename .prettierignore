# Build output & deps
node_modules/
dist/
build/
out/
*.tsbuildinfo
coverage/
*.vsix

# Web client copied into the cli package for npm distribution (Story 11.5, AC2) — a
# derived artifact (apps/web/dist), not ours to reformat.
packages/cli/web-dist/

# Lockfile (pnpm owns its formatting)
pnpm-lock.yaml

# BMad assets, planning artifacts & agent-tool caches — not ours to reformat.
_bmad/
_bmad-output/
integration/
docs/
.cline/
.agent/
.agents/
.claude/

# Pre-existing repo docs outside this story's scope.
epic-cycle-workflow-creation.md
