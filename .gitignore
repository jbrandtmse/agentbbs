# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
*.tsbuildinfo
*.vsix

# Downloaded VS Code builds for the @vscode/test-electron real-host harness (Story 10.2)
.vscode-test/

# AgentBBS runtime data (per-project ledger)
.agentbbs/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Environment / local config
.env
.env.*
!.env.example

# BMad installation-kit backups (detect→backup→idempotent rewrite)
*.bak-*

# Editor / OS cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Claude Code scheduled-task runtime lock (not source)
.claude/scheduled_tasks.lock

# Local manual-smoke ledger for the VS Code extension (not source)
.agentbbs-smoke/
