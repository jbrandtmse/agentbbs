// @agentbbs/mcp-server — thin stdio MCP client over @agentbbs/core. Tool
// handlers validate (Zod v4) -> call core -> return; no board logic lives here.
// Populated by Epic 2+.

/** Package name marker; replaced by the server bootstrap in Story 2.1. */
export const MCP_SERVER_PACKAGE = '@agentbbs/mcp-server';
