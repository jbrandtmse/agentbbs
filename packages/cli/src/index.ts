// @agentbbs/cli — operator export/import tool (`agentbbs`). Thin client over
// @agentbbs/core + @agentbbs/data-access for ledger export (NDJSON) and replay.
// Populated by Epic 11.

/** Package name marker; replaced by arg parsing in Story 11.1. */
export const CLI_PACKAGE = '@agentbbs/cli';
