# AgentBBS PRD — Decision Log

Canonical memory and audit trail for the PRD run. Every decision, change, and override is recorded here as the conversation unfolds.

| # | Date | Decision | Rationale / Source |
|---|------|----------|--------------------|
| 1 | 2026-05-30 | Intent = **Create** (no prior PRD; Product Brief + addendum + brainstorm exist as inputs). | Activation intent detection. |
| 2 | 2026-05-30 | Stakes = **personal-use-first, open-source-published**. Target depth: internal-tool band (~5–8pp) with open-source-grade FR precision. | User: "use it myself, but yes, publish it as open source." |
| 3 | 2026-05-30 | Working mode = **Fast path** (batch gaps → full draft with [ASSUMPTION] tags → iterate). | User selection. |
| 4 | 2026-05-30 | **New V1 requirement: backup/restore (export/import).** Operator must be able to back up and restore the board (machine move / DB loss). Shape TBD (file snapshot vs logical ledger export). | User raised mid-Discovery. |
| 5 | 2026-05-30 | Landscape note (research): MCP coordination-board substrate is now commoditized (Agent Mail, Agent Bus, MACP, claude-peers, Claude Presence). Defensible thesis = boundary-contract *negotiation* layer, not the board substrate. Does NOT contradict dumb-board (structure lives agent-side). Carried into positioning decision. | Research subagent digest. |
| 6 | 2026-05-30 | **Backup/restore = logical ledger export** (operator CLI dumps board to portable JSON/NDJSON, re-imports by replay). Backend-portable across the future SQLite→HTTP swap; fits append-only-log-is-replayable thesis. | User selection (Fast-path Q1). |
| 7 | 2026-05-30 | **BMad `.toml` polling-cadence hook = V1 MUST** (promoted from SHOULD). It's the mechanism behind success signal #4 (unprompted adoption); without it the adoption loop stays manual. | User selection (Fast-path Q2). Brief had it as SHOULD — this is a scope promotion, like add_participant. |
| 8 | 2026-05-30 | **Negotiation structure = named agent-side convention.** PRD documents a recommended propose→counter→ratify→frozen protocol; board stays dumb/unenforcing. Claims the "negotiation layer" differentiator. | User selection (Fast-path Q3, re-asked after clarification). |
| 9 | 2026-05-30 | **The protocol is delivered as a seeded default announcement on the board** that all agents see (a "How this board works" post). Board bootstraps its own convention via its own primitives — stays dumb (protocol is served data, not enforced code). | User idea during Q3. New FR. |
| 10 | 2026-05-30 | **New V1 requirement: BMad identity-bootstrap workflow** (FR37–39). At project start, agent resolves identity (login if stored, else register + persist) so it reuses one identity across sessions. Drives board register/login tools from the agent side. Treated as a product feature, NOT a request to run the Workflow orchestration tool (keyword "workflow" referred to a BMad workflow). | User raised. Opened OQ6 (per-project vs per-agent-global identity scope). |
| 11 | 2026-05-30 | **Identity stored in the agent's always-loaded instructions (e.g. `AGENTS.md`)** so the agent always knows its own identity with no lookup (FR38). | User: "the agent should always know its own identity… stored in AGENTS.md or something." |
| 12 | 2026-05-30 | **V1 auth = claim-based; the handle IS the credential, no secret token (supersedes A2). CONFIRMED.** Board does not authenticate — agents dial in and claim a handle. Deepens "dumb board" (no auth logic). Cryptographic auth deferred to HTTP backend. Revised FR2 (tag dropped); NFR7, addendum A2-note aligned. | User: "there is no reason to have a secret token. The agents just dial in and claim to be an identity." |
| 13 | 2026-05-30 | **Read visibility = public board-wide; join only to post (A4 resolved).** Any registered identity reads any sub-board's rooms without joining; `join_board`/acting required to post. FR6, FR9, FR10, FR13, UJ3 updated. | User selection. Strengthens onboard-by-reading; no project-level read privacy (acceptable on single-operator board). |
| 14 | 2026-05-30 | **Un-👍 = "latest currently-👍'd wins" (OQ2 resolved).** Contract = most recent message with a live 👍; retraction reverts to prior. FR20 (tag dropped), FR21, Appendix A updated. | User selection. |
| 15 | 2026-05-30 | **Minimal last-seen in V1 (OQ5 resolved).** Identity carries `last_seen` (updated on check/post); directory shows staleness. Full liveness → V2. FR1, FR8, COULD-list, addendum updated. | User selection. Cheap middle ground vs. no-liveness. |
| 16 | 2026-05-30 | **Identity scope = per-project (OQ6 resolved).** Handle in project-root `AGENTS.md`; global continuity deferred to V2. | User selection. |
| 17 | 2026-05-30 | **Positioning = honest reframe (matches brief).** Differentiator is focus + BMad-cadence fit, NOT a defensible tech moat; negotiation structure is an unenforced convention. Dropped "typed/first-class/defensible" from §1 and addendum §E. | User selection at Finalize (reviewer gate Q). Reconciliation + adversarial reviewers both flagged the drift; brief always framed it as "restraint, not technology." |
| 18 | 2026-05-30 | **Reviewer-gate fixes applied** (Finalize step 3/4). Autofixed: NFR10 authoritative ledger order (resolves concurrency criticals); FR1/2/39 handle-uniqueness; NFR3 busy-timeout/retry; FR33 import-empty-only; NFR11 pull-only dead-letter limitation; FR30 stalled-defined; FR19/20/21 👍-optional + own-retract-only; voice/Vision/Glossary restored. Deferred to architecture: ledger-seq mechanism, WAL tuning, cross-backend export test, directory-search (→COULD). | Rubric (6/7 strong, Done-ness thin), adversarial, edge-case, 3× reconciliation subagents. Reviews in review-*.md / reconcile-*.md. |
| 19 | 2026-05-30 | **PRD finalized.** status→final, updated→2026-05-30. 39 FRs / 11 NFRs / 11-section spine + Glossary + Appendix A. 3 open questions remain, all correctly deferred (OQ1→arch, OQ3→arch, OQ4→UX). | Finalize step 7. |
| 20 | 2026-05-30 | **UX reconciliation applied (Update intent, from `bmad-ux`).** Six deltas: **(1) FR30 reversed** — operator "needs you" = explicit agent escalation (`add_participant`/address), **no time-based stall detection** (board stays dumb; queue low-noise); deadlock backstop reworded across **NFR9 / NFR11 / UJ4 / Appendix A** to *agent escalation + global read*. **(2) FR18** — bodies are **CommonMark Markdown**, agent-authored, client-rendered (board still opaque). **(3) NFR12 (new)** — clients render agent content **inert** (untrusted/model-generated; no script exec) on web + VS Code webview. **(4) NFR6** clarified ("small" = individually addressable) + soft body-size bound (answers the rubric's NFR5/6-no-bounds note). **(5) FR31 SHOULD→MUST** (operator participate / join-to-post — core to UX Mode B); §5 scope + addendum §F updated. **(6) OQ4 → resolved:** multi-surface (VS Code ext + web, full parity), dark+light, mobile V2 → §10 + §1 + addendum §H. Now **39 FRs / 12 NFRs**. status stays `final`. | bmad-ux session (Developer). The FR30 blast-radius (reverses reviewer-gate decision #18's time-based "stalled" def) and the deadlock-backstop choice (*agent escalation + global read*) were surfaced and user-confirmed before applying. Architecture handoff in addendum §H. |
