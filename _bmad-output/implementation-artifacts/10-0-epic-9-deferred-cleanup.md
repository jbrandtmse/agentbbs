---
baseline_commit: 6a726a6
---

# Story 10.0: Epic 9 Deferred Cleanup (lead-side, no-code triage)

Status: done

<!-- Retrospective-Review gate artifact — NOT in epics.md. Created by the /epic-cycle gate. -->
<!-- Triages the Epic 9 retrospective + Epic 9 addendum retrospective + deferred-work.md OPEN items before Epic 10 feature work. -->

## Outcome

**TRIAGE-ONLY retro-review gate.** Epic 10 ships the **VS Code extension surface** — a NEW `apps/vscode-extension` (esbuild bundle + extension host), a native `TreeDataProvider`, per-room `WebviewPanel`s mounting the EXISTING `@agentbbs/ui-shared` components, a `postMessage` host↔webview bridge, and webview CSP/serialization. Per **Rule 13** it is a **thin client** over the shared core: it adds NO `packages/core`/`data-access`/`mcp-server` change, NO new MCP tool/event/error code, NO install-kit change, and it is a SEPARATE host from the Epic 9 web `json-api.ts` host. Consequently **none of the carried-forward OPEN deferred items has an Epic-10 trigger** — they live in board-code, the MCP room-tools, the BMad install kit, or the Epic-9 *web* surface, none of which Epic 10 touches. There is **no actionable cleanup code/test for Story 10.0**; it is handled lead-side as a triage record (no dev/QA/code-review/smoke pipeline; no production-source or test change; baseline unchanged at 1202 logic-passing tests).

The substantive Epic-9 **addendum-retro action items (the three Epic-10 carries) are NOT Story 10.0 cleanup** — they are **feature guidance already baked into `epics.md` Epic 10 success criteria** (Rules 14/15 + panel-exclusivity) that specific Epic 10 stories own. Recorded below so those stories carry them; the lead also carries them into the per-story spawn context and owns the Rule-14 integrated smoke at the end-of-epic gate.

**Resolved-since-recorded (confirmation only):** `9-OPERATOR-INITIATE-PARITY` (MED, recorded at the Epic 9 main retro as a follow-up) was **RESOLVED by Stories 9.11–9.14** (the `/bmad-correct-course` operator↔agent parity addendum: announce_project / post_announcement / join_board / update_focus initiate + proto-room respond-parity). `9.4-join-project-inert` was **RESOLVED by Story 9.12**. The `deferred-work.md` line that still lists the initiate-parity gap predates the addendum; it is closed.

**One NEW item recorded at this gate:** the Epic-10 baseline `pnpm test` surfaced a Windows-only teardown flake in `packages/data-access/src/seed-protocol-race.test.ts` (`E10-baseline-seedrace-eperm`, LOW) — recorded in `deferred-work.md` rather than normalized (see anti-pattern: do not normalize known failures). It is pre-existing test-infra, NOT an Epic-10 trigger; deferred with rationale.

## Retro-Review Triage (Epic 9 + addendum → Story 10.0)

Triage covers the **Epic 9 retrospective** (`epic-9-retro-2026-06-01.md`), the **Epic 9 addendum retrospective** (`epic-9-addendum-retro-2026-06-02.md`, Stories 9.11–9.14 parity), and the OPEN items in `deferred-work.md`, at the `/epic-cycle` Retrospective Review gate on **2026-06-02** before Epic 10 feature work.

| Item | Source (retro or deferred-work.md) | Triage Decision |
|---|---|---|
| **1.5** — append-invariant lint guard disabled in `*.test.ts` (string-literal regex, not AST) | Epic 1 / deferred-work | **DEFER (OPEN)** — board-code lint concern over real `better-sqlite3` call sites; NO Epic-10 trigger (the VS Code client speaks no raw SQL — it reads via the `data-access` port like the web host). |
| **1.6** — `wireToPayload` does not validate a known-type-but-malformed payload row | Epic 1 / deferred-work | **DEFER (OPEN)** — corruption-tolerance hardening at the `data-access` seam; no Epic-10 trigger (the extension host reuses the EXISTING read projections; it adds no new event-folding path). |
| **5.1-roomid-cap-edge** — residual `room_id` `ROOM_ID_MAX_LENGTH`=200 cap-edge call test | Epic 5 / deferred-work | **DEFER (OPEN, low-value)** — `room_id` is op-allocated, never user-supplied at the cap edge; no Epic-10 trigger (Epic 10 adds no new MCP room tool / Zod validator). Close won't-fix at a future room-tool touch. |
| **8.4-helper-crlf** — install-kit helper appends an LF block to a CRLF target | Epic 8 / deferred-work | **DEFER (OPEN, low-value)** — empirically not a safety defect (idempotency/backup/foreign-safety hold under CRLF). Lives in the BMad install kit, which Epic 10 does NOT touch. |
| **9.1-L2** — DESIGN.md documents text-body-light contrast as 17.4:1 (true WCAG 16.671:1) | Epic 9 / deferred-work | **DEFER (OPEN, doc nit)** — non-load-bearing planning-doc reference number; AA met with ~3.7× margin; the code-side contrast pin is correct. Untouched by Epic 10. |
| **9.4-mention** — `@mention`-based NEEDS-YOU secondary escalation | Epic 9 / deferred-work | **DEFER (OPEN)** — still no real structured mention marker in the data model; per **Rule 13** a thin client cannot fabricate a board marker, and a body-text regex scan is the forbidden heuristic. Epic 10's TreeView (10.3) consumes the SAME deterministic `add_participant(@operator)` escalation the web tree uses — full parity on the load-bearing path. |
| **9.5-shiki-warmup** — DISMISSED-as-race; optional `isHighlighterWarm()` DOM `beforeAll` hardening | Epic 9 / deferred-work | **DEFER (OPEN) + AWARENESS CARRY → Stories 10.4/10.5.** The architecture is deterministic (module-scoped singleton + per-file Vitest isolation + sync seed when warm); not a defect. But Epic 10 mounts the SAME `ui-shared` Markdown renderer in a webview: any NEW markdown-DOM test added for the webview MUST use the `beforeAll(prewarmHighlighter)` discipline (else it hits the async-effect path). Flagged into 10.4/10.5 Dev Notes; the item itself stays deferred. |
| **9.10-no-modal-substring** — brittle raw-substring `not.toContain` checks in the web `App.test.tsx` no-modal sweep | Epic 9 / deferred-work | **DEFER (OPEN, web-only maintainability nit).** The role-based no-modal assertions are authoritative + non-vacuous. The substring-narrowing is a WEB-harness nit. The no-modal *invariant* itself is a Rule-13/parity contract Epic 10's webview must honor — carried as feature guidance (it builds its OWN no-modal verification), but THIS test-narrowing item is web-only. |
| **9.10-tree.css-comment** — `SidebarTreeItem.tsx` header comment says the focus-ring rule is in `tree.css` (ships in `chrome.css`) | Epic 9 / deferred-work | **DEFER (OPEN, cosmetic).** Pure web-component comment inaccuracy. Epic 10 builds a NATIVE `TreeDataProvider` (Story 10.3), NOT the web `SidebarTreeItem` — it will not be touched. |
| **9.13-trim** — web host `requireBodyString` does not TRIM (whitespace-only focus persists via a direct non-UI POST) | Epic 9 / deferred-work | **DEFER (OPEN, benign) + AWARENESS CARRY → Stories 10.2/10.6.** Lives in the Epic-9 WEB host `json-api.ts`, which Epic 10 does not touch. But the VS Code `postMessage` bridge that mirrors the `update_focus`/`reply` ops should apply the SAME trim discipline the `FocusAffordance` client uses. Flagged into 10.2/10.6 Dev Notes; the web-host item itself stays deferred. |
| **E10-baseline-seedrace-eperm** (NEW) — Windows teardown `EPERM` in `seed-protocol-race.test.ts` `removeTempTree` under full-suite parallel load | Epic 10 baseline `pnpm test` / deferred-work (new) | **DEFER (OPEN, test-infra, Windows-only).** Assertions PASS (exactly-one-protocol-announcement holds); only the post-assert temp-dir cleanup `rmSync` races the Windows handle release. Passes in isolation; pre-existing; NOT an Epic-10 trigger (Epic 10 touches no `data-access` test). Recorded, not normalized. Suggested resolution: lengthen the `removeTempTree` retry/backoff (or add a brief Windows handle-release delay) at the next `data-access` test-infra touch. |
| **Addendum Action 1 — Rule 14 integrated exploratory smoke on the VS Code webview host before the Epic 10 merge** | Epic 9 addendum retro | **CARRY (lead-owned, end-of-epic gate).** The operator↔agent parity now rides shared `ui-shared` compose/picker/proto-room components; the whole-flow free-form seam smoke MUST be re-run on the NEW webview host (it does not transfer for free). The lead runs it after Story 10.6's smoke, before the SC-4 merge. Already in `epics.md` Epic 10 success criteria. |
| **Addendum Action 2 — proto-room navigability is part of the operator parity CONTRACT (Rule 15)** | Epic 9 addendum retro | **CARRY INTO Stories 10.3/10.4.** Announced proto-rooms (`active:false`) must be NAVIGABLE + openable + reply-to-activate in the native TreeView (10.3) and the WebviewPanel (10.4) — not just a web affordance. Lead passes into the 10.3/10.4 spawn context. Already in `epics.md` Epic 10 success criteria. |
| **Addendum Action 3 — panel-exclusivity is web-DOM-specific** | Epic 9 addendum retro | **CARRY INTO Stories 10.4/10.5.** The 9.14 single-open-initiate-panel model won't carry to VS Code; `WebviewPanel`/native surfaces need their OWN exclusivity handling. Lead passes into the 10.4/10.5 spawn context. Already in `epics.md` Epic 10 success criteria. |
| **9-OPERATOR-INITIATE-PARITY** (MED) — operator initiate surface | Epic 9 main retro / deferred-work | **RESOLVED (confirmation)** by Stories 9.11–9.14 (the correct-course parity addendum). Closed; recorded for the audit trail. |
| **9.4-join-project-inert** (LOW) — `＋ join a project…` clickable no-op | Epic 9 main retro / deferred-work | **RESOLVED (confirmation)** by Story 9.12 (wired to `JoinProjectPicker` → existing `join_board`). Closed. |

**Summary:** included in Story 10.0 = **0** (no actionable code), deferred = **11** (1.5, 1.6, 5.1-roomid-cap-edge, 8.4-helper-crlf, 9.1-L2, 9.4-mention, 9.5-shiki-warmup, 9.10-no-modal-substring, 9.10-tree.css-comment, 9.13-trim, + the NEW E10-baseline-seedrace-eperm), carried-as-feature-guidance = **3** (Rule-14 integrated smoke → lead/end-of-epic; Rule-15 proto-room parity → Stories 10.3/10.4; panel-exclusivity → Stories 10.4/10.5), plus **2 awareness sub-notes** (Shiki prewarm discipline → 10.4/10.5; trim discipline → 10.2/10.6), resolved-confirmations = **2** (9-OPERATOR-INITIATE-PARITY, 9.4-join-project-inert), dropped = **0**. The retro-review gate triage is complete; Epic 10 feature work (Stories 10.1–10.6 — the VS Code extension surface) proceeds.

## Dev / QA / Review

N/A — lead-side triage record. No code, no tests, no pipeline stages. The deferred items remain **OPEN** in `deferred-work.md` and carry to the next applicable trigger (a board-code touch for 1.5/1.6, a room-tool touch for 5.1-roomid-cap-edge, an install-kit helper touch for 8.4-helper-crlf, a `data-access` test-infra touch for E10-baseline-seedrace-eperm, the web surface for the Epic-9 web nits). The three feature-guidance carries are owned by the named Epic 10 stories + the lead's end-of-epic smoke.
