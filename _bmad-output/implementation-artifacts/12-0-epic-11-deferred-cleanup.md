---
baseline_commit: e81b825
---

# Story 12.0: Epic 11 Deferred Cleanup (lead-side, no-code triage)

Status: done

<!-- Retrospective-Review gate artifact — NOT in epics.md. Created by the /epic-cycle gate. -->
<!-- Triages the Epic 11 retrospective + deferred-work.md OPEN items before Epic 12 feature work. -->

## Outcome

**TRIAGE-ONLY retro-review gate.** Epic 12 ships **global-board topology + cross-project onboarding + operator board skills** — it reconfigures the BMad **install kit** (`integration/bmad/install-agentbbs.md` + its inline helper) for the global single-machine board, extends the **onboarding identity bootstrap** (`integration/bmad/identity-bootstrap.md`) to announce-or-join the project sub-board, adds a **cross-project integration play** to the prompt snippet / skill-rules, and ships four **operator slash-command skills** (`/agentbbs-check`, `/agentbbs-projects`, `/agentbbs-read`, `/agentbbs-post`). Per the epic header and **Rule 13** there is **NO board-engine change**: `packages/core` / `data-access` / `mcp-server` and the final **17-tool agent surface** stay byte-identical; the engine was already global. Epic 12 introduces **no new board code, no new MCP tool / event / error code, no UI surface, no CLI-subcommand error path, and no `data-access` test**.

Consequently **none of the carried-forward OPEN deferred items has a standalone Epic-12 cleanup shape** — they live in board-code (1.5, 1.6), the MCP room-tools (5.1), the Epic-9 **web** surface (9.x), the **VS Code** extension (10.x), the operator **CLI** error path (11.2, 11.3), or **test-infra** flakes (Shiki, Windows EPERM), none of which Epic 12 touches with production code. There is **no actionable cleanup code/test for Story 12.0**; it is handled lead-side as a triage record (no dev/QA/code-review/smoke pipeline; no production-source or test change; baseline unchanged at **1588** tests, suite green at e81b825).

**The genuinely Epic-12-relevant items are the ones that touch the same ASSETS Epic 12 re-edits** — the install-kit helper (Story 12.6, capstone, re-runs the Rule-11 executable safety proof) and the prompt-snippet / onboarding content-guards (Stories 12.2 / 12.3). Those are routed as **AWARENESS CARRIES to the named stories** (fold-in-when-touched), NOT done standalone in 12.0 — folding the CRLF-detection fix into the same change that extends + re-proves the helper avoids editing it twice, exactly the Rule-9 "defer to the named consumer / measure-and-close when it lands" discipline.

**Resolved-since-recorded (confirmation only):** the Epic-10 / Epic-11 named-consumer items closed during their epics — `10.2` remaining bridge write ops (→ resolved by Stories 10.3–10.6), `10.3` open-room placeholder (→ 10.4), `10.4` `img-src https:` CSP (→ 10.5), `10.4` `onDelta` unconsumed (→ 10.6), `11.1` export inert stub (→ 11.2), `11.1` import inert stub (→ 11.3). Recorded for the audit trail.

**Zero NEW items recorded at this gate** — no Epic-12 baseline regression surfaced; the suite is green at e81b825 with no new flake signature.

## Retro-Review Triage (Epic 11 retro + deferred-work.md OPEN set → Story 12.0)

Triage covers the **Epic 11 retrospective** (`epic-11-retro-2026-06-06.md`) and the OPEN items in `deferred-work.md`, at the `/epic-cycle` Retrospective Review gate on **2026-06-08** before Epic 12 feature work.

| Item | Source (retro or deferred-work.md) | Triage Decision |
|---|---|---|
| **1.5** — append-invariant lint guard disabled in `*.test.ts` (string-literal regex, not AST) | Epic 1 / deferred-work | **DEFER (OPEN)** — board-code lint concern over real `better-sqlite3` call sites. NO Epic-12 trigger (Epic 12 ships markdown assets + slash skills; it writes no raw SQL and adds no `*.test.ts` that appends via SQL). |
| **1.6** — `wireToPayload` does not validate a known-type-but-malformed payload row | Epic 1 / deferred-work | **DEFER (OPEN)** — corruption-tolerance hardening at the `data-access` seam. No Epic-12 trigger (Epic 12 adds no event-folding path; its operator skills READ via the shipped tools). Note 1.6's named consumer was 11.3 import-validation, partially addressed by `validateParsedArchive`. |
| **5.1-roomid-cap-edge** — residual `room_id` `ROOM_ID_MAX_LENGTH`=200 cap-edge call test | Epic 5 / deferred-work | **DEFER (OPEN, low-value)** — `room_id` is op-allocated, never user-supplied at the cap edge. No Epic-12 trigger (Epic 12 adds no new MCP room tool / Zod validator; the operator post skill calls the EXISTING `post_announcement`/`reply`). Close won't-fix at a future room-tool touch. |
| **8.4-helper-crlf** — install-kit helper (`applyBlock`) appends an LF-terminated block to a CRLF target → cosmetically mixed endings in that file | Epic 8 / deferred-work | **DEFER (OPEN) + AWARENESS CARRY → Story 12.6.** This is the ONE item with a genuine Epic-12 trigger: Story 12.6 (capstone) re-touches the SAME `install-agentbbs.md` inline helper (it must write the new global-board config + operator-skill targets) AND re-runs the Rule-11 executable safety proof. The deferred-work suggested resolution was explicitly "when the helper is next touched, optionally detect the target's prevailing newline (CRLF vs LF)…". Story 12.6 IS that touch. Folded into 12.6 Dev Notes (detect prevailing EOL OR document the LF assumption) rather than done standalone here — empirically idempotency/backup/foreign-safety already hold under CRLF, so it is quality-polish that should ride the same re-proof, not a separate edit. |
| **7.3-snippet-callform-latent** — the Story 7.3 prompt-snippet content-guard keeps the narrower `` `token` ``-only phantom regex (no call-form `(?:`|\{)` broadening); latent because that snippet block uses no call-form today | Epic 7 / deferred-work (NOTE) | **DEFER (NOTE) + AWARENESS CARRY → Stories 12.2 / 12.3.** Stories 12.2 (onboarding announce) and 12.3 (cross-project play) EDIT the snippet / onboarding assets and ship Rule-10 content-guards. IF either adds a `` `tool{ args }` `` call-form tool reference to a guarded block, apply the broadened `` /`([a-z][a-z_]{2,})(?:`|\{)/g `` regex from the Story 8.1 guard (the template). Carried so the latent gap is closed if-and-only-if those stories make it live; no action otherwise. |
| **9.1-L2** — DESIGN.md documents text-body-light contrast as 17.4:1 (true WCAG 16.671:1) | Epic 9 / deferred-work | **DEFER (OPEN, doc nit)** — non-load-bearing planning-doc reference number; AA met with ~3.7× margin; the code-side contrast pin is correct. Untouched by Epic 12 (no design-token / DESIGN.md change). |
| **9.4-mention** — `@mention`-based NEEDS-YOU secondary escalation | Epic 9 / deferred-work | **DEFER (OPEN)** — still no real structured mention marker in the data model; per Rule 13 a thin client cannot fabricate a board marker, and a body-text regex scan is the forbidden heuristic. Epic 12's cross-project play (12.3) escalates via the EXISTING deterministic `add_participant(@operator)`, NOT a mention scan — full consistency with the load-bearing path. |
| **9.5-shiki-warmup** — DISMISSED-as-race; optional `isHighlighterWarm()` DOM `beforeAll` hardening | Epic 9 / deferred-work | **DEFER (OPEN, test-infra)** — the architecture is deterministic; not a defect. NO Epic-12 trigger (Epic 12 adds no markdown-DOM test; its assets are `.md` skills exercised by content-guards + real-server smokes, not Shiki renders). |
| **9.10-no-modal-substring** — brittle raw-substring `not.toContain` checks in the web `App.test.tsx` no-modal sweep | Epic 9 / deferred-work | **DEFER (OPEN, web-only maintainability nit)** — the role-based no-modal assertions are authoritative + non-vacuous. Web-harness-only; Epic 12 ships no web surface. |
| **9.10-tree.css-comment** — `SidebarTreeItem.tsx` header comment says the focus-ring rule is in `tree.css` (ships in `chrome.css`) | Epic 9 / deferred-work | **DEFER (OPEN, cosmetic)** — pure web-component comment inaccuracy. Untouched by Epic 12. |
| **9.13-trim** — web host `requireBodyString` does not TRIM (whitespace-only focus persists via a direct non-UI POST) | Epic 9 / deferred-work | **DEFER (OPEN, benign)** — lives in the Epic-9 WEB host `json-api.ts`. NO Epic-12 trigger (Epic 12's operator skills call the MCP tools directly through the agent, not the web host's `requireBodyString`). |
| **10.3-joinproject-inert (vscode)** — `＋ join a project…` action row is an inert seam in the native TreeView | Epic 10 / deferred-work | **DEFER (OPEN, by design)** — VS Code extension UI surface; Epic 12 ships no extension change. |
| **10.3-unread-count-test-gap** — uncapped exact unread count in `TreeItem.description` has no direct test assertion | Epic 10 / deferred-work | **DEFER (OPEN, test-coverage gap)** — VS Code tree-provider; no Epic-12 trigger. Owner stays "next tree-provider touch". |
| **10.3-operator-handle-dup** — `tree/operator-handle.ts#resolveOperatorHandle` is DUPLICATED (not imported) from the web host's `ui.ts#resolveOperatorHandle`; forward drift risk if trim+lowercase canonicalization changes on one side | Epic 10 / deferred-work | **DEFER (OPEN, forward-risk) + LIGHT AWARENESS → Story 12.4.** Epic 12's operator read skills (12.4) "resolve the current repo's recorded handle (`login`)". They are NEW markdown slash-skills that instruct the agent to read the `AGENTBBS-IDENTITY` sentinel + call `login` — they consume neither existing `resolveOperatorHandle` copy, so they do not WORSEN the drift. Flagged only so 12.4 reuses the SAME documented sentinel-read + canonicalization convention (trim+lowercase) rather than inventing a third. The dedup itself stays deferred to a handle-canonicalization touch on either existing surface. |
| **10.5-shiki-flake / 10.6-shiki-flake** — Shiki full-suite tokenizer flake (intermittent at the canonical gate; GREEN in isolation) | Epic 10 / deferred-work | **DEFER (OPEN, test-infra)** — untouched package; no Epic-12 trigger. Re-run the file in isolation to confirm if it surfaces at an Epic-12 gate (Rule 6: verify vs git ground-truth before reading as a regression). |
| **10.5-retain-context-rerender** — non-retained room panel re-mounts on each hidden→visible focus, discarding unsaved in-webview UI state | Epic 10 / deferred-work | **DEFER (OPEN, by design, VS Code UX edge)** — no Epic-12 trigger. |
| **E10-baseline-seedrace-eperm** — Windows teardown `EPERM` in `seed-protocol-race.test.ts` `removeTempTree` under full-suite parallel load | Epic 10 / deferred-work | **DEFER (OPEN, test-infra, Windows-only)** — assertions pass; only the post-assert temp-dir cleanup races the Windows handle release. Passes in isolation; pre-existing; NOT an Epic-12 trigger (Epic 12 touches no `data-access` test). Treat a SOLE such failure at an Epic-12 gate as this known flake. |
| **11.2-dbopen-trigger** — the DB-OPEN-failure trigger of AC5 (`--db` at a directory / non-SQLite file → clear error + non-zero exit) is not DIRECTLY asserted; only the unwritable-out-path trigger of the SAME shared catch→exit-1 path is | Epic 11 / deferred-work | **DEFER (OPEN, low-value test-hardening)** — the AC5 error CONTRACT (message + non-zero exit) is proven over the identical code path via the out-path trigger; only the failure CAUSE differs. NO Epic-12 trigger (Epic 12 adds no `export`/`import` CLI error path). Close at the next CLI-error touch. |
| **11.3-replay-nonatomicity** — event replay is not wrapped in one transaction; a NON-CONTIGUOUS / hand-mangled archive throws mid-replay after earlier events commit, leaving a partial ledger | Epic 11 / deferred-work | **DEFER (OPEN, within-V1-scope, corruption-path only)** — a NORMAL `agentbbs export` is always contiguous from seq 1, so an empty-board replay never trips the mid-replay check; fail-loud (AC4) is honored. NO Epic-12 trigger (Epic 12 adds nothing to the export/import surface). Clean fix recorded (pre-replay contiguity check → single batched atomic `append` → post-check), no contract change; fold in at the next import-hardening touch. |
| **10.2/10.3/10.4/10.6, 11.1 named-consumer items** | Epic 10–11 / deferred-work | **RESOLVED (confirmation)** by their named consumers within Epics 10–11 (bridge write ops → 10.3–10.6; open-room → 10.4; CSP img-src → 10.5; onDelta → 10.6; export/import inert stubs → 11.2/11.3). Closed; recorded for the audit trail. |

**Summary:** included in Story 12.0 = **0** (no actionable standalone code), deferred = **18** (1.5, 1.6, 5.1-roomid-cap-edge, 8.4-helper-crlf, 7.3-snippet-callform-latent, 9.1-L2, 9.4-mention, 9.5-shiki-warmup, 9.10-no-modal-substring, 9.10-tree.css-comment, 9.13-trim, 10.3-joinproject-inert, 10.3-unread-count-test-gap, 10.3-operator-handle-dup, 10.5/10.6-shiki-flake, 10.5-retain-context-rerender, E10-baseline-seedrace-eperm, 11.2-dbopen-trigger, 11.3-replay-nonatomicity — counting the two Shiki entries as one), carried-as-awareness = **3** (8.4-helper-crlf → Story 12.6 capstone helper re-proof; 7.3-snippet-callform-latent regex broadening → Stories 12.2/12.3 content-guards if call-form goes live; operator-handle canonicalization convention → Story 12.4 light), resolved-confirmations = **6** (10.2 bridge ops, 10.3 open-room, 10.4 img-src, 10.4 onDelta, 11.1 export stub, 11.1 import stub), new = **0**, dropped = **0**. The retro-review gate triage is complete; Epic 12 feature work (Stories 12.1–12.6) proceeds.

## Dev / QA / Review

N/A — lead-side triage record. No code, no tests, no pipeline stages. The deferred items remain **OPEN** in `deferred-work.md` and carry to the next applicable trigger (board-code touch for 1.5/1.6; room-tool touch for 5.1-roomid-cap-edge; install-kit helper touch for 8.4-helper-crlf — now scheduled into Story 12.6; web surface for the Epic-9 web nits; VS Code extension touch for the 10.x items; CLI-error touch for 11.2/11.3; `data-access`/Shiki test-infra touch for the flakes). The three awareness carries are owned by the named Epic-12 stories (12.6 helper re-proof; 12.2/12.3 content-guards; 12.4 identity-resolution convention).
