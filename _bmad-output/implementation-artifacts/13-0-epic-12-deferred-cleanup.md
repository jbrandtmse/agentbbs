---
baseline_commit: b4093a2
---

# Story 13.0: Epic 12 Deferred Cleanup (lead-side, no-code triage)

Status: done

<!-- Retrospective-Review gate artifact — NOT in epics.md. Created by the /epic-cycle gate. -->
<!-- Triages the Epic 12 retrospective + deferred-work.md OPEN items before Epic 13 feature work. -->

## Outcome

**TRIAGE-ONLY retro-review gate.** Unlike every prior Epic X.0 (where the deferred items had no standalone trigger and all deferred), **Epic 13 IS the deferred-work cleanup epic** — its entire purpose is to RETIRE the prioritized P1–P4 `deferred-work.md` backlog. So this gate's job is the inverse of the usual one: confirm that the items the 2026-06-09 Sprint Change Proposal (`sprint-change-proposal-2026-06-09.md`) scheduled into Stories 13.1–13.6 are complete and correctly mapped, and triage the residual items the SCP did **not** enumerate.

**Epic 12 retrospective contributes ZERO triage items.** `epic-12-retro-2026-06-09.md` records no open action items, no carried deferred review findings, and no Epic-13 preparation tasks; both items it carried (`7.3-snippet-callform-latent`, `8.4-helper-crlf`) were closed within Epic 12, and the two lessons (Rules 22, 23) were codified. Its "Next epic preview" said "no Epic 13 is defined" — accurate that morning, but **superseded the same day** (2026-06-09 20:32) by the Sprint Change Proposal that created Epic 13. The substance of this triage is therefore the **`deferred-work.md` OPEN set**, which the SCP already triaged into the epic.

**Reconciliation finding (this gate's value-add).** The SCP counted **16 genuinely-open items** (13 P1–P4 → Stories 13.1–13.6; 3 P5 excluded pending a product decision). An independent audit of the live `deferred-work.md` OPEN headings at this gate confirms the SCP's 16 **and surfaces two additional genuinely-OPEN items the SCP's enumeration did not list**: `5.1-roomid-cap-edge` and `10.3-join-project-inert`. Both are correctly **DEFERRED** (below the P1–P4 bar / product-decision class) — see the table. Neither is silently dropped; both stay OPEN in `deferred-work.md` with a recorded owner. (This is exactly the retro-review gate's purpose: every OPEN item is triaged on purpose, not just the ones a planning doc happened to enumerate.)

**No standalone 13.0 code.** The actionable cleanup lives in Stories 13.1–13.6 (each closes its named item(s) with evidence per the SCP), so Story 13.0 itself is a lead-side triage record: no dev/QA/code-review/smoke pipeline, no production-source or test change; baseline unchanged at **1663** tests, suite green at `b4093a2`.

**Zero NEW items recorded at this gate** — no Epic-13 baseline regression surfaced; the suite is green at `b4093a2` with no new flake signature.

## Retro-Review Triage (Epic 12 retro + deferred-work.md OPEN set → Story 13.0)

Triage covers the **Epic 12 retrospective** (`epic-12-retro-2026-06-09.md`) and the OPEN items in `deferred-work.md`, at the `/epic-cycle` Retrospective Review gate on **2026-06-10** before Epic 13 feature work. "SCHEDULED" = this epic resolves the item in the named story (the SCP's plan); "DEFER" = stays OPEN past Epic 13.

| Item | Source (retro or deferred-work.md) | Priority | Triage Decision |
|---|---|---|---|
| **E10-baseline-seedrace-eperm** — Windows teardown `EPERM` in `seed-protocol-race.test.ts` under full-suite parallel load | Epic 10 / deferred-work | P1 | **SCHEDULED → Story 13.1** (shared hermetic temp-dir helper with retry/backoff; N≥3 consecutive clean Windows full-suite runs; Rule-7 mutation that assertions still fire). |
| **E12-postmerge** — CLI import-dispatch negative path flaked once with `ENOENT` under full-suite parallel load on Windows | Epic 12 / deferred-work | P1 | **SCHEDULED → Story 13.1** (same temp-dir teardown class; same hardening + N≥3 proof). |
| **9.5-shiki-warmup** — Shiki markdown-render warmup (alias-miss signature; DOM tests RED under parallel load, GREEN in isolation) | Epic 9 / deferred-work | P1 | **SCHEDULED → Story 13.2** (suite-wide `beforeAll(prewarmHighlighter)` + `isHighlighterWarm()` guard or serialize markdown DOM tests; N≥3 proof; consolidated — one fix, three entries). |
| **10.5-shiki-flake** — Shiki `@shikijs/primitive` `startIndex`-undefined tokenizer fault under full-suite concurrency | Epic 10 / deferred-work | P1 | **SCHEDULED → Story 13.2** (same consolidated warmup hardening). |
| **10.6-shiki-flake** — same Shiki full-suite tokenizer flake, intermittent at the gate (10.6 hardened only the one new file) | Epic 10 / deferred-work | P1 | **SCHEDULED → Story 13.2** (same consolidated warmup hardening). |
| **11.3-replay-nonatomicity** — `import` event replay not wrapped in one transaction; a non-contiguous/mangled archive throws mid-replay, leaving a partial ledger | Epic 11 / deferred-work | P2 | **SCHEDULED → Story 13.3** (pre-replay contiguity check → single batched atomic `append(orderedNewEvents)` → post-append seq assertion; Rule-7 planted mid-replay throw must leave an EMPTY ledger; no core/MCP/port change). |
| **11.2-dbopen-trigger** — DB-OPEN-failure trigger of AC5 not directly asserted (only the unwritable-out-path trigger of the same catch→exit-1 path is) | Epic 11 / deferred-work | P2 | **SCHEDULED → Story 13.3** (assert `export` AND `import` against an unopenable `--db` → `failed …` on stderr + non-zero exit). |
| **1.6** — `wireToPayload` does not validate a known-type-but-malformed payload row | Epic 1 / deferred-work | P2 | **SCHEDULED → Story 13.4** (validate payload shape at the `mapping.ts` read seam; reject/surface a data-access-local error on a planted malformed row — NOT added to the closed `BOARD_ERROR_CODES`). |
| **1.5** — append-invariant lint guard disabled in `*.test.ts` (string-literal regex) | Epic 1 / deferred-work | P3 | **SCHEDULED → Story 13.4** (re-enable, AST-based where practical; enforce no raw `INSERT/UPDATE/DELETE` and order-by-`seq`-not-`created_at` in test files; migrate any genuine test-only bypass to `append`). |
| **10.3-operator-handle-dup** — `tree/operator-handle.ts` canonicalization DUPLICATED (not imported) from the web host's `ui.ts#resolveOperatorHandle`; forward drift risk | Epic 10 / deferred-work | P3 | **SCHEDULED → Story 13.5** (one shared trim+lowercase canonicalization util both surfaces import; byte-identical behavior pinned by a test). |
| **10.3-unread-count-test-gap** — uncapped exact unread count in `TreeItem.description` (`"N new"`) has no direct test assertion | Epic 10 / deferred-work | P4 | **SCHEDULED → Story 13.6** (add the direct test assertion). |
| **9.1-L2** — DESIGN.md text-body-light contrast documented as `17.4` (true WCAG `~16.7`) | Epic 9 / deferred-work | P4 | **SCHEDULED → Story 13.6** (correct the doc number; not a planning-artifact amendment — a story task per the SCP). |
| **9.10-tree.css-comment** — `SidebarTreeItem.tsx` header comment says the focus-ring rule is in `tree.css` (ships in `chrome.css`) | Epic 9 / deferred-work | P4 | **SCHEDULED → Story 13.6** (correct the comment). |
| **9.10-no-modal-substring** — brittle raw-substring `not.toContain` no-modal sweep in web `App.test.tsx` | Epic 9 / deferred-work | P4 | **SCHEDULED → Story 13.6** (make precise-token OR confirm role-based assertions authoritative + document the substring narrowing; mutation-test non-vacuous). |
| **9.4-mention** — `@mention`-based NEEDS-YOU secondary escalation | Epic 9 / deferred-work | P5 | **DEFER (OPEN)** — explicitly OUT of Epic 13 per the SCP; needs a product decision (no structured mention marker exists; a body-text regex scan is the forbidden heuristic that would break the FR21 determinism, Rule 13). |
| **9.13-trim** — web host `requireBodyString` does not TRIM (whitespace-only focus persists via a direct non-UI POST) | Epic 9 / deferred-work | P5 | **DEFER (OPEN)** — explicitly OUT of Epic 13 per the SCP; needs a product decision on the host-side trim/validation contract. |
| **10.5-retain-context** — non-retained room panel re-mounts on each hidden→visible focus, discarding unsaved in-webview UI state | Epic 10 / deferred-work | P5 | **DEFER (OPEN)** — explicitly OUT of Epic 13 per the SCP; needs a product/UX decision (state-retention vs `retainContextWhenHidden` cost). |
| **5.1-roomid-cap-edge** — residual `room_id` `ROOM_ID_MAX_LENGTH`=200 cap-edge call test (residual of 4.3-b after the body-cap half resolved) | Epic 5 / deferred-work | — (below P4) | **DEFER (OPEN, low-value)** — NOT enumerated by the SCP; surfaced by this gate's audit. Below the P1–P4 bar: not a flake (P1), not a corruption-path correctness gap (P2 — the cap is a defensive bound proven correct by parity with `projectIdSchema`), not drift (P3), and below even the P4 batch (which targets specific named nits). `room_id` is op-allocated, never user-supplied at the cap edge; no mcp-server tool pins a syntactic slug cap-edge today. Close won't-fix at the next room-tool touch. Stays OPEN. |
| **10.3-join-project-inert** — `＋ join a project…` action row (`agentbbs.joinProject`) is an INERT SEAM in the VS Code TreeView (analogue of the web's resolved `9.4-join-project-inert`) | Epic 10 / deferred-work | — (P5-class) | **DEFER (OPEN, by design)** — NOT enumerated by the SCP; surfaced by this gate's audit. Wiring an operator-INITIATE "join a project" affordance is a **product/feature decision** (operator-initiate parity, same class as the P5 bucket), NOT hardening/tech-debt; Epic 13 is a frozen-contract hardening pass and ships no VS Code feature surface. Stays OPEN for a future operator-initiate-parity decision. |

**Summary:** included in Story 13.0 (standalone code) = **0** (the actionable cleanup is the dedicated Stories 13.1–13.6); **SCHEDULED into this epic = 14 deferred-work items → Stories 13.1–13.6** (E10-baseline-seedrace-eperm + E12-postmerge → 13.1; 9.5-shiki-warmup + 10.5-shiki-flake + 10.6-shiki-flake → 13.2; 11.3-replay-nonatomicity + 11.2-dbopen-trigger → 13.3; 1.6 + 1.5 → 13.4; 10.3-operator-handle-dup → 13.5; 10.3-unread-count-test-gap + 9.1-L2 + 9.10-tree.css-comment + 9.10-no-modal-substring → 13.6 — the SCP's "13 P1–P4" counts the three Shiki entries as one cluster; itemized they are 14 entries / one consolidated 13.2 fix); **DEFER (stays OPEN) = 5** (P5: 9.4-mention, 9.13-trim, 10.5-retain-context; plus the two SCP-unenumerated residuals 5.1-roomid-cap-edge and 10.3-join-project-inert); from-Epic-12-retro = **0**; new = **0**; dropped = **0**. The retro-review gate triage is complete; Epic 13 feature work (Stories 13.1–13.6) proceeds.

## Dev / QA / Review

N/A — lead-side triage record. No code, no tests, no pipeline stages. The 14 SCHEDULED items are resolved-with-evidence by their named Stories 13.1–13.6 (each closing its `deferred-work.md` entry). The 5 DEFERRED items remain **OPEN** in `deferred-work.md`: the 3 P5 items carry to a future product-decision epic; `5.1-roomid-cap-edge` carries to the next room-tool touch (won't-fix-likely); `10.3-join-project-inert` carries to a future operator-initiate-parity decision.
