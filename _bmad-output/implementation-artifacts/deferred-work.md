# Deferred Work

Items deferred during the epic-cycle (code review, dev, QA). Each entry: story ID, severity, summary, rationale, suggested resolution. Clear an item when a later story resolves it.

**Status legend:** items are marked **RESOLVED** (with resolving story + evidence) when closed, or **OPEN** when they carry forward to a future triage. Resolved items are retained (not deleted) so the resolution is recorded.

Ledger snapshot (reconciled at the Story 5.3 code review — EPIC 5 COMPLETE, 2026-05-31): **1.2 RESOLVED** (Story 2.0), **1.4 RESOLVED** (Story 1.7), **1.5 OPEN**, **1.6 OPEN**, **2.2 RESOLVED** (Story 3.0), **2.4 RESOLVED** (Story 3.0), **2.5 RESOLVED** (Story 3.0), **3.0-a RESOLVED** (Story 4.0 — synthetic alias-proof fixture + barrel re-export + proof test retired), **3.0-b OPEN** (guard-before-append doubles the per-call ledger read — code review of 3.0; → Story 6.1), **E3-typecheck-dist RESOLVED** (Story 4.0 — `tsconfig.typecheck.json` `paths` mapping resolves cross-package `@agentbbs/*` from `src`; no-`dist` typecheck green), **E3-tool-names OPEN** (MCP tool names not contract-pinned — Epic 3 retro; → Epic 7), **4.3-a RESOLVED** (Story 5.0 — malformed-`room_id`/`handle` Zod-boundary-rejection tests for `reply`/`add_participant` + a `read_room` READ-side parity spot-check), **4.3-b BODY-CAP HALF RESOLVED** (Story 5.1 — interim `body` char cap replaced with the formal 256 KB / `BODY_TOO_LARGE` core byte-cap + at-/over-cap edge tests, incl. multi-byte-boundary straddle; see code review of story 5.1), **5.1-roomid-cap-edge OPEN** (the RESIDUAL `room_id` `ROOM_ID_MAX_LENGTH`=200 cap-edge — NOT exercised by 5.1/5.3; → low-value test-hardening, fold into the next room-tool touch / Epic 7 contract), **4.6-a OPEN → Story 6.1** (`roomJoinSeq`/`roomMessagesSince` per-call full-stream fold cost — measure when `check` composes them; 5.3's `currentContract`/`readContract` share the same per-call full-stream fold, same perf-watch), **4.5-tool-label OPEN** (cosmetic "12th vs 13th tool" doc drift; → reconcile when `check` lands / Epic 7 contract). **Story 5.2 / 5.3 introduced NO new deferred items** (5.3 code review was clean — 0 HIGH/MED/defer, 1 LOW test-count drift auto-resolved inline). The OPEN set carrying into the Epic 5 retrospective: **1.5, 1.6, 3.0-b(→6.1), 4.6-a(→6.1), 5.1-roomid-cap-edge, E3-tool-names(→Epic 7), 4.5-tool-label(cosmetic)**.

**Story 6.1 update (2026-05-31 — `check` dev stage): 3.0-b RESOLVED + 4.6-a RESOLVED** (both closed-with-evidence — the per-call full-stream fold cost was MEASURED over real SQLite at V1 sizes when `check` came due; `check` incl. recordSeen's double-read is ~1–18 ms median across 200–5000 events, and a contained one-pass-floor + participated-rooms-only optimization in `check.ts` made 4.6-a's fold ~8× faster at 5000 ev; both acceptable at V1, append invariant preserved). The OPEN set now: **1.5, 1.6, 5.1-roomid-cap-edge, E3-tool-names(→Epic 7), 4.5-tool-label(cosmetic)** — both Story-6.1-targeted perf-watch items are now closed. Story 6.1 introduced NO new deferred items at the dev stage.

**Story 7.0 update (2026-05-31 — Epic 7 retro-review, MCP tool contract ratified): E3-tool-names RESOLVED + 4.5-tool-label RESOLVED.** Story 7.0 authored `docs/mcp-tool-contract.md` (the ratified, versioned 17-tool surface — every tool's name / one-line purpose / snake_case wire params / `structuredContent` envelope / error codes, plus the CLOSED 10-code error set, the CLOSED 10-event vocabulary, and the open-read FR9 / membership-gated-write / participation-gated-action / grant-on-act model; NFR8 open-source-ready; cross-linked from the README) and a DRIFT-GUARD test (`packages/mcp-server/src/tool-contract.drift.test.ts`) that pins the doc's canonical tool-name list to the tools the REAL `McpServer` registers (live `Client.listTools()`, Rule 3) AND the doc's §4 closed-error-code table to `BOARD_ERROR_CODES`. So the agent-facing names + envelopes + closed codes are now contract-pinned (E3-tool-names) and the tool count is pinned at **17** (4.5-tool-label — the "12 tools" README/doc drift is corrected). **NO production-logic change** (the doc describes the EXISTING surface; the test pins it). The README "12 tools" prose + the stale "(the 12 tools)" tree comment were corrected to 17 / count-agnostic phrasing. The OPEN set now: **1.5, 1.6, 5.1-roomid-cap-edge** — both Epic-7-due contract items are now closed. Story 7.0 introduced NO new deferred items.

## Deferred from: code review of story 1.2 (2026-05-30)

- **Story 1.2 · LOW · Unused `eslint-plugin-boundaries` dependency. — RESOLVED (Story 2.0, 2026-05-31).**
  - **Resolution:** Story 2.0 removed `eslint-plugin-boundaries` from root `package.json` `devDependencies` and the `pnpm-workspace.yaml` catalog, and refreshed `pnpm-lock.yaml` via `pnpm install` (0 occurrences remain in all three files; 31 transitive packages pruned). The full gate (`lint`/`typecheck`/`test`/`build`/`format`) stayed green and the boundary rules still fire — they were always implemented with `no-restricted-imports`, never the plugin.
  - **Summary:** `eslint-plugin-boundaries` is declared in root `package.json` `devDependencies` and the `pnpm-workspace.yaml` catalog (and is present in `pnpm-lock.yaml`, 4 occurrences) but is never imported by `eslint.config.js`. The dev deliberately implemented all three import-boundary clauses with `no-restricted-imports` instead (documented in the Dev Agent Record).
  - **Rationale:** Dead installed weight; mildly contradicts the Dev Agent Record's own "eslint-plugin-boundaries was NOT added" note. Not blocking — the boundary rules are fully functional and independently proven to fire without it. Removing it now would churn the lockfile right before the lead's smoke/commit, which is undesirable mid-review.
  - **Suggested resolution:** In a follow-up housekeeping change, remove `eslint-plugin-boundaries` from root `devDependencies`, drop its catalog entry in `pnpm-workspace.yaml`, and refresh `pnpm-lock.yaml`. Re-run `pnpm install` + `pnpm run lint`/`test`/`build` to confirm still green.

## Deferred from: code review of story 1.4 (2026-05-31)

- **Story 1.4 · LOW · `runWithRetry` has no inter-attempt backoff for immediately-surfaced busy errors. — RESOLVED (validated by Story 1.7, 2026-05-31).**
  - **Resolution:** Story 1.7 (the named multi-process concurrency consumer that stresses this path) proved the busy_timeout+retry model resolves real cross-process contention with margin: N=6 writers × M=100 = 600 appends produced exactly 600 unique, strictly-monotonic `seq` rows with no lost writes, and `busyErrors` stayed **0** across all writers. The contention probe confirmed the busy/retry path is genuinely traversed (per-writer `maxAppendMs` 184–448 ms against writer 0's 150 ms held lock) yet still never surfaced a premature `StoreBusyError`; the test ran 22× consecutively, all green. The zero-backoff retry was therefore never the bottleneck under genuine load — production connections open with `busy_timeout=5000`, which spaces contended attempts inside each `fn()`. The speculative inter-attempt backoff is not needed; close as resolved-by-evidence. [Source: 1-7-multi-process-concurrency-verification.md — Completion Notes / contention probe.]
  - **Summary:** `runWithRetry` (`packages/data-access/src/sqlite/connection.ts`) retries a busy `fn()` back-to-back with zero delay between whole-call attempts. The documented concurrency model relies on better-sqlite3's per-call `busy_timeout=5000ms` to block-and-wait *inside* each `fn()` before it throws, so in real contention the bounded attempts are naturally spaced (~`busy_timeout × attempts` worst case). For busy errors that SQLite surfaces *immediately* without waiting (e.g. a `SQLITE_BUSY_SNAPSHOT` WAL write-write collision, or any connection that has set `busy_timeout=0`), all `MAX_WRITE_ATTEMPTS` (3) fire within microseconds — the retry provides little practical benefit in that narrow case. The behavior is correctly BOUNDED (no busy-spin: capped at `attempts`, then a typed `StoreBusyError`), and matches NFR3/AR4 as worded ("WAL + busy_timeout + bounded retry" — no jittered-backoff requirement). Not a blocker.
  - **Rationale:** Production connections all open with `busy_timeout=5000` (verified in `openDatabase`), so genuine cross-process contention DOES space the attempts; the zero-gap path is only reachable with the `busy_timeout=0` test-harness condition or the residual instant-`SQLITE_BUSY_SNAPSHOT` case. Adding backoff now would be speculative tuning ahead of real evidence and outside this story's "connection plumbing only" scope.
  - **Suggested resolution:** Validate retry behavior under genuine sustained N×M contention in **Story 1.7** (the multi-process concurrency proof, which is the named consumer that stresses this path). If 1.7 surfaces lost writes or premature `StoreBusyError`s under real load, add a small bounded backoff (e.g. a few ms, optionally jittered) between whole-call attempts in `runWithRetry`. Until then, leave as-is.

## Deferred from: code review of story 1.5 (2026-05-30)

- **Story 1.5 · LOW · Append-invariant lint guard is disabled in `*.test.ts` files. — OPEN (carried forward; Story 2.0 triage 2026-05-31).**
  - **Summary:** `eslint.config.js` section 6 turns the `no-restricted-syntax` append-invariant guard (`UPDATE events` / `DELETE FROM events` / `ORDER BY created_at` literal-matchers) `off` for `**/*.test.{ts,tsx}`. This is a deliberate Story 1.2 decision so the boundary-enforcement fixture can embed forbidden SQL as assertion string literals, and so legitimate test helpers (e.g. Story 1.5's `append.qa.test.ts`, which runs `SELECT ... ORDER BY created_at ASC` to PROVE the seq-vs-created_at orders differ) are not falsely flagged. Consequence: a genuine append-invariant violation introduced inside a *test helper* (not production source) would escape the lint guard and rely solely on code review to catch it. Not caused by Story 1.5; production `data-access`/`core` source remains fully guarded and green.
  - **Rationale:** The exemption is correct and necessary as designed — production source is where the invariant must hold, and that source IS guarded. The residual gap (test-helper SQL) is low-risk: test helpers do not ship and are reviewed. Tightening it now would require a more surgical rule (e.g. only exempting specific fixture files, or matching real `db.prepare(...)`/`Statement.run` call sites rather than any string literal) and is out of scope for an append-only-write story.
  - **Suggested resolution:** If/when a richer SQL-lint approach lands (AST-level matching of actual `better-sqlite3` call sites instead of string-literal regex), re-enable a narrowed append-invariant guard for test files that still permits the fixture/assertion strings. Until then, rely on code review for test-helper SQL. Revisit during the Epic 1 retro or whenever the append-invariant tooling is next touched.

## Deferred from: code review of story 1.6 (2026-05-30)

- **Story 1.6 · LOW · `wireToPayload` does not validate the payload shape of a known-type-but-malformed row. — OPEN (carried forward; Story 2.0 triage 2026-05-31).**
  - **Summary:** `rowToEvent` (`packages/data-access/src/mapping.ts`) hardens the `type` column via `asEventType` (throws loudly on an unknown/foreign type). The payload conversion in `wireToPayload`, however, reads each expected key positionally and coerces with `String(...)`/`Number(...)`. A row whose `type` is a *valid* vocabulary value but whose stored JSON payload is missing or malformed (e.g. external DB tampering, or a future producer bug) would map silently: a missing string key becomes `"undefined"` and a missing `message_seq` becomes `NaN`, rather than failing at the seam. On the happy path this never occurs — payloads are written by `payloadToWire` in the same module and stored verbatim by SQLite, and the all-10-type real-runtime round-trip proves the producer↔consumer contract is exact. The gap is reachable only by a foreign/corrupt row with a known type.
  - **Rationale:** The read direction's contract for this story is the write→read round-trip (fully proven). Defence against externally-corrupted payloads is a validation/hardening concern, not a mapping-fidelity concern, and adding per-field schema validation (e.g. a Zod parse at the seam) is out of scope for the read-query/mapping story — it would also need a decision on the failure mode (throw vs. skip-and-log) that belongs with the projection/validation consumer. Type-column hardening (the loud-failure case most likely to leak an *untyped* event into core) is already in place.
  - **Suggested resolution:** When a payload-validation story lands (or when the first `core` projection in Epic 2+ folds these events and needs corruption-tolerance), parse each read-back payload against its `EventPayloadMap` shape at the `mapping.ts` seam (Zod v4 is already the project's boundary validator) and throw a typed seam error on a malformed-but-known-type row, mirroring `asEventType`'s loud-failure stance. Until then, the type-column guard plus the round-trip contract are sufficient for Epic 1.

## Deferred from: code review of story 2.2 (2026-05-31)

- **Story 1.2 · LOW · `boundary-enforcement.test.ts` is flaky under full-suite parallel load (5000ms ESLint cold-start timeout). — RESOLVED (Story 3.0, 2026-05-31).**
  - **Resolution:** Story 3.0 (Task 2 / AC #2) hoisted a SINGLE shared `ESLint` instance into a `beforeAll` (the typescript-eslint flat-config compile is now paid once, not ~20× per case) AND gave every ESLint-invoking `describe` a generous `{ timeout: 30_000 }` (mirroring `register-race.test.ts`). The cold-start cost is off the per-test critical path and can no longer race the 5000ms wall clock. Verified across 3 consecutive COLD full `vitest run`s (262/262 green each, no boundary-case timeout). Coverage unchanged — the same `no-restricted-imports` / `no-restricted-syntax` / naming rule-ID assertions still fire (19/19).
  - **Summary:** `packages/core/src/boundary-enforcement.test.ts` (a Story 1.2 test) invokes a full ESLint instance programmatically inside the default 5000ms per-test budget. On the FIRST `pnpm run test` run during Story 2.2's review gate it timed out (`Test timed out in 5000ms`) on the `rejects a better-sqlite3 import from core` case — a cold ESLint+typescript-eslint startup under the parallel load of the full 28-file suite (which now also forks built workers for the cross-process race test). The same test passes in isolation (19/19, ~2.4s) and the full suite passed clean on the immediate re-run (180/180), confirming a transient timing failure, not a logic regression. This is a PRE-EXISTING test introduced by Story 1.2; Story 2.2 did not touch it, but the added cross-process race test (which forks OS processes + may trigger a build in `beforeAll`) increases first-run scheduler pressure that makes the cold-start more likely to exceed the budget.
  - **Rationale:** Not introduced by Story 2.2 and not a correctness issue — the boundary rule itself fires correctly (proven in isolation). But a per-test ESLint cold-start racing a 5000ms wall clock under parallel load is inherently order/scheduler-dependent and could intermittently red the CI gate, so it should not stay silent. Left as-is for this story rather than widening scope to retune a Story 1.2 test mid-review.
  - **Suggested resolution:** Give the ESLint-invoking cases in `boundary-enforcement.test.ts` an explicit generous per-test timeout (e.g. `it(name, { timeout: 30_000 }, …)`, mirroring how `register-race.test.ts` sets `TEST_TIMEOUT_MS`), OR hoist the shared `ESLint` instance / flat-config compile into a `beforeAll` so the cold-start cost is paid once rather than per-test. Re-run the full suite several times to confirm non-flaky. Best folded into the next change that touches Story 1.2's lint-enforcement tests (or an Epic 2 housekeeping pass).
  - **Re-observed (Story 2.4 code-review gate, 2026-05-31):** same case timed out at 5000ms on the reviewer's FIRST `pnpm run test`; passed 19/19 in isolation (~2.8s) and the warm full-suite re-run was 234/234 green. Confirms the flake persists and recurs at each review gate — strengthens the case for the timeout/`beforeAll` fix above. No code change made this story.

## Deferred from: code review of story 2.4 (2026-05-31)

- **Story 2.4 (process; spans Epic 2) · LOW · Cross-package tests need a `core` build before they see newly-added `core` exports (monorepo build-ordering papercut). — RESOLVED (Story 3.0, 2026-05-31).**
  - **Resolution:** Story 3.0 (Task 1 / AC #1) added a `resolve.alias` to the root `vitest.config.ts` (top-level AND inside the `agentbbs` project config) mapping each `@agentbbs/<pkg>` specifier to that package's `src/index.ts`. Cross-package tests now resolve against live TS source, so a newly-added `core` export is visible to `mcp-server`/`cli`/`ui-shared` tests with NO prior `pnpm run build`. Proven by `packages/mcp-server/src/cross-package-alias.proof.test.ts`: a fresh `core` export (`CROSS_PACKAGE_ALIAS_PROOF`/`crossPackageAliasProof`) was RED (undefined / not-a-function) from a stale `dist`, then GREEN from the SAME stale `dist` once the alias landed. Build-mode honesty retained: `pnpm run build` stays in the gate as the `dist`/`exports`-artifact guard; the alias maps package ROOTS only (object-form exact match) and no deep `@agentbbs/<pkg>/<subpath>` specifier is imported by any test. project-rules Rule 2 is superseded (Story 3.0, Task 6).
  - **Summary:** `@agentbbs/mcp-server` tests import `@agentbbs/core` through its package `exports`, which resolve to the BUILT `core/dist/` — there is no `src` alias in the Vitest config, so cross-package specifiers are NOT served from TS source. When a story adds a new `core` export (Story 2.4 added `updateFocus` + `NO_IDENTITY`), the mcp-server integration/boundary tests do not see it until `core` is rebuilt: the tool delegate reads `updateFocus === undefined` → throws `TypeError` → mapped to `INTERNAL_ERROR`, producing a confusing red that looks like a logic bug but is purely staleness. Story 2.4's Debug Log documents hitting and diagnosing exactly this; the same class of "stale-dist `INTERNAL_ERROR`" was called out in the 2.4 review stage directive and has now surfaced across multiple Epic 2 stories that add core exports consumed by mcp-server. Intra-package (core-only) unit tests are unaffected because Vitest transforms intra-package source directly. Current mitigation is purely procedural: run `pnpm run build` before the full suite.
  - **Rationale:** Not a correctness defect — the production code and the tests are right; it is a developer-experience / gate-reliability papercut whose blast radius is "a dev or reviewer who runs `pnpm test` without a prior build gets a misleading `INTERNAL_ERROR`." It recurs every time a cross-package export is added, so it is a class-of-issue, not a one-off. Fixing it is a config/process change outside any single story's scope (and the 2.4 review directive explicitly said NOT to fix it in-story), so it is logged here for the Epic 2 retrospective to pick up.
  - **Suggested resolution (pick one; for the Epic 2 retro to decide):** (a) Add a Vitest `resolve.alias` mapping `@agentbbs/core` (and the other workspace packages) to their `src/index.ts` so cross-package tests run against live TS source and never need a prior build — simplest dev-experience win, but verify it does not mask a real `exports`/`dist` packaging problem (consider keeping ONE build-mode integration test that exercises the real `dist`); OR (b) add a root `pretest` script (`"pretest": "pnpm -r build"` or a scoped `pnpm --filter @agentbbs/core build`) so `pnpm test` always refreshes `dist` first — costs a build on every test run but keeps tests honest against the shipped artifact; OR (c) document the build-first requirement prominently (a `test` script banner / CONTRIBUTING note) if the team prefers to keep tests against `dist` with no aliasing. Whichever is chosen, capture it as an Epic 2 retro action so Epics 3–6 (which add many more core exports consumed by mcp-server) do not keep re-hitting it.

## Deferred from: code review of story 2.5 (2026-05-31)

- **Story 2.5 (class-level; also affects Story 2.4) · LOW · `recordSeen` (and the mirrored `updateFocus`) APPENDS an orphan event BEFORE the fail-loud read-back guard throws on an unregistered handle. — RESOLVED (Story 3.0, 2026-05-31).**
  - **Resolution:** Story 3.0 (Tasks 3–4 / AC #3, #4) fixed both ops as a CLASS via a shared helper `appendIdentityEventOrThrow` (`packages/core/src/identity/append-identity-event.ts`): it GUARDS existence BEFORE appending (read + `findIdentity` first; throw `"…not registered…"` WITHOUT appending if absent), only then appends the one event, then reads back and retains the broken-seam fail-loud guard (append succeeded but read-back still misses ⇒ throws `"…after a successful append…"`). `recordSeen` and `updateFocus` both delegate, each supplying its own event shape. The unregistered path now writes NO orphan (the ledger is unchanged), proven at the core layer (in-memory fake) AND over the real SQLite ledger (`record-seen.integration.test.ts` asserts `totalEvents() === 0` after the throw). The two pinning QA tests were inverted to assert no-orphan, the previously-missing symmetric `updateFocus` unregistered-path coverage was added, and a broken-seam test pins the retained defensive branch in both files.
  - **Summary:** `recordSeen` (`packages/core/src/identity/record-seen.ts`) appends ONE `identity.seen`, then reads the identity back via `eventsByActor`/`findIdentity`, and THROWS if the fold yields no record. For a handle with NO prior `identity.registered`, the projection mints no phantom (the deliberate IGNORE stance), so the guard fires — but the orphan `identity.seen` row has ALREADY been persisted (append happens first). The result is an inert, dangling `identity.seen` event with no owning identity. `updateFocus` (Story 2.4, `packages/core/src/identity/update-focus.ts`) has the IDENTICAL orphan-append-then-throw structure — `recordSeen` was deliberately written to mirror it per the story Dev Notes. The behavior is intentional and tested (the unit suite's "still APPENDS exactly one orphan identity.seen before failing" case + the real-ledger integration test both pin it as the contract), not an accidental oversight.
  - **Rationale:** Not reachable in V1 — both ops are invoked ONLY with a registered/established session handle (the mcp-server tool validates the session holder before delegating; `recordSeen` has no MCP consumer at all this story, and its future consumers — Story 6.1 `check`, the Epic 4 post tools — likewise act on an established identity). The orphan event is benign: the projection ignores a `seen`/`focus_updated` without a prior registration (mints no phantom, corrupts no other identity, leaves the unregistered handle underived), and an append-only ledger tolerates inert rows by design. Auto-resolving in-story was deliberately NOT done because (1) the clean fix is to guard existence BEFORE the append (read-then-append — safe here: presence is not uniqueness-constrained, no atomicity requirement), but applying it to `recordSeen` ALONE would break the deliberate symmetry with `updateFocus`, force a rewrite of the two QA tests that pin the orphan-append as correct, and leave the identical wart in `updateFocus` unaddressed — i.e. a partial, asymmetric change that adds inconsistency rather than removing the class of issue; and (2) the path is unreachable, so the risk is a hypothetical future caller passing an unvalidated handle, not a live defect. Same shape as the Story 1.6 `wireToPayload` deferral (a defensive-hardening gap on an unreachable-in-V1 path).
  - **Suggested resolution:** When a future caller can pass an unvalidated handle (or during an Epic 2/identity hardening pass), address `recordSeen` AND `updateFocus` together as a class: either guard existence before the append (read-then-append; reorder so the read-back/existence check precedes `append` and only append once an identity is confirmed), OR extract the shared "append-one-identity-event-then-read-back-or-throw" into a single helper that performs the existence check first. Update the two orphan-append QA tests (unit + integration) to assert NO orphan is written on the unregistered path. Keep the fail-loud throw for the genuinely-broken-seam case (append succeeded but read-back still misses). Best folded into the Epic 2 retro alongside the build-ordering papercut, since it spans 2.4 + 2.5.

## Deferred from: code review of story 3.0 (2026-05-31)

- **Story 3.0 · LOW · The cross-package alias-proof fixture (`CROSS_PACKAGE_ALIAS_PROOF` / `crossPackageAliasProof`) is a PERMANENT public export in the `@agentbbs/core` barrel (ships in `dist/` and the package's public API surface). — RESOLVED (Story 4.0, 2026-05-31).**
  - **Resolution:** Story 4.0 (Task 2 / AC #2) retired the synthetic fixture now that Epic 3's REAL cross-package `core` exports (`announceProject`/`PROJECT_EXISTS`, `joinBoard`, `boardDirectory`, `requireMembership`, …), consumed by `mcp-server` tests, are the standing operational proof of the Vitest `src` alias. Deleted `packages/core/src/cross-package-alias-proof.ts`, removed the `CROSS_PACKAGE_ALIAS_PROOF`/`crossPackageAliasProof` re-export block from `packages/core/src/index.ts`, and deleted the dedicated proof test `packages/mcp-server/src/cross-package-alias.proof.test.ts`; the `vitest.config.ts` comment was repointed at the living proof. Redundancy proven BEFORE removal: from a no-`dist` state the FULL `pnpm test` ran GREEN (50 files / 322 tests — exactly the Epic 3 baseline 324 minus the 2 retired cases), so every cross-package `@agentbbs/core` import in `mcp-server`/`cli` tests still resolves to `src` via the surviving alias without the sentinel. After a rebuild, `core/dist/index.d.ts` no longer contains `CROSS_PACKAGE_ALIAS_PROOF` (0 occurrences) — the test-only sentinel is gone from the public surface. No production code ever imported it (grep-verified).
  - **Summary:** AC #1's proof that the Vitest `resolve.alias` resolves `@agentbbs/core` from `src` requires a real cross-package export that a DIFFERENT package's test imports through the `@agentbbs/core` specifier. The implementation adds `packages/core/src/cross-package-alias-proof.ts` and re-exports its sentinel + function from `packages/core/src/index.ts`. Because it lives in the production barrel, it is a permanent member of the shipped public surface — a downstream consumer could import a test-only sentinel, and it shows up in `core/dist/`.
  - **Rationale:** Deliberate, documented trade-off (the fixture file + barrel comment both say "test-only / test infrastructure, not board logic"). It is inert (a constant + identity function, dependency-free) and harmless at runtime. Not auto-resolved in review because the proof test depends on it being a genuine cross-package export — deleting it now would delete the AC #1 proof's subject. There is no test-only sub-path entrypoint pattern established in this monorepo yet to relocate it cleanly.
  - **Suggested resolution:** Once Epic 3 lands its first REAL `core` export consumed by an `mcp-server` test (e.g. `announceProject` / `PROJECT_EXISTS`), that living test becomes the standing proof of the `src` alias, making the synthetic fixture redundant — at that point retire `cross-package-alias-proof.ts` and its barrel re-export and repoint the proof test at the real export (or delete the synthetic proof test entirely). Alternatively, establish a test-only entrypoint (a separate `exports` subpath or a `*.testkit.ts` not re-exported from the public barrel) and move the fixture there.

- **Story 3.0 · LOW · Guard-before-append doubles the per-call ledger read on the `recordSeen`/`updateFocus` happy path (two `eventsByActor` + fold reads instead of one). — RESOLVED (closed-with-evidence: measured at Story 6.1, acceptable at V1, 2026-05-31).**
  - **Resolution (Story 6.1, Task 6):** `check` (the named hot-path consumer) wires `recordSeen` into EVERY dial-in, so the guard-before-append double-read now runs on the marquee pull-only path. Measured the full `check` op cost (which INCLUDES `recordSeen`'s two `eventsByActor`+fold reads, plus `eventsSince(0)` + the membership/participation/floor folds) over the REAL SQLite ledger at representative V1 sizes (a "busy" actor: member of several boards, participant in many rooms). Result (median / p95, ms): 200 ev → 1.0 / 1.9; 500 → 2.1 / 4.2; 1000 → 3.3 / 5.9; 2000 → 6.6 / 10.8; 5000 → 17.8 / 26.8. At V1 scale (single-file SQLite, a few hundred–few thousand events) the WHOLE check — recordSeen double-read included — is a few ms; `recordSeen`'s incremental contribution (two actor-indexed reads of a small per-identity stream) is a small fraction of that. The double read does NOT show up at V1 scale — closed as measured-acceptable. The optional `skipGuard` fast-path (passing a pre-verified flag from the session-holder-validated tool) is NOT needed at V1 and was deliberately NOT added (it would complicate the no-orphan guarantee for unverified callers for no measured benefit); revisit only if a future much-larger-ledger regime makes presence-ping cost material. [Source: Story 6.1 Dev Agent Record — Task 6 measurement.]
  - **Summary:** The shared `appendIdentityEventOrThrow` helper now reads the actor's stream and folds it ONCE before the append (the existence guard) and again AFTER the append (the read-back), so each happy-path `recordSeen`/`updateFocus` call performs two `eventsByActor(handle)` queries + two `findIdentity` folds, where the old append-then-readback shape did one. `recordSeen` is on a hot path: per the story Dev Notes its first consumer (Story 6.1 `check`) marks presence on EVERY check call.
  - **Rationale:** Not a defect — the pre-append read is exactly what AC #3 mandates ("read the identity back / check registration FIRST"), so the extra read is inherent to the correctness contract, not an oversight. The query is index-scoped to a single actor and the cost is small at V1 scale (single-file SQLite, low identity-event counts). Logged so the `check` consumer's author can weigh it, not because it needs a fix now.
  - **Suggested resolution (DONE):** When Story 6.1 wires `recordSeen` into `check` (a per-call hot path), measure the presence-ping cost; if the double read shows up, fold the existence guard into the read-back or short-circuit the guard when the caller already holds a validated, established identity. → MEASURED at Story 6.1; did not show up at V1 scale; closed without the fast-path.

## Deferred from: code review of story 4.2 (2026-05-31)

- **Story 4.2 · LOW · Stale doc comment on `RoomWire.active` (`packages/mcp-server/src/tools/room-shared.ts`). — RESOLVED inline (Story 4.2 code review, 2026-05-31).**
  - **Resolution:** Rewrote the `RoomWire.active` JSDoc to describe the Story 4.2 activation read-model (`true` iff ≥1 reply exists, derived in `foldRooms`; a still-proto room is `false`), replacing the obsolete "Always `false` until Story 4.3." Comment-only change, zero behaviour impact; Prettier-clean; full gate stayed green (375/57). Logged here for completeness (auto-resolved during review, not carried forward).
  - **Summary:** The Story 4.1 `room-shared.ts` (unchanged since baseline `85247dd`, so strictly outside this story's diff) carried `/** Whether the room is active (a reply exists). Always `false` until Story 4.3. */` on `RoomWire.active`. Story 4.2 semantically invalidated it: the new `foldRooms` `room.replied` branch makes `active = true` reachable NOW (not 4.3), and this story's own integration test asserts `active: true` flowing through `roomToWire`. A future reader of the wire-shape contract would have been misled about when `active` flips.
  - **Rationale:** Trivial, zero-risk doc fix directly entailed by this changeset's semantics; resolved inline rather than deferred. The actual reply WRITE-op (the `reply` tool that appends `room.replied`) is still Story 4.3 — only the activation READ-MODEL moved to 4.2, which is exactly what the corrected comment now reflects.
  - **Suggested resolution:** N/A — resolved.

## Deferred from: code review of story 4.3 (2026-05-31)

- **Story 4.3 (item 4.3-a) · LOW · No call-level negative test that a malformed `room_id` (uppercase / bad slug charset) is rejected at the Zod boundary with nothing appended. — RESOLVED (Story 5.0, 2026-05-31).**
  - **Resolution:** Story 5.0 (Task 1 / AC #1) added family-wide negative-path boundary-rejection cases over the real `Client`↔`McpServer`↔SQLite stack, asserting a malformed `room_id` (`''`, `'   '`, `'Bad Room!'`, `'UPPER'`, `'--leading'`, `'a..b'`) is rejected at the Zod boundary BEFORE the delegate — `isError === true`, `readErrorPayload(result) === undefined` (a Zod rejection, NOT a closed board code — distinct from the well-formed-but-unknown `ROOM_NOT_FOUND` path), `maxSeq` unchanged, and the relevant event count untouched. Evidence: `reply.integration.test.ts` ("rejects a malformed room_id … the real room.replied is untouched" — establishes a genuine `room.replied`=1 via a real reply first, then proves each malformed reply leaves it at 1); `add-participant.integration.test.ts` ("rejects a malformed room_id OR a malformed handle …" — also covers a malformed `handle` against the shared `handleSchema` `^[a-z0-9._@-]+$`, real `room.participant_added`=1 untouched); plus a READ-side parity spot-check in `read-room.integration.test.ts` (same `roomIdSchema`, `isError` not `ROOM_NOT_FOUND`). +3 integration cases, no production-source change.
  - **Summary:** `reply` introduces a NEW `roomIdSchema` (`packages/mcp-server/src/tools/room-shared.ts`) whose slug regex `^[a-z0-9]+(?:-[a-z0-9]+)*$` is byte-identical to the existing `projectIdSchema` (`project-shared.ts`). The `reply.integration.test.ts` discovery-surface case asserts the schema's `properties`/`required` are present, but no test drives a tool call with a syntactically-invalid `room_id` (e.g. `Not-A-Slug`, `UPPER`, `--leading`, `a..b`) and asserts the SDK rejects it before the delegate with nothing appended. The directly-parallel sibling tool's suite (`post-announcement.integration.test.ts:487`) DOES establish a boundary-rejection test (empty subject/body via `.min(1)`), so the pattern exists and `reply` omits the `room_id` syntactic-rejection analog.
  - **Rationale:** Not a defect and not blocking. (1) The regex is correct by parity with a validator already shipped and exercised across Epic 3/4; (2) across the entire mcp-server suite, NO tool currently has a *syntactic* slug-rejection call test (the `project_id` negative tests are all *semantic* — unknown id → BOARD_NOT_FOUND, or slug-to-empty), so the gap is consistent with codebase-wide precedent, not a 4.3 regression; (3) the empty-`body` boundary is transitively covered because `reply` reuses the exact `announcementBodySchema` that 4.1 tests. The SDK's reject-before-delegate mechanism is itself proven by `register-tool.ts` + other tools' tests. Auto-fixing was declined to avoid gold-plating a provably-correct-by-identity validator against a coverage bar the codebase does not set.
  - **Suggested resolution:** When the next story touches the room tools (4.4 `read_room` / 4.5 `add_participant`, which will add more slug-id params), add one shared negative-path integration case asserting a malformed slug `room_id`/`project_id` is rejected at the boundary (`isError` true, ledger `maxSeq` unchanged), covering the whole family at once. Alternatively fold a small unit test over `roomIdSchema.safeParse(...)` rejecting a representative bad-charset id. Until then, the discovery-surface assertion + the `projectIdSchema` parity are sufficient.

- **Story 4.3 (item 4.3-b) · LOW · No explicit max-length boundary test for `room_id` (`ROOM_ID_MAX_LENGTH`=200) / reply `body` (`ANNOUNCEMENT_BODY_MAX_LENGTH`=16_000). — BODY-CAP HALF RESOLVED (Story 5.1, 2026-05-31); the `room_id` cap-edge residual carries forward as item 5.1-roomid-cap-edge (below).**
  - **Resolution (body-cap half, Story 5.1):** Story 5.1 replaced the interim `body` char cap (`.max(16000)`, a code-less Zod error) with the FORMAL 256 KB UTF-8 BYTE cap enforced in CORE (`assertBodyWithinCap` / `MAX_BODY_BYTES`) returning the closed `BODY_TOO_LARGE` code, and added the cap-edge tests: a body EXACTLY at the cap is accepted and stored verbatim; one byte over throws `BODY_TOO_LARGE` with NOTHING appended — for BOTH `reply` and `post_announcement`, at the core-unit level AND over the real `Client`↔`McpServer`↔SQLite stack (`body-cap.integration.test.ts`). QA additionally pinned the multi-byte-boundary edge (exact-at-cap with an emoji/CJK tail passes; +1 whole multi-byte char crosses), proving BYTE (not char) semantics. The interim `ANNOUNCEMENT_BODY_MAX_LENGTH` was cleanly retired (no dangling references). Verified at the Story 5.1 code review (full honest gate green; lint 0 / build 7-7 / typecheck 0 / test 500-71 / format clean).
  - **Owner note (superseded):** Story 5.0 triage assigned the whole item to 5.1; 5.1 closed the body-cap half (the part that mattered — the interim char cap is gone). The `room_id` length-cap edge (`ROOM_ID_MAX_LENGTH`=200, UNAFFECTED by the body-cap replacement) was NOT exercised by 5.1 and is split out as its own residual item (5.1-roomid-cap-edge) so this entry's resolution is unambiguous.
  - **Summary:** Both Zod validators carry a `.max(...)` defensive cap (`room-shared.ts`), but no test exercises the at-cap / over-cap boundary (id or body of exactly the cap length accepted; cap+1 rejected at the boundary, nothing appended). The NFR6 hostile-body integration test exercises a multi-KB body successfully (well under 16k), but does not probe the cap edge.
  - **Rationale:** Not a defect and not blocking. These are sane defensive bounds (a sanity cap, not a product constraint — the formal 256 KB `BODY_TOO_LARGE` cap is the deferred Epic 5 Story 5.1 concern), shared with the already-shipped `post_announcement` tool; the `.max` constraint is standard Zod with no custom logic. The malformed-charset half of this coverage class (former item 4.3-a) is now CLOSED by Story 5.0; the remaining cap-EDGE half is Story 5.1's (it re-homes the body-size boundary).
  - **Suggested resolution:** Story 5.1 adds the cap-edge cases when it replaces the interim body cap with the formal 256 KB / `BODY_TOO_LARGE` (it re-homes the body-size coverage and can add the `room_id` id cap-edge at the same time). Until then, leave as-is.

## Deferred from: Epic 3 retrospective (2026-05-31)

- **Epic 3 (process) · LOW · `tsc` typecheck (and forked-process tests) still resolve `@agentbbs/core` via `dist`, so build-first is NOT fully eliminated — the Story 3.0 Vitest `src` alias fixed in-Vitest tests only. — RESOLVED (Story 4.0, 2026-05-31) for the typecheck half; forked-worker `dist` dependency is inherent and remains covered by gate ordering (see Resolution).**
  - **Resolution:** Story 4.0 (Task 1 / AC #1) resolved the TYPECHECK half by adding a `paths` mapping to `tsconfig.typecheck.json` (the single non-composite typecheck config — NOT `tsconfig.base.json`) that maps each `@agentbbs/<pkg>` specifier to that package's `src/index.ts`, mirroring exactly the Story 3.0 Vitest `resolve.alias` (same five package roots). `pnpm run typecheck` now resolves cross-package `@agentbbs/*` from live source and no longer needs a prior build. Evidence: from a no-`dist` state, `pnpm run typecheck` went from RED (exit 2; 172 errors, 70 of them `TS2307` "Cannot find module '@agentbbs/core'" cross-package module-not-found) to GREEN (exit 0; 0 errors); `--traceResolution` confirmed `@agentbbs/core` → `packages/core/src/index.ts`. The mapping carries NO `baseUrl` (deprecated `TS5101` in the installed TS 6.0.3; `paths` works without it when targets are written relative, leading `./`), so it is future-proof past TS 7.0 and emits no deprecation. Build-honesty preserved: `pnpm run build` (per-package `tsc -b` against the real `exports`/`dist`) stays in the gate as the shipped-`dist` guard, so the typecheck-only `paths` cannot mask a broken barrel `exports`. — The FORKED-WORKER half (`register-race`/`concurrency` spawn real Node processes that resolve `@agentbbs/core` via `exports` → `dist`, NOT any alias/`paths`) is INHERENT to real-process resolution and is deliberately NOT changed by this story; those tests self-build their worker `dist` in `beforeAll` (BUILD-IF-MISSING/STALE), and the standard `build`-before-`test` gate ordering remains the documented mitigation (`project-rules.md` §2 caveat). Not a regression; the only remaining `dist`-before-everything dependency in the repo.
  - **Summary:** Story 3.0's `resolve.alias` removed the build-first requirement for the Vitest suite, but two paths still resolve `@agentbbs/*` through the package `exports` → `dist`: (1) `pnpm run typecheck` (`tsc --noEmit`) reads each package's `dist/*.d.ts`, so a newly-added cross-package `core` export trips `TS2305` until `pnpm run build`; every Epic 3 dev/QA/CR stage hit this transient false-red and built first. (2) The forked cross-process worker tests (`register-race`, `concurrency`) spawn real Node processes that resolve `@agentbbs/core` via `exports` → `dist` (NOT the Vitest alias) — a deleted/stale `core/dist` makes the worker die `ERR_MODULE_NOT_FOUND` (surfaced by the Story 3.0 lead smoke; now caveated in `project-rules.md` §2). The standard gate (`build` before `test`/`typecheck`) covers both, so this is a friction/papercut, not a correctness defect.
  - **Rationale:** The user opted to "keep the cadence" at the Epic 3 retro rather than fix this now — the build-first dance for typecheck is a minor, well-understood step covered by the gate ordering. A fuller fix is a tooling change (out of any single story's scope), so it is logged for the Epic 4 Story 4.0 triage to decide.
  - **Suggested resolution (Epic 4 Story 4.0 to weigh):** add a `pretypecheck` build (or `tsc -b` project references with `paths` mapping `@agentbbs/*` → `src` for the typecheck tsconfig) so typecheck resolves cross-package source the way the Vitest alias does; for the forked workers, keep the build-first gate (their real-process resolution genuinely needs `dist`). If neither is judged worth the complexity, leave the gate ordering as the documented mitigation and close this as won't-fix.

- **Epic 3 (contract) · LOW · The MCP tool NAMES are not contract-pinned — `list_members`, and the `{ projects: [...] }` / `{ members: [...] }` result envelopes, were chosen by the implementer, not ratified against a tool-contract doc. — RESOLVED (Story 7.0, 2026-05-31).**
  - **Resolution:** Story 7.0 (AC #1/#2) ratified the full agent-facing surface in `docs/mcp-tool-contract.md`: all 17 registered tools enumerated with name / one-line purpose / snake_case wire params / `structuredContent` envelope / error codes (the `list_members` name and the `{ projects }`/`{ members }`/`{ rooms }`/`{ announcements }`/`{ room }`/`{ room, messages }`/`{ room_id, contract }`/`{ message_seq, reactions }`/`{ announcements, messages, cursor }` envelopes are now the RATIFIED contract, not implicit), plus the CLOSED 10-code error set, the CLOSED 10-event vocabulary, and the open-read (FR9) / membership-gated-write / participation-gated-action / grant-on-act access model. A DRIFT-GUARD test (`packages/mcp-server/src/tool-contract.drift.test.ts`) pins the doc's canonical tool-name list to the tools the REAL `McpServer` registers (live `Client.listTools()` — Rule 3 real-runtime evidence) AND the doc's §4 closed-code table to `BOARD_ERROR_CODES`, so any future add/rename of a tool or error code without a matching doc edit FAILS the gate. The contract is no longer set implicitly story-by-story — it is ratified once and self-enforcing. The doc is NFR8 open-source-ready and cross-linked from the README. No tool was renamed (the agent population is zero, but the established convention was judged sound and ratified as-is).
  - **Summary:** The PRD addendum (§C) fixed tool FIELD SHAPES but not the full 12-tool NAME list. Epic 3 named `announce_project`, `list_projects`, `join_board`, `list_members` by convention (and chose `{ projects }`/`{ members }` envelopes because MCP `structuredContent` must be an object). These are the stable agent-facing contract; renaming later is a breaking change for every agent. Story 3.4 explicitly flagged `list_members` (vs `get_board_directory`) as a reviewer-may-rename decision.
  - **Rationale:** The names are reasonable and internally consistent (parallel `list_*` reads, `*_project`/`*_board` writes). No defect — but the contract was being set implicitly story-by-story rather than ratified once. Epic 7's `mcp-tool-contract.md` (the now-17-tool surface + names + envelopes + closed error codes) was the natural place to ratify or rename → done in Story 7.0.
  - **Suggested resolution:** N/A — resolved (Story 7.0 ratified the names + envelopes + closed codes as-is and added the drift guard).

## Deferred from: code review of story 4.4 (2026-05-31)

No carry-forward deferrals. Story 4.4 (`read_room` tool + `roomMessages` history projection + `readRoom` op) was APPROVED with 0 HIGH / 0 MED / 0 decision-needed and **0 defer-to-backlog**. The single LOW raised (a test-count drift in the tracking docs — stated +22/416/7-integration, measured +24/418/9-integration after 2 QA-addition integration tests) was **auto-resolved inline** in the story file + `sprint-status.yaml`, not deferred. All 6 ACs + all 9 directive verifications + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed clean; honest gate re-run green (lint / build 7-7 / typecheck / test 418-63 / format). This entry is recorded for the audit trail (a clean review with nothing carried forward).

- **Note on the two OPEN Story 4.3 room-tool test-hardening items (above):** Story 4.3 deferred (a) a malformed-`room_id` boundary-rejection call test and (b) a `room_id`/`body` max-length cap-edge test, with the suggested resolution "add when the room tools are next touched (4.4 `read_room` / 4.5)." Story 4.4 touched the room tools and reused the SAME `roomIdSchema` (its `read_room` input), but did NOT add either test (its integration suite asserts the discovery-surface `room_id` property + the unknown-but-well-formed → `ROOM_NOT_FOUND` semantic path, not a syntactic-slug rejection or a cap edge). So both 4.3 items remain **OPEN** and carry forward to Story 4.5 `add_participant` (the next room tool, which adds more slug-id params) — the family-wide negative/cap-edge case is still best added once there. Not re-filed as a 4.4 finding: `read_room` adds no new validator (it reuses the 4.3 `roomIdSchema`, correct by parity with `projectIdSchema`), and the gap is consistent with codebase-wide precedent (no mcp-server tool pins a syntactic slug-rejection call test today).

## Deferred from: code review of story 4.5 (2026-05-31)

- **Story 4.5 · LOW · "12th/final V1 tool" label drift. — RESOLVED (Story 7.0, 2026-05-31).**
  - **Resolution:** Story 7.0 ratified the tool count at **17** in `docs/mcp-tool-contract.md` and added the drift-guard test that asserts the documented canonical tool-name list equals the live `McpServer.listTools()` set — so the count is now pinned by an executable gate, not prose. The user-facing instances of the stale count were corrected: the README "Agents only ever see these 12 tools" prose now references the 17-tool contract (and adds `update_focus` / `list_members` / `unreact` / `read_contract` to the headline table), and the stale "(the 12 tools)" architecture-tree comment was made count-agnostic. The remaining "12th/final V1 tool" mentions in code comments (`server.ts`, `server.bootstrap.test.ts`) and historical story files are internal narrative whose authoritative correctness gate (the bootstrap exhaustive sorted tool-list assertion, now joined by the drift guard) lists all 17 and is green; they were intentionally NOT churned (no behaviour impact, and the drift guard is the durable source of truth). The cosmetic count drift can no longer reach a user-facing surface without failing the gate.
  - **Summary:** `add_participant` is documented as the "12th/final V1 tool" (the story file, the `server.ts` doc comment, the `server.bootstrap.test.ts` comment, and the `sprint-status.yaml` note) but it is the **13th** distinct registered tool. The surface grew to **17** with `react`/`unreact` (5.2), `read_contract` (5.3), and `check` (6.1). The "12" derives from the PRD/architecture §7 canonical planning surface — an approximate count, not the registered count.
  - **Rationale:** Cosmetic, doc-only; no code impact. The actual correctness gate (the **exhaustive** sorted tool-list assertion in `server.bootstrap.test.ts`) lists all real tools + the 2 representatives and is green. Epic 7's contract was the natural point to ratify the full registered tool list (alongside `E3-tool-names`) → done in Story 7.0.
  - **Suggested resolution:** N/A — resolved (count pinned at 17 + drift-guarded; user-facing prose corrected).

- **Note on the two OPEN Story 4.3 room-tool test-hardening items (still carried forward):** Story 4.5 `add_participant` was the next room tool and reused BOTH shared validators (`roomIdSchema` from `room-shared.ts` for `room_id`, `handleSchema` from `identity-shared.ts` for `handle`) without adding a new one. Its integration suite asserts the discovery-surface params (`room_id`, `handle`) + the semantic error paths (HANDLE_NOT_FOUND / ROOM_NOT_FOUND / NOT_A_MEMBER / NO_IDENTITY), but — consistent with codebase-wide precedent — did NOT add (a) a syntactic malformed-`room_id` boundary-rejection call test or (b) a `room_id`/`handle` max-length cap-edge test. So both Story 4.3 items remain **OPEN**. The code-review did not auto-add them: the validators are correct by parity with `projectIdSchema`/the identity tools (both already shipped + exercised across Epics 2–4), no mcp-server tool currently pins a syntactic slug-rejection call test, and adding them now would gold-plate a provably-correct-by-identity validator against a coverage bar the codebase does not set. Suggested resolution unchanged: fold one shared family-wide negative/cap-edge case over `roomIdSchema`/`handleSchema` (`.safeParse` unit, or a boundary-rejection integration case asserting `isError` + `maxSeq` unchanged) into the next room/identity-tool touch, or into the Epic 4 retrospective's test-hardening sweep.

## Deferred from: code review of story 4.6 (2026-05-31)

Story 4.6 (the per-(identity, room) JOIN-CURSOR projection — `roomJoinSeq` + `roomMessagesSince`, the floor Story 6.1 `check` consumes) was **APPROVED with 0 HIGH / 0 MED / 0 decision-needed and 0 defer-to-backlog**. All 6 ACs + all 8 directive verifications + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed clean; honest gate re-run green (lint / build 7-7 / typecheck / test **475-69** / format `--check`). **Epic 4 is COMPLETE after this story** — whole suite green end-to-end (all 12 V1 tools + the room/identity/project projections + this join-cursor floor). The two LOW items below were **dismissed** (not carried to backlog) but are recorded here for the audit trail — the second is a forward-contract note Story 6.1's author should see.

- **Story 4.6 · LOW · `roomJoinSeq` re-implements `participants.ts`'s participant-event filtering inline rather than reusing it. — DISMISSED (by-design).**
  - **Summary:** `roomJoinSeq` (`packages/core/src/rooms/join-cursor.ts:96-119`) folds `room.replied` (actor match) + `room.participant_added` (payload.handle match) for the room directly, instead of importing from `participants.ts` (`roomParticipants`/`isParticipant`), which folds the SAME two event kinds.
  - **Rationale:** The two projections compute DIFFERENT things — `roomParticipants` returns the membership SET in first-seen order (via a `Set`); `roomJoinSeq` returns the MIN `seq` (a scalar floor). A shared helper would couple two distinct return shapes and add indirection for negligible gain; the shared predicate is a trivial two-case `switch` on a `roomId` match, and both files' headers cross-document the relationship. Not a defect, not auto-changed (no behaviour benefit; would churn a correct, clean projection).
  - **Suggested resolution:** None required. If a third consumer of the identical "is this handle a participant via reply-or-add, and at what seq" predicate ever appears, consider extracting a single `participatingEvents(events, roomId, handle)` primitive that both `roomParticipants` and `roomJoinSeq` consume. Until then, leave as-is.

- **Story 4.6 · LOW · Non-participant `roomJoinSeq` is `undefined`; Story 6.1 `check` must gate per-room on `!== undefined` and NOT `?? 0` a non-participant (that would flood). — DISMISSED (out-of-scope; documented 6.1 contract / forward-note).**
  - **Summary:** A non-participant's `roomJoinSeq` returns `undefined` (AC #5). `roomMessagesSince`'s signature is `sinceSeq: number`, so it can never be called with `undefined` in-process — but a consumer that naively coalesced `roomJoinSeq(...) ?? 0` for a non-participant would pass `0` and receive the room's FULL history (a back-history flood — the exact failure 4.6 prevents for participants). Design decision 5 specifies a non-participant is "simply not in `check`'s room scope," i.e. 6.1 must skip the room entirely (gate on `roomJoinSeq !== undefined`) rather than floor at 0.
  - **Rationale:** This is Story 6.1's `check` contract, correctly forward-declared in 4.6's docblocks + Design decision 5 — NOT a 4.6 defect. The `number` signature already prevents the misuse inside this story, and every `?? 0` in the 4.6 test suite is applied only to participants whose cursor is guarded `expect(cursor).toBeDefined()` first (so the fallback is never actually taken). The QA "combined floor" preview (`packages/data-access/src/join-cursor.integration.test.ts:284-352`) exercises the participant path in both `max(checkCursor, joinSeq)` directions but does NOT cover the non-participant-skip path.
  - **Suggested resolution:** When Story 6.1 (`check`) lands, its per-room loop MUST: (1) compute `roomJoinSeq`; (2) if `undefined`, SKIP the room (the actor is not a participant → not in scope — do NOT `?? 0`); (3) else apply `seq > max(perIdentityCheckCursor, roomJoinSeq)`. Add a 6.1 integration case asserting a non-participant's `check` does NOT surface that room at all (the missing arm of the combined-floor preview). The floor-composition primitive (`max` with a participant's defined `joinSeq`) is already proven by 4.6's preview test; only the non-participant skip is new to 6.1.

- **Carried-forward OPEN items for the Epic 4 retrospective triage (Story 4.6 did NOT resolve any — it is a pure CORE projection with NO room MCP tool, NO new validator, NO `server.ts` change):**
  - **1.5 OPEN** — append-invariant lint guard disabled in `*.test.ts` (test-helper SQL relies on review). Unaffected by 4.6.
  - **1.6 OPEN** — `wireToPayload` does not validate a known-type-but-malformed payload row. Unaffected by 4.6.
  - **3.0-b OPEN** — guard-before-append doubles the per-call ledger read on `recordSeen`/`updateFocus` (→ Story 6.1, the `check` hot-path consumer). NOT touched by 4.6 (which adds no append path); still lands with 6.1.
  - **E3-tool-names OPEN** — MCP tool names not contract-pinned (→ Epic 7 `mcp-tool-contract.md`). 4.6 adds NO MCP tool, so nothing new to pin.
  - **4.6-a RESOLVED (closed-with-evidence: measured + contained optimization at Story 6.1, 2026-05-31)** (raised at the Epic 4 retrospective, 2026-05-31; carried at the Story 5.0 triage) — `roomJoinSeq`/`roomMessagesSince` fold the full event stream per call (per-room read cost). SAME class as 3.0-b. **Resolution:** Story 6.1's `check` composes the per-board + per-room floors per dial-in. The naive shape (calling `boardJoinSeq`/`roomJoinSeq`/`roomMessages` once PER ROOM, each a full-stream scan) measured super-linear: 200 ev → 1.5 ms median, 1000 → 10.4, 2000 → 42.0, 5000 → 140.7 (p95 168). A CONTAINED optimization was applied in `check.ts`: the actor's per-board + per-room join floors are computed in ONE pass over the stream (the batched equivalent of `boardJoinSeq`/`roomJoinSeq`, which stay the canonical single-scope primitives), and the message scope folds `roomMessages` ONLY for the actor's PARTICIPATED rooms (a `Map` of `{roomId → floor}`), never every room. Re-measured: 200 → 1.0 ms, 1000 → 3.3, 2000 → 6.6, 5000 → 17.8 (p95 26.8) — ~8× faster at 5000 and a much flatter curve; cheap at every V1 size (a few ms for a few hundred–few thousand events). The append invariant is preserved (no stored derived position — the floors are still DERIVED-by-query, just batched). Closed as measured-acceptable with the optimization landed. [Source: Story 6.1 Dev Agent Record — Task 6.]
  - **4.5-tool-label OPEN** (cosmetic) — the "12th/final V1 tool" wording for `add_participant` is an off-by-one vs the 13 registered tools (see the "code review of story 4.5" entry above). Doc-only; the exhaustive bootstrap tool-list assertion is the source of truth and is green. Reconcile when `react`/`check` land / at the Epic 7 contract. Story 5.0 left it (out of scope — not the actionable carry-forward).
  - **4.3-(a) RESOLVED (Story 5.0, 2026-05-31) / 4.3-(b) OPEN → Story 5.1** — malformed-`room_id` boundary-rejection call test + `room_id`/`body` max-length cap-edge test. Story 4.6 added NO MCP tool and NO new Zod validator, so it could not attach the family-wide cases. **Story 5.0** (the Epic 4 deferred-cleanup story) then closed **4.3-a** with malformed-`room_id`/`handle` Zod-boundary-rejection tests over the real stack for `reply`/`add_participant` (+ a `read_room` READ-side parity spot-check); see the resolved entry under "code review of story 4.3" above. **4.3-b** (the cap-EDGE test) is now **OWNED BY Story 5.1**, which replaces the interim body char cap with the formal 256 KB / `BODY_TOO_LARGE` and tests the cap edge there (testing the interim cap now would be thrown away). **UPDATE (Story 5.1 code review, 2026-05-31):** 4.3-b's BODY-cap half is now RESOLVED by Story 5.1 (see "code review of story 5.1" below); the residual `room_id` cap-edge is split out as item 5.1-roomid-cap-edge.

## Deferred from: code review of story 5.1 (2026-05-31)

Story 5.1 (the formal 256 KB `BODY_TOO_LARGE` body cap + verbatim guarantee) was **APPROVED with 0 HIGH / 0 MED**. All 4 ACs + the 7 directive verifications + the 3 adversarial layers re-confirmed clean; honest gate re-run green (lint 0 / build 7-7 / typecheck 0 / test **500-71** / format `--check`). The core cap (`assertBodyWithinCap` / `MAX_BODY_BYTES`) throws the closed `BODY_TOO_LARGE` BEFORE any append for BOTH `reply` and `post_announcement`; bodies are stored byte-for-byte verbatim end-to-end (proven over the real `Client`↔`McpServer`↔SQLite stack at exactly 256 KB). The `Buffer.byteLength` typing in core was handled with a minimal local ambient `declare const Buffer` (NOT `@types/node`) — verified load-bearing: core fails `tsc -b` with `TS2591` without it, confirming `@types/node` is genuinely absent from core's type closure and the module boundary is preserved. Two items below: one residual OPEN (carried), one comment fix auto-RESOLVED at review.

- **Story 5.1 (item 5.1-roomid-cap-edge) · LOW · No explicit cap-edge test for `room_id` (`ROOM_ID_MAX_LENGTH`=200). — OPEN (residual of 4.3-b after the body-cap half was resolved).**
  - **Summary:** `roomIdSchema` (`packages/mcp-server/src/tools/room-shared.ts`) carries a `.max(200)` defensive cap, but no test drives a 200-/201-char `room_id` to assert the at-cap value is accepted and a cap+1 value is rejected at the Zod boundary with nothing appended. Story 5.1 closed the BODY cap edge (the part that mattered — it re-homed the body-size boundary to core) but did not exercise the unrelated `room_id` length cap.
  - **Rationale:** Not a defect and not blocking. `ROOM_ID_MAX_LENGTH`=200 is a sane defensive bound (a sanity cap, not a product constraint), is unaffected by the body-cap replacement, and is standard Zod with no custom logic. In practice a `room_id` is an op-allocated slug (subject slug + disambiguator), so a 200-char id is not reachable via the post path; the cap guards only a hand-crafted `reply`/`read_room` call. Codebase-wide, NO tool currently has a syntactic length cap-edge call test, so the gap is consistent with precedent, not a 5.1 regression.
  - **Suggested resolution:** Fold one shared cap-edge case (at-cap `room_id`/`project_id` accepted, cap+1 rejected at the boundary, `maxSeq` unchanged) into the next room-tool touch, or a small unit test over `roomIdSchema.safeParse(...)`. Until then, leave as-is — the charset rejection that mattered is covered (Story 5.0, item 4.3-a) and the byte body cap is covered (Story 5.1).

- **Story 5.1 · LOW · Stale "defers the formal cap" comments in the `reply` / `post_announcement` MCP tool headers. — RESOLVED (auto-fixed at the Story 5.1 code review, 2026-05-31).**
  - **Summary:** The header comments of `packages/mcp-server/src/tools/reply.ts` (lines 21-24) and `packages/mcp-server/src/tools/post-announcement.ts` (lines 17-19) still asserted "The 256 KB body cap / BODY_TOO_LARGE is Epic 5 Story 5.1 — `room-shared.ts` uses a sane interim cap and explicitly defers the formal cap; this story introduces no body-size code." That became FALSE the moment Story 5.1 landed the cap: the body schema's interim char `.max` was dropped and the formal cap now lives in core (`assertBodyWithinCap`), reached through these very tools. A future reader of the tool contract would have been misled about where the cap lives.
  - **Resolution:** Rewrote both header comments to state that the body is validated non-empty at the Zod boundary and the formal 256 KB `BODY_TOO_LARGE` cap is enforced in core (`assertBodyWithinCap`) before any append. Comment-only change, zero behaviour impact; Prettier-clean; the full gate stayed green (500/71). Logged here for completeness (auto-resolved during review, not carried forward).
  - **Suggested resolution:** N/A — resolved.

## Deferred from: code review of story 5.3 (2026-05-31)

Story 5.3 (FR21, the marquee Epic 5 capability — the `currentContract` projection + `readContract` core read op + the `read_contract` MCP open-read tool: the room's CURRENT AGREED CONTRACT, the highest-`seq` message currently holding a live 👍, COMPUTED by query and NEVER stored) was **APPROVED with 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog**. One LOW (a Dev-Agent-Record test-count drift) was **auto-resolved inline** in the story file + `sprint-status.yaml`, not carried forward. All 5 ACs + all 9 directive verification points + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed clean; honest gate re-run GREEN (lint 0 / build 7-7 / typecheck 0 / test **575-78** / format `--check`; no `.only`/`.skip`/`.todo`). **EPIC 5 IS COMPLETE after this story** — the whole suite is green end-to-end (the verbatim message + 256 KB cap, react/unreact live-reaction projection, and now the computed contract + `read_contract` tool). This entry is recorded for the audit trail (a clean review with nothing carried forward).

- **Story 5.3 · LOW · Dev-Agent-Record test-count drift (572 → 575; breakdown "16 unit + 5 op + 6 integration" → 19 + 4 + 6). — RESOLVED inline (Story 5.3 code review, 2026-05-31).**
  - **Summary:** The Completion Notes / Change Log stated 572 passed and +27 = "16 `contract.test.ts` + 5 `read-contract.test.ts` + 6 integration". The measured suite is **575 passed / 78 files** and the real new-test breakdown is **19 + 4 + 6 = 29** (QA appended 3 cases to `contract.test.ts` — the multi-level ≥3 reversion + the two highest-seq-not-most-reactors tests — after the dev's draft count, 16→19; and the op file `read-contract.test.ts` carries 4 it() blocks, 2 describe × 2, not 5). Net +29 over the post-5.2 baseline of 546 = 575. Code unaffected — identical in shape to the Story 4.4 review's auto-resolved test-count correction.
  - **Resolution:** Corrected the Completion Notes + Change Log figures (572→575, +27→+29, 16+5+6→19+4+6) and the `sprint-status.yaml` story-5-3 dev/review note. Doc-only; the suite itself was already at 575/78. Auto-resolved during review, not carried forward.
  - **Suggested resolution:** N/A — resolved.

- **Carried-forward OPEN items for the Epic 5 retrospective triage (Story 5.3 did NOT resolve any — it is a read-only CORE projection + read tool with NO new MCP validator, NO new event type, NO new error code, and NO append path):**
  - **1.5 OPEN** — append-invariant lint guard disabled in `*.test.ts` (test-helper SQL relies on review). Unaffected by 5.3.
  - **1.6 OPEN** — `wireToPayload` does not validate a known-type-but-malformed payload row. Unaffected by 5.3.
  - **3.0-b RESOLVED (Story 6.1, 2026-05-31)** — guard-before-append doubles the per-call ledger read on `recordSeen`/`updateFocus` (the `check` hot-path consumer). MEASURED at Story 6.1 (the full `check` op incl. recordSeen's double-read: ~1–18 ms median across 200–5000 events over real SQLite); did not show up at V1 scale → closed measured-acceptable, no fast-path added. See the resolved entry under "code review of story 3.0" above.
  - **4.6-a RESOLVED (Story 6.1, 2026-05-31)** — `roomJoinSeq`/`roomMessagesSince` fold the full event stream per call (per-room read cost). MEASURED + a CONTAINED optimization applied in `check.ts` (per-board/per-room floors computed in one pass; message scope folds only participated rooms): re-measured ~1–18 ms median across 200–5000 events (~8× faster than the naive per-room-scan shape at 5000). The append invariant is preserved (floors stay DERIVED-by-query, batched). Note: `currentContract`/`readContract` share the SAME per-call full-stream fold shape (one `eventsSince(0)` + a `roomMessages`/`liveReactors` pass) — the same DERIVED-by-query architecture; the Story 6.1 measurement is the standing evidence that this fold class is cheap at V1 scale. See the resolved entry under the "code review of story 4.6" Epic 4 carry-forward above.
  - **5.1-roomid-cap-edge OPEN** — no at-cap (200) / cap+1 (201) call test for `room_id` `ROOM_ID_MAX_LENGTH`. Story 5.3's `read_contract` reuses the shared `roomIdSchema` (its only input) WITHOUT adding a new validator, consistent with codebase-wide precedent (no mcp-server tool pins a syntactic length cap-edge call test). Fold one shared cap-edge case into the next room-tool touch / the Epic 7 contract.
  - **E3-tool-names OPEN → Epic 7 `mcp-tool-contract.md`** — MCP tool names not contract-pinned. Story 5.3 names `read_contract` + the `{ room_id, contract }` envelope by the established convention (`read_*` for reads, an object-wrapped result because MCP `structuredContent` must be an object); ratify or rename in Epic 7 while the agent population is still zero. The bootstrap exhaustive sorted tool-list assertion (`server.bootstrap.test.ts`) is the source of truth meanwhile.

## Deferred from: code review of story 6.2 (2026-05-31)

Story 6.2 (the Epic-6-closing story — DOCUMENTS + reaffirms the pull-only / bounded / dead-letter `check` contract that Story 6.1 already implements; **ZERO new production code** — no new MCP tool / event type / error code / push surface; `check.ts` core+tool and `server.ts` unchanged) was **APPROVED with 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog**. One LOW (a doc tool-count inaccuracy) was **auto-resolved inline** in `docs/pull-only-delivery.md`, not carried forward. All 4 ACs + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed clean; honest gate re-run GREEN end-to-end (lint 0 / build 7-7 / typecheck 0 / test **615-88** / format `--check`; no `.only`/`.skip`/`.todo`). The bounded (per-board + per-room floor, ABSOLUTE + SCALE-INVARIANCE), no-push (structural grep of all production `mcp-server/src` for push CALL forms → 0, + behavioural zero-notification over a real client), and dead-letter-persists (durability-without-delivery: Z never checks → `announcement.posted` stays in the ledger + open-read-visible to a non-member operator) tests all run over the real `createDataAccess` SQLite ledger (Rule 3). **EPIC 6 IS COMPLETE after this story** — the whole suite is green end-to-end (the SM1 zero-relay loop: `check` pull-only dial-in + stored cursor + per-scope floors, now documented + proven bounded/no-push/dead-letter). This entry is recorded for the audit trail (a clean review with nothing new carried forward).

- **Story 6.2 · LOW · Doc tool-count inaccuracy ("12 tools") in `docs/pull-only-delivery.md`. — RESOLVED inline (Story 6.2 code review, 2026-05-31).**
  - **Summary:** The new contract doc asserted "every one of the 12 tools" (§1) and "without changing the 12 tools agents already see" (§5/V2-path), but `server.ts` registers **17** tools (register, login, update_focus, announce_project, list_projects, join_board, list_members, post_announcement, list_announcements, list_rooms, reply, read_room, add_participant, react, unreact, read_contract, check). This is a fresh instance of the same repo-wide tool-count-label drift tracked below as `4.5-tool-label` (the "12th/final V1 tool" wording) and to be ratified under `E3-tool-names` (→ Epic 7 `mcp-tool-contract.md`). NOT load-bearing — the no-push property holds for any N — but it would mislead an outside developer (NFR8 open-source readiness).
  - **Resolution:** Edited both occurrences to count-agnostic phrasing ("every one of its tools" / "the tools agents already see") so the doc is accurate today AND does not bake in a number Epic 7's contract-pinning may itself adjust. Re-checked: no other tool-count claim remains in the doc; `prettier --check` clean; full gate re-run green. Doc-only; auto-resolved during review, not carried forward.
  - **Suggested resolution:** N/A — resolved. (The underlying convention ratification still lands in Epic 7 via `4.5-tool-label` / `E3-tool-names`.)

- **Carried-forward OPEN items for the Epic 6 retrospective triage (Story 6.2 introduced NO new deferred items — it is a docs + reaffirming-tests story with ZERO production code):**
  - **1.5 OPEN** — append-invariant lint guard disabled in `*.test.ts` (test-helper SQL relies on review). Unaffected by 6.2 (its 4 new test files append nothing via raw SQL; they drive the real tools + read out-of-band via the `DataAccess` port).
  - **1.6 OPEN** — `wireToPayload` does not validate a known-type-but-malformed payload row. Unaffected by 6.2.
  - **5.1-roomid-cap-edge OPEN** — no at-cap (200) / cap+1 (201) call test for `room_id` `ROOM_ID_MAX_LENGTH`. 6.2 adds NO MCP tool / validator. Fold one shared cap-edge case into the next room-tool touch / the Epic 7 contract.
  - **E3-tool-names OPEN → Epic 7 `mcp-tool-contract.md`** — MCP tool names not contract-pinned. 6.2 adds NO MCP tool. The bootstrap exhaustive sorted tool-list assertion (`server.bootstrap.test.ts`) is the source of truth meanwhile.
  - **4.5-tool-label OPEN (cosmetic) → Epic 7** — the V1 tool-count label drift (originally the "12th/final V1 tool" wording for `add_participant`; now also the "12 tools" prose in `docs/pull-only-delivery.md`, fixed inline above). The actual registered count is **17**. Doc-only; the exhaustive bootstrap tool-list assertion is the source of truth and is green. Ratify the exact count + names in the Epic 7 `mcp-tool-contract.md`.

## Deferred from: code review of story 7.3 (2026-05-31)

Story 7.3 (the FR27 agent-prompt snippet — `integration/bmad/agent-prompt-snippet.md`: copy-pasteable system-prompt text the Epic 8 install kit inlines, covering identity bootstrap + the post-step `check` cadence + the four-move Negotiation Protocol; **DOCUMENTATION ONLY**, board enforces none of it; the LAST Epic 7 story) was **APPROVED with 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog**. **ZERO production-logic change** — the changeset is the new snippet, the content-guard test, two README cross-links, and tracking docs; NO non-test `*.ts` under `packages/*/src/` (the only `.ts` is the content guard). All 3 ACs + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed clean; honest gate re-run GREEN end-to-end (lint 0 / build 7-7 / typecheck 0 / test **645-94** / format `--check`; no `.only`/`.skip`/`.todo`). **Snippet accuracy independently audited:** every tool the inlined block tells an agent to call (`register`/`login`/`check`/`read_room`/`reply`/`react`/`unreact`/`read_contract`) is a REAL registered tool in `docs/mcp-tool-contract.md` §6 (the 17-tool canonical list) — **NO phantom tools**; the cadence is correctly framed PULL (the board never pushes); the protocol mapping matches `docs/negotiation-protocol.md` including the FR21 "Frozen = highest-`seq` message currently holding a live 👍" semantic (NOT "most 👍s") + revert-on-retraction; the DOCUMENTATION-ONLY / board-enforces-none disclaimer is present. **Content guard verified non-vacuous + correctly scoped by mutation:** (a) dropping the "Frozen" move → RED (case 3); (b) a phantom `subscribe` tool injected INSIDE the `AGENTBBS-PROMPT-SNIPPET:BEGIN/END` block → RED (the QA `no-phantom-tools` case, which parses §6's sentinel block — transitively pinned to the live `McpServer` via `tool-contract.drift.test.ts` — as the source of truth); (c) the same `subscribe` token added OUTSIDE the block (operator prose) → GREEN (the phantom scan is correctly scoped to the inlined block, no false-positive). The mutations were reverted; the working tree is the as-authored changeset. **EPIC 7 IS COMPLETE after this story** — the whole suite is green end-to-end (the convention `docs/negotiation-protocol.md` (7.1) + its discovery, the seeded announcement (7.2) + the recommended adoption text, this snippet (7.3); the tool contract (7.0) underpins all three). This entry is recorded for the audit trail (a clean review with nothing new carried forward).

- **Story 7.3 · LOW · Phantom-tool regex matches only backticked lowercase-snake tokens ≥3 chars (`/`([a-z][a-z_]{2,})`/g`), so a phantom tool named with a digit/hyphen or referenced WITHOUT backticks inside the inlined block would evade the scan. — DISMISSED (by-design; realistic phantom shape is covered).**
  - **Summary:** The `no-phantom-tools` case (`packages/mcp-server/src/agent-prompt-snippet-doc.test.ts`) extracts candidate tool tokens from the inlined block via a backticked-`snake_case` regex. A phantom token outside that shape — e.g. an un-backticked tool name, or a hypothetical `tool2`/`add-foo` form — is not in the candidate set, so it would not be flagged.
  - **Rationale:** Not a defect, not blocking. Every real registered tool in `docs/mcp-tool-contract.md` §6 is pure `[a-z_]` snake_case (no digits/hyphens), so the regex correctly captures the *realistic* phantom an author would actually write (`subscribe`/`watch`/`poll`) — mutation-proven RED for an in-block `subscribe`. The current snippet has zero phantom tools, the converse-sanity assertion already defeats the all-backticks-stripped vacuity case, and the guard's scope (the inlined block only) is itself mutation-proven (an out-of-block phantom stays GREEN, no false-positive). Tightening the regex to also catch non-snake/un-backticked shapes would gold-plate a guard against an unrealistic phantom shape, against a precision bar the sibling guards (`tool-contract.drift.test.ts`, `negotiation-protocol-doc.test.ts`) do not set. Dismissed, not deferred — it is a theoretical hardening nit on an already-strong, drift-bound guard, with no impact on the shipped artifact.
  - **Suggested resolution:** None required. If a future tool name ever adopts a digit/hyphen, or the snippet starts referencing tools un-backticked, broaden the candidate regex (and re-confirm the allowlist) at that point.

- **Carried-forward OPEN items for the Epic 7 retrospective triage (Story 7.3 introduced NO new deferred items — it is a docs + content-guard story with ZERO production code; the one LOW above was DISMISSED in review, not carried):**
  - **1.5 OPEN** — append-invariant lint guard disabled in `*.test.ts` (test-helper SQL relies on review). Unaffected by 7.3 (its one new test file appends nothing via raw SQL; it reads two repo `.md` files from disk and presence-checks tokens).
  - **1.6 OPEN** — `wireToPayload` does not validate a known-type-but-malformed payload row. Unaffected by 7.3.
  - **5.1-roomid-cap-edge OPEN** — no at-cap (200) / cap+1 (201) call test for `room_id` `ROOM_ID_MAX_LENGTH`. 7.3 adds NO MCP tool / validator. Fold one shared cap-edge case into the next room-tool touch (now Epic 8+, since the Epic 7 contract stories added no new room validator).
  - **(Resolved earlier in Epic 7 — no longer OPEN):** `E3-tool-names` and `4.5-tool-label` were both **RESOLVED by Story 7.0** (`docs/mcp-tool-contract.md` ratified the 17-tool surface + names + envelopes + closed codes, drift-guarded against the live `McpServer`). They are recorded here only to make explicit that the Epic 7 OPEN set is now exactly the three items above.

## Deferred from: code review of story 8.1 (2026-05-31)

Story 8.1 (the FR37–39 identity-bootstrap workflow — `integration/bmad/identity-bootstrap.md`: a self-contained, AGENT-EXECUTED Markdown workflow the Story 8.4 install kit inlines verbatim, resolving an agent's `persona@project` handle once per project and recording the PLAIN handle into an `AGENTBBS-IDENTITY` sentinel block in the project's `AGENTS.md`; three identity cases [no-handle→`register`, recorded→`login` with the `LOGIN_UNKNOWN` fall-through, `HANDLE_TAKEN`→bounded `-2…-9` disambiguation]; claim-based NFR7 / no secret / safe-to-commit; **ASSET ONLY — NO board code, NO new tool/event/error code**) was **APPROVED with 0 HIGH / 1 MED / 1 LOW — both auto-resolved inline, NOTHING carried forward.** All 4 ACs + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed; honest gate re-run GREEN end-to-end (lint 0 / build 7-7 / typecheck 0 / test **657-96** / format `--check`; no `.only`/`.skip`/`.todo`). The dev's content-guard (`identity-bootstrap-doc.test.ts`, 9 tests) + QA's real-runtime execution proof (`tools/identity-bootstrap-workflow.integration.test.ts`, 3 tests) were BOTH independently re-verified non-vacuous by the reviewer (mutation probes RED, every artifact restored byte-identical): a standalone phantom `` `subscribe` `` → RED; a Step-3 handle-assertion sentinel mutation → RED (the execution proof genuinely receives `'amelia-dev@taskflow'` from the live `register` tool over a real `Client`↔`createBoardServer`↔`createDataAccess` SQLite path). Rule 4 source-facts (register/login shapes + `HANDLE_TAKEN`/`LOGIN_UNKNOWN`) re-confirmed TRUE against the live tool sources + contract §6. Rule 1 N/A (no service introduced), Rule 3 satisfied (the execution proof IS the real-runtime evidence), Rule 5/6 N/A.

- **Story 8.1 · MED · Content-guard phantom-tool scan was blind to the `` `tool{ args }` `` CALL-FORM this asset uses prominently. — RESOLVED inline (Story 8.1 code review, 2026-05-31).**
  - **Summary:** The `no-phantom-tools` candidate regex was `` /`([a-z][a-z_]{2,})`/g `` (token wrapped by a *closing backtick* only). This asset presents its tool calls primarily as `` `register{ handle, current_focus }` `` / `` `login{ handle }` `` (a `{` follows the token), so those never became candidates — a phantom introduced ONLY in call-form would slip. Reviewer-proven: mutating `` `register{…}` `` → `` `signup{…}` `` left the guard GREEN 9/9 (whereas a standalone `` `subscribe` `` was caught). 
  - **Resolution:** Broadened the candidate regex to `` /`([a-z][a-z_]{2,})(?:`|\{)/g `` (capture the leading token before a closing backtick OR a `{`). Re-verified non-vacuous: clean asset 9/9 green (no false positives), call-form `` `provision{…}` `` → RED, standalone phantom still → RED. Asset SHA256 unchanged. **Reconciles with the Story 7.3 LOW dismissal (above)** of the SAME regex family without contradicting it (project-rules Rule 8): 7.3's "unrealistic phantom shape" rationale held because the SNIPPET block uses no call-form (its blind spot is latent); for 8.1 the call-form is the dominant invocation style so the blind spot is LIVE. The fix is additive (still catches every shape 7.3's regex caught) and scoped to this guard.
  - **Suggested resolution:** N/A — resolved.

- **Story 8.1 · LOW · Story Dev Agent Record recorded the DEV gate count (654/95) not the post-QA count (657/96), and the File List omitted the QA execution-proof test. — RESOLVED inline (Story 8.1 code review, 2026-05-31).**
  - **Summary:** The dev wrote "test 654 (95 files)" and a File List without `tools/identity-bootstrap-workflow.integration.test.ts`; QA added that 3-test file (654→657, 95→96) but did not reconcile the dev-stage figures.
  - **Resolution:** Reconciled in the story's Review Findings header + File List + Change Log (authoritative post-CR count **657 / 96 files**, QA test listed). Doc-only; the suite itself was already 657/96. Same class as the Story 4.4 / 5.3 auto-resolved test-count drifts.
  - **Suggested resolution:** N/A — resolved.

- **Carried-forward OPEN items for the Epic 8 triage (Story 8.1 introduced NO new OPEN items — it is an asset + two tests, ZERO production code; both review findings were auto-resolved inline):**
  - **1.5 OPEN** — append-invariant lint guard disabled in `*.test.ts`. Unaffected by 8.1 (its tests append nothing via raw SQL; the content guard reads `.md` files, the execution proof drives the real `register`/`login` tools + reads out-of-band via the `DataAccess` port).
  - **1.6 OPEN** — `wireToPayload` does not validate a known-type-but-malformed payload row. Unaffected by 8.1.
  - **5.1-roomid-cap-edge OPEN** — no at-cap (200) / cap+1 (201) call test for `room_id` `ROOM_ID_MAX_LENGTH`. 8.1 adds NO MCP tool / validator. Fold one shared cap-edge case into the next room-tool touch.
  - **7.3-snippet-callform-latent NOTE (not a defect today)** — the Story 7.3 snippet guard (`agent-prompt-snippet-doc.test.ts`) keeps the narrower `` `token` ``-only phantom regex. Its dismissal still holds because that snippet's inlined block uses NO `` `tool{ args }` `` call-form (verified empty). IF a future edit ever adds a call-form tool reference to the snippet block, apply the same `(?:`|\{)` broadening there (the 8.1 guard is the template). Recorded so the latent gap is not forgotten; no action required now.

## Deferred from: code review of story 8.4 (2026-05-31)

Story 8.4 (the **CAPSTONE** of Epic 8 + the AgentBBS MVP feature set — the single self-contained `integration/bmad/install-agentbbs.md` (AR27 / FR40): ONE agent-executed Markdown kit an operator copies into a target BMad project and runs once, which INLINES every prior integration asset (7.3 / 8.1 / 8.2 / 8.3, no sibling files) and carries an INLINE, self-contained Node helper (`applyBlock` + `mergeMcpServer`) for all file surgery; §0 prerequisite check FIRST [verify the `agentbbs` MCP server / STOP + report if absent; does NOT install the server, AC #3]; idempotent + timestamped-backup-before-overwrite + foreign-safe writes [never touches the `epic-cycle` kit or unrelated `.toml`/`.json` keys, AC #2]; runs the 8.1 identity bootstrap → records the plain handle into `AGENTS.md`; **ASSET + ONE inline helper script — NO board code, NO new tool/event/error code**) was **APPROVED with 0 HIGH / 0 MED / 1 LOW auto-resolved inline + 1 LOW deferred (below) + 2 dismissed; NOTHING blocking carried forward.** All 3 ACs + the 3 adversarial layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) re-confirmed; honest gate re-run GREEN end-to-end (lint 0 / build 7-7 / typecheck 0 / test **709** [103 files, 0 failed / 0 skipped, no `.only`/`.skip`/`.todo`] / format `--check`). The THREE tests were all independently re-verified **non-vacuous by the reviewer** (mutation probes RED, every artifact restored byte-identical; SHA256 verified): (1) defeating the helper's idempotency short-circuit → safety test (i) RED (`expected 'replaced' to be 'noop'`); (2) making `mergeMcpServer` drop foreign servers → safety test (iv) RED (`expected undefined to deeply equal { command: 'other-bin' … }`); (3) drifting the canonical `agent-prompt-snippet.md` source → content-guard snippet drift-pin RED (`expected false to be true`). The executable safety-property test EXTRACTS + dynamically imports + EXECUTES the kit's OWN inline helper against real temp files (Rule 3 / Epic 7 retro Action A); the QA connection test genuinely SPAWNS the real shipped `dist/main.js` over `StdioClientTransport` using the kit's connection record and asserts the live `listTools()` advertises all 8 kit-referenced tools — both real-runtime, not hollow. Reviewer also EMPIRICALLY exercised the helper across insert/replace/create/CRLF boundaries (idempotency holds in every case). Rule 4 source-facts re-confirmed TRUE: the four canonical assets exist; the bin is `agentbbs-mcp-server` → `./dist/main.js` (`packages/mcp-server/package.json`); env `AGENTBBS_DB`; the kit's named tools are all in the live contract §6 set (transitively pinned to the live `McpServer` by `tool-contract.drift.test.ts`); the kit's `epic-cycle` mentions are ALL negative (never-touch) — it issues ZERO write-instruction to any `epic-cycle` file (foreign boundary respected). Rule 1 N/A (no service — the safety + connection tests are the integration evidence), Rule 5 (NFR5 pull-only underlies the installed cadence — stated, not worked around), Rule 6 N/A (asset story, no `docs/adr`-constrained AC), Rule 8 satisfied (inlined copies pinned to source; `epic-cycle` untouched), Rule 10 satisfied (both guards present + reviewer-mutation-tested).

- **Story 8.4 · LOW · The kit helper's `applyBlock` JSDoc advertised an `'inserted'` return `action` the implementation never produces (an insert into an existing file reports `'replaced'`). — RESOLVED inline (Story 8.4 code review, 2026-05-31).**
  - **Summary:** The `@returns` union read `{action:'created'|'inserted'|'replaced'|'noop', …}` but the code returns only `'created'` (new file), `'replaced'` (existing file changed, whether appended or swapped), or `'noop'` (byte-identical). `'inserted'` is dead. The safety test even pre-anticipated it (`['inserted','replaced'].includes(r1.action) || r1.action === 'created'`), but the value is never returned. Cosmetic doc/code mismatch in a stranger-facing artifact.
  - **Resolution:** Took the **conservative** fix — aligned the JSDoc to what the code actually does (dropped `'inserted'`, added a one-line plain-prose gloss of the three action values) rather than changing the returned value (which a stranger's tooling could branch on). Notably, the first attempt backtick-quoted the action words (`` `created` `` etc.) and the kit's OWN content-guard `no-phantom-tools` case correctly went RED (`["created","replaced","noop"]` flagged as tool-looking tokens) — a live demonstration the guard works; the gloss was reworded to plain prose (no backticks) so the allowlist stays minimal. Kit SHA256 changed `7b6892…` → `1084da…` (the only reviewer change to the kit; the four other canonical sources are byte-identical to baseline). Gate re-run GREEN (709); both integration tests accept `'replaced'`/`'created'` so neither broke.
  - **Suggested resolution:** N/A — resolved.

- **Story 8.4 · LOW · `applyBlock` appends an LF-terminated owned block to a CRLF target, yielding mixed line endings in that file. — DEFERRED (cosmetic; no safety impact; → fold into any future helper hardening).**
  - **Summary:** The helper reads/writes `'utf8'` and constructs the owned block with `\n`. A consuming project whose `AGENTS.md` / `.toml` uses CRLF gets an LF-terminated AgentBBS block spliced in — cosmetically mixed endings within that one file. The reviewer empirically confirmed this does NOT break the load-bearing guarantees: re-detection via `String.indexOf(marker)` is ending-agnostic (the marker line carries no newline), so idempotency still holds (CRLF insert → identical re-apply = `noop`, proven), the backup-before-overwrite + foreign-safety properties are unaffected, and only the kit's own block region differs in EOL style.
  - **Rationale:** Not a safety defect and not blocking — the three AC #2 properties all hold under CRLF (empirically). Normalizing EOLs would require either detecting the target's dominant ending and matching it, or documenting an LF assumption; both are scope beyond a faithful capstone, and most BMad/Claude-Code consuming projects are LF. Deferred rather than dismissed because it IS a real (if minor) artifact-quality nit in a stranger-facing helper.
  - **Suggested resolution:** When the helper is next touched, optionally detect the target file's prevailing newline (CRLF vs LF) and build `ownedBlock` / the insert separator with that ending, OR add a one-line note in the kit prose that the helper writes LF blocks. Until then, leave as-is — idempotency and foreign-safety are unaffected.

- **Dismissed in review (Story 8.4 — recorded for the audit trail, NOT carried):**
  - **`mergeMcpServer` reformats the whole `.mcp.json` to 2-space indent + trailing `\n` on first touch (DISMISSED).** The kit/helper explicitly preserves foreign *values* exactly (proven byte-for-byte by safety test (iv): a pre-existing foreign server + an unrelated top-level key survive across an add AND an update), and the kit prose scopes the promise to "preserving … formatting **as much as practical**". `.mcp.json` is strict JSON (no comments to lose); only whitespace normalizes, and only once (a subsequent identical merge is a true `noop`). Not a foreign-safety violation.
  - **Sub-millisecond same-file double-overwrite could clobber a prior timestamped backup (DISMISSED).** `utcStamp()` is millisecond-precision (`…T23-17-08-123Z`); two overwrites of the SAME file within the same millisecond would collide on the backup name and `copyFileSync` would overwrite the first backup. Not realistic in the install flow — each `applyBlock`/`mergeMcpServer` call is a discrete agent step against a DIFFERENT owned file (~7 distinct targets), and the helper only backs up on an ACTUAL content change (a no-op makes none). A real install never overwrites one file twice within a millisecond. Theoretical only; not blocking.

- **Carried-forward OPEN items for the Epic 8 retrospective triage (Story 8.4 introduced NO new OPEN production items — it is an asset + one inline helper + three tests, ZERO board/production `.ts`; one LOW auto-resolved inline, one LOW deferred as a cosmetic helper nit, two dismissed):**
  - **1.5 OPEN** — append-invariant lint guard disabled in `*.test.ts`. Unaffected by 8.4 (its three tests append nothing via raw SQL; the content guard reads `.md`/`.toml` files, the safety test executes the kit's helper on temp files, the connection test drives the real server's `listTools()` + the kit's helpers on temp files).
  - **1.6 OPEN** — `wireToPayload` does not validate a known-type-but-malformed payload row. Unaffected by 8.4.
  - **5.1-roomid-cap-edge OPEN** — no at-cap (200) / cap+1 (201) call test for `room_id` `ROOM_ID_MAX_LENGTH`. 8.4 adds NO MCP tool / validator. Fold one shared cap-edge case into the next room-tool touch.
  - **8.4-helper-crlf LOW (new, deferred above)** — the LF-block-into-CRLF-file cosmetic nit; fold into any future helper hardening.
  - **7.3-snippet-callform-latent NOTE (unchanged)** — still latent (the 7.3 snippet block uses no call-form); 8.4's own content-guard already uses the broadened `(?:`|\{)` call-form-aware regex, so the kit itself is covered.

## Deferred from: code review of story 9.1 (2026-06-01)

Story 9.1 (the FIRST UI story — `@agentbbs/ui-shared` React/TSX scaffold + `tokens.css` semantic token core [dark + light, web-canonical DESIGN.md hex] + two-family typography encoding + WCAG `contrastRatio` utility & AA-floor test [parses the SHIPPED tokens.css, both modes, per-combo floor] + first in-package token consumer `TokenProbe` rendered in a happy-dom DOM test proving the dark→light custom-property flip + the UI test tier wired into the default `pnpm test` as a SECOND vitest project) was **APPROVED with 0 HIGH / 0 MED / 2 LOW deferred (below); nothing blocking.** All 3 ACs met. Reviewer re-ran the honest gate GREEN (build 7/7 · typecheck 0 · default `pnpm test` = **728 passed**, 106 files, no `.only`/`.skip`/`.todo`). The three marquee semantics were independently mutation-verified non-vacuous BY THE REVIEWER and tokens.css restored byte-identical (cmp) each time: (1) weakening `--text-muted`→`#6a6a6a` → `contrast.test.ts` RED on the edge floor; (2) deleting `--accent-hover` from the LIGHT scope → QA's `tokens.test.ts` completeness + symmetry tests RED; (3) breaking light `--text-body`→`#abcdef` → `TokenProbe.test.tsx` RED on the dark→light flip. The reviewer also recomputed all 10 contrast combos out-of-band: every one clears its floor (lowest small-text = `text-muted` dark 4.518 ≥ 4.5 strict; agreed-green-light 3.392 ≥ 3 LARGE/UI-only). Rule 1 (AC3 is a real DOM-render Integration AC), Rule 3 (TokenProbe genuinely renders into a live happy-dom — `textContent`/`style.color` asserted, not a stub), Rule 5 (AA contrast is genuinely MEASURED over the shipped tokens.css, not asserted — no tripwire), Rule 7 (all three semantics mutation-tested), Rule 10 (contrast + completeness guards PARSE the shipped tokens.css, no hand-copy) all satisfied. Rule 6 N/A (no `docs/adr`). Single-root-config invariant preserved (DOM env is a second `test.projects[]` entry in the ONE root vitest.config.ts); module boundary clean (no `@agentbbs/core`/`data-access` import); build-honesty preserved (cross-package `paths` + `jsx`/DOM-lib widening only in `tsconfig.typecheck.json`, never in `tsconfig.base.json`). The two dev/QA-surfaced adjudication items were re-confirmed correctly disposed: (a) the DESIGN.md text-body-light 17.4-vs-16.671 reference-value delta is a non-load-bearing doc nit, NOT a Rule-5 NFR tripwire (the NFR is measurable, measured, and passes with ~3.7× margin); (b) the in-story Rule-8 `*-light`-name contradiction was reconciled to the ratified Dev Notes design (same names redefined under the light selector; no `*-light` consumer tokens).

- **Story 9.1 · LOW · `TokenProbe.test.tsx` emits a React `act(...)` warning to stderr because `IS_REACT_ACT_ENVIRONMENT` is unset. — RESOLVED (Story 9.2, 2026-06-01).**
  - **Resolution:** Story 9.2 (Task 5) added `packages/ui-shared/src/test-setup-dom.ts` (sets `globalThis.IS_REACT_ACT_ENVIRONMENT = true`) and wired it into the `ui-shared-dom` Vitest project via `setupFiles` in the root `vitest.config.ts`. The full `pnpm test` (780 passed, 111 files) now runs with NO `act(...)` warning on stderr — confirmed across `TokenProbe.test.tsx` plus the new `MarkdownView.test.tsx` / `CodeBlock.test.tsx` component-render tests this story added. `act()` semantics are now correct (effects flush synchronously inside `act`) for every DOM-project test. Landed with its first real consumer (the 9.2 component tests), exactly as the suggested resolution intended.
  - **Summary:** The AC3 render test wraps `root.render()` / `root.unmount()` in `act()`, but `globalThis.IS_REACT_ACT_ENVIRONMENT` is not set in the `ui-shared-dom` project, so React logs `Warning: The current testing environment is not configured to support act(...)` twice on stderr. The test still PASSES and genuinely renders (asserts `probe.textContent === 'hello'` and `probe.style.color === 'var(--text-body)'`, which only hold if React actually mounted the component — proven non-vacuous by the reviewer's mutation flip). So this is stderr noise, not a correctness or vacuity defect.
  - **Rationale:** Non-blocking — the render is real and the assertions are load-bearing. Setting the flag is trivial but is better done once, alongside a shared DOM-project setup file, when Story 9.2 adds more `.test.tsx` component tests (so the setup lands with its first real consumer rather than as a one-off here).
  - **Suggested resolution:** In Story 9.2, add a tiny setup file for the `ui-shared-dom` vitest project (`setupFiles`) that sets `globalThis.IS_REACT_ACT_ENVIRONMENT = true` (and optionally a DOM cleanup), silencing the warning and letting `act()` flush effects properly. Until then, leave as-is.

- **Story 9.1 · LOW · DESIGN.md documents the text-body-light contrast as 17.4:1; true WCAG(#1e1e1e on #ffffff) = 16.671:1 (Δ≈0.729). — DEFERRED (non-load-bearing doc reference number; AA met with vast margin; → optional one-char fix on a future DESIGN.md accuracy pass).**
  - **Summary:** DESIGN.md's measured-contrast table (row "body text on surface-base", light column) reports 17.4:1; the true WCAG ratio for `#1e1e1e` on `#ffffff` is 16.671:1 (reviewer recomputed; matches the dev's 16.67). This is a minor inaccuracy in the planning doc's reference column only — the token VALUES are correct and the AA floor (≥4.5) is met with ~3.7× margin. `contrast.test.ts` documents the discrepancy inline and uses a 1.0 cross-check tolerance that absorbs it while still catching a wrong-pair resolution (off by whole integers).
  - **Rationale:** NOT a Rule-5 NFR tripwire — Rule 5 fires on an unmeasurable/impossible/contradictory NFR; this NFR is fully measurable, measured, and passing. A slightly-off documentation reference number is a doc nit. DESIGN.md is `status: final` and the value is non-load-bearing, so amending it now is not required.
  - **Suggested resolution:** If a future doc-accuracy pass touches the DESIGN.md contrast table, correct 17.4 → ~16.7 for that one row. Otherwise leave as-is; the code-side pin is correct and self-documenting.

## Deferred from: story 9.4 (2026-06-01)

- **Story 9.4 · @mention-based NEEDS YOU escalation (the AC3 "/ @mention" SECONDARY signal) — DEFERRED with rationale (do NOT fake it).**
  - **Summary:** Story 9.4's AC3 + Task 1 name `add_participant(@operator)` as the **load-bearing, testable V1** NEEDS YOU escalation (a `room.participant_added` event naming the operator handle) and `@mention` as a **documented SECONDARY** signal. The load-bearing path is fully implemented + mutation-tested non-vacuous (`needsYouRooms` in `packages/core/src/rooms/needs-you.ts`; the host `/api/needs-you`; the live `foldTreeDelta` escalation). The `@mention` secondary is **deferred, NOT faked** (per the Task 1 instruction "implement on a real marker or defer with rationale — do not fake it").
  - **Rationale:** There is **no real `@mention` marker in the data model today.** Message bodies are untrusted free-text markdown (`announcement.posted.body` / `room.replied.body`); the closed event vocabulary (`project-context.md#Event vocabulary`) has no `message.mentioned` event and no structured mention field, and the body is rendered INERT (NFR12) — there is no parsed mention token to key off. Deriving NEEDS YOU from a regex scan of body text would be (a) a heuristic, not deterministic board state, and (b) easily spoofed/false-positived (a literal `@ops` inside a code block or a quote), directly undermining AC3's "deterministic, explicit-escalation-only" contract. Faking it on a body-text scan is exactly the anti-pattern the task forbids. The deterministic `add_participant` path already covers "an agent explicitly pulls the operator in," which is the load-bearing case.
  - **Suggested resolution:** When/if `@mention` becomes a first-class escalation, introduce it as a **real structured marker** — either a new additive event type (e.g. `message.mentioned { roomId, handle }`, appended by a future `mention`/`reply`-with-mentions op) or a structured `mentions: string[]` field on the reply payload — then extend `needsYouRooms` to union the explicitly-mentioned-operator rooms with the add_participant set (still deterministic, still append-derived, never a body-text heuristic). Until that marker exists, NEEDS YOU is `add_participant(@operator)`-only by design.


## Deferred from: code review of story 9.5 (2026-06-01)

Story 9.5 (the room main-column thread — breadcrumb `sub-board › #room` → joined-participants row + operator posture [`you: watching` → `you: @operator (peer)`, AC2 Mode A→B signal] → seq-ordered `MessageThread` of `MessagePost`s [each `@handle` mono-accent + right-aligned timestamp + inert Story-9.2 `MarkdownView` body within `--measure` + hairline divider; >30-line whole-post collapse → "show more"] → composer placeholder seam [Story 9.7 fills], all in `@agentbbs/ui-shared` prop-driven/presentation-only; per-post display `created_at` + `participants` attached HOST-LAYER on `/api/rooms/:id` leaving core `RoomMessage` + the ratified MCP message wire byte-untouched; apps/web `fetchRoom`/`buildRoomViewModel`/`loadRoomViewModel` + the tree-row-click room open) was **APPROVED with 0 HIGH / 0 MED / 0 LOW-blocking / 1 LOW deferred (below) + 1 timing-race theory DISMISSED-with-analysis; nothing blocking carried forward.** Both ACs met. Honest gate re-run GREEN (lint 0 · typecheck 0 · full `pnpm test` **927 passed**, 128 files, 0 failed/0 skipped, no `.only`/`.skip`/`.todo`); full suite run 3× + markdown/room DOM suites 5× = 8 clean runs. The seq-order semantic was reviewer-mutation-verified non-vacuous (reversing the host's `messages.map` → the json-api seq-order/shape tests RED; reverted byte-identical, `.reverse()` grep = 0; re-GREEN). `messageToWire` + core `RoomMessage` `git diff HEAD` EMPTY; `wire.contract-drift` + `tool-contract.drift` GREEN (Rule 10). NFR2 module boundary clean (no core/data-access import in `src/room/**`). NFR12 inert-in-thread proven on BOTH the direct-MessageThread and the full-RoomView paths. Collapse off-by-one bracketed (30 = no collapse, 31 = collapse). Posture authorship-independent (pulled-in non-replying peer = peer). Rule 1 (RoomView really consumed by apps/web via a tree-row click in `App.test.tsx`), Rule 3 (host over real in-memory ledger + real happy-dom render), Rule 7 (seq-order + collapse non-vacuous), Rule 8 (no flaky test normalized — Shiki disposed in writing below) all satisfied; Rules 5, 6 N/A.

- **Story 9.5 · LOW · The Shiki highlighter warmup the dev/QA flagged as a transient full-suite failure is NOT a genuine async race — DISMISSED as a race; a cheap DOM-`beforeAll` warmth assertion is the optional hardening. (recorded per Rule 8 — NOT silently normalized.)**
  - **Summary:** The dev's Debug-Log "transient failure in render-markdown" was a deterministic lang-alias miss: a `` ```ts `` fence (`ts` ∉ `HIGHLIGHT_LANGS`, which lists `typescript` not the `ts` alias) fell to `plainInertHtml` with no token class-spans, failing the "emits `.code-keyword/.code-type/.code-fn`" assertion. The dev fixed it in-changeset by using `` ```typescript `` (the proven-tinting case). That is a fixed, deterministic alias gap — not an async warmup race.
  - **Why no genuine race exists:** the highlighter singleton (`highlighterPromise`/`highlighterReady` in `highlight.ts`) is **module-scoped**, and Vitest's **default file isolation** (no `isolate:false` in `vitest.config.ts`) gives each test file a fresh module registry — so no cross-file singleton bleed. Every markdown-rendering DOM test (`MarkdownView`, `CodeBlock`, `RoomView`, `App`) does `beforeAll(prewarmHighlighter)`; when warm, `MarkdownView` seeds **synchronously** via `renderMarkdownSync` in its `useState` initializer (the async `useEffect` path is redundant insurance), so the first paint is deterministic. `renderMarkdown` itself `await`s `prewarmHighlighter()` before `renderer.render`, and the `.then` that sets `highlighterReady` runs before that await resolves — so the sync `highlight` callback always sees a warm highlighter within a single call. The reviewer could NOT reproduce a failure across 8 clean runs (3× full suite + 5× the markdown/room DOM suites).
  - **Rationale:** Not a defect to fix (no race; the observed failure is the now-corrected alias) and not blocking. Recorded rather than dismissed-in-prose-only so the perceived intermittent risk has a written disposition (Rule 8: a known intermittent must not be silently normalized).
  - **Suggested resolution:** If a future markdown-DOM test is ever added WITHOUT a `beforeAll(prewarmHighlighter)`, it would hit the async-effect path and need the microtask-flush discipline the existing tests use. Cheap optional hardening when these tests are next touched: add `expect(isHighlighterWarm()).toBe(true)` immediately after `await prewarmHighlighter()` in the DOM `beforeAll`, turning a silent regression-to-the-async-path into an explicit RED. Until then, leave as-is — the architecture is deterministic and 8/8 runs were clean.


## Deferred from: code review of story 9.10 (2026-06-01) — EPIC 9 CAPSTONE

Story 9.10 (the calm-posture + a11y capstone for the web Operator UI: calm empty/cold/disconnected states + quiet=healthy; the prop-driven `ConnectionFooter` LED [`● connected` / `○ reconnecting…`, never a modal] wired from the SSE `EventSource` open/error; the terse lowercase voice sweep + a content-guard; and the keyboard/screen-reader a11y floor — APG tree roles + roving tabindex + arrow keys + `aria-expanded`; thread `role="list"` of announced `listitem`s + a coalescing polite `aria-live`; keyboard composer with Esc; AA `:focus-visible` ring ≥3:1; `prefers-reduced-motion`; landmarks) was **APPROVED with 0 HIGH / 0 MED / 2 LOW deferred (below); nothing blocking.** All three ACs met with real-runtime evidence (Rule 3). Reviewer re-ran the honest gate GREEN end-to-end: **lint 0 · typecheck 0 · build (all 7 + apps/web) green · full `pnpm test` 1094 passed (135 files, 0 failed/0 skipped, no `.only`/`.skip`/`.todo`) · format --check clean** — the Epic 9 suite is green 1.1→1.10 end-to-end. The marquee NO-MODAL-ANYWHERE calm-posture invariant was **reviewer-mutation-verified non-vacuous** (planted `<div role="dialog">` in `App.tsx` → all 4 app-level no-modal-sweep tests RED across the empty/cold/disconnected/post-failure matrix; reverted byte-identical — `git diff --stat App.tsx` unchanged at 79+/19-, grep MUTATION=0; re-GREEN). The dev/QA-proven mutations (voice guard `connected`→`✓ You are securely connected…` → MicrocopyGuard RED; coalesce per-delta-no-debounce → A11yFloor RED) were re-confirmed, and the QA accumulation test (a 3-re-render burst within the rolling debounce window → ONE "3 new posts", not last-delta) is genuinely non-vacuous (it sums `pendingDeltaRef`, not the latest delta). The a11y floor is a MEASURED NFR (Rule 5): `focus-and-motion.test.ts` runs the SHIPPED `contrastRatio` WCAG math over the SHIPPED tokens.css hexes — the `--accent` focus ring clears ≥3:1 over both surfaces in both themes (7/7 green), not asserted-around. EventSource `open`→connected / `error`→reconnecting mapping VERIFIED against the real api-client `EventSource` wrapper (Rule 4); the 9.9 live fold resumes idempotently-by-seq on reopen. REGRESSION: the project-`<li>` tree-collapse bubbling guard (act only when `event.target.closest('[data-tree-id]') === currentTarget`) holds — the full 9.8 tab suite + room-select are green in the 1094 run. MODULE BOUNDARY (NFR2) clean: every new/changed `ui-shared` chrome/a11y component is presentation-only + prop-driven (no `@agentbbs/core`/`data-access` import); the new `chrome.css` is a published asset subpath (exports + files + eslint allowlist); no new board behavior, no core/host contract change. VOICE content-guard (Rule 10) pins the terse strings to the rendered components with the aria-hidden decorative `!` flag glyph correctly scoped OUT of the needs-you scan (no scan-weakening — Rule 10 corollary). Rules 1 (chrome/a11y consumed by apps/web in-story via real App-shell DOM tests), 3, 5, 7, 8, 10 satisfied; Rule 6 N/A (no `docs/adr` — ADR none-required confirmed). Left UNCOMMITTED for the lead's post-CR smoke gate.

- **Story 9.10 · LOW · The app-level no-modal structural sweep (`App.test.tsx` — `assertNoModalAnywhere`) asserts `innerHTML.toLowerCase()` does NOT contain the substrings `modal` / `backdrop` / `scrim` / `overlay`. — DEFERRED (currently correct + non-vacuous; brittle to a future legitimate token).**
  - **Summary:** Alongside the robust role-based checks (`[role="dialog"]`/`[role="alertdialog"]`/`[aria-modal="true"]`/`[role="alert"]`/`<dialog>` — these are the load-bearing, non-vacuity-proven assertions), the sweep also does four raw-substring `not.toContain` checks over the lowercased rendered HTML. They pass today (the app emits none of these tokens) and add defense-in-depth against a class/data-attr-named scrim. But a future LEGITIMATE token containing one of these substrings (e.g. a CSS class `overlay-none`, a `data-testid="modal-free"`, a tooltip with the word "overlay") would false-RED the sweep even with no actual modal present.
  - **Rationale:** Not a defect — the substring checks currently catch real modals (proven: the planted `role="dialog"` reverse-case turns the sweep RED, and the role-based assertions independently guard the invariant). It is a maintainability nit, not a correctness or vacuity gap; the role-based checks remain authoritative.
  - **Suggested resolution:** When this sweep is next touched (or when a false-RED surfaces), narrow the substring checks to attribute-scoped queries (e.g. `querySelectorAll('[class*="scrim"],[class*="backdrop"],[class*="overlay"]')` filtered to actually-elevated/positioned nodes) or drop them in favor of the role/`<dialog>`/`aria-modal` checks that already pin the invariant. Until then, leave as-is.

- **Story 9.10 · LOW · `SidebarTreeItem.tsx` header comment says the focus ring `.nav-row:focus-visible` rule is "in tree.css"; it actually ships in `chrome.css`. — DEFERRED (cosmetic doc-word; styling is correct + asserted).**
  - **Summary:** The Story-9.10 rewrite of the `SidebarTreeItem` file comment states "The visible focus ring (AA, ≥3:1) is the `.nav-row:focus-visible` rule in tree.css." The rule actually lives in the new `chrome.css` (`.nav-row:focus-visible` / `.nav-project:focus-visible`), which is the side-effecting stylesheet `apps/web/src/main.tsx` imports. The `nav-row` class is genuinely applied (`SidebarTreeItem` line 99) so the ring works; `focus-and-motion.test.ts` pins the rule's presence in `chrome.css`.
  - **Rationale:** Pure comment inaccuracy — no behavioral impact (the rule exists, is imported, is applied, and is contrast-tested). Not worth a churn commit on its own.
  - **Suggested resolution:** On the next touch of `SidebarTreeItem.tsx`, correct "tree.css" → "chrome.css" in the header comment. Leave as-is otherwise.

---

## Epic 9 retrospective (2026-06-01) — Operator UI complete; one named scope gap

**Story 9.1–9.10 all APPROVED, 0 HIGH / 0 MED across the epic, full suite 1094 green, every story real-Chrome smoke-verified.** The Epic 9 OPEN set carries the same three perennial board-code LOWs (1.5, 1.6, 5.1-roomid-cap-edge — no Epic-9 trigger; Epic 9 is UI, not board code) plus the per-story LOWs already recorded inline (9.1-L2 DESIGN doc ratio nit, 9.5 Shiki-warmup analysis, 9.10 no-modal-substring + tree.css comment nit). No NEW board-code deferrals.

- **9-OPERATOR-INITIATE-PARITY (NEW, MED, scope gap found at the Epic 9 retro — owner: a follow-up epic via `/bmad-correct-course` BEFORE Epic 10 VS Code).** Epic 9 fully delivered the operator's **respond/participate** surface (Mode A global-read browse + Mode B join-to-post-as-peer via `reply` + 👍/agree + `add_participant` + tabs + live + calm/a11y). It deliberately did NOT scope the operator's **initiate** surface — there is no UI/host path to **create a project** (`announce_project`), **post a new announcement / open a new room** (`post_announcement`), or **proactively join a sub-board** from the tree. None of these were Epic 9 ACs (the epic scoped browse + participate-as-peer, not initiation, which is agent-initiated in the board model). **Decision (Josh, retro):** capture as a gap; do NOT wire now; a follow-up epic created with `/bmad-correct-course` after the Epic 9 merge will establish human↔agent feature parity, sequenced BEFORE the VS Code extension (Epic 10). Note the board WRITE ops already exist in core (`announceProject`/`postAnnouncement`/`joinBoard`) and `joinBoard` already has a host POST endpoint (9.7) — the gap is the host write endpoints for announce/post_announcement + the compose UI + wiring the tree's join-project picker.
- **9.4-join-project-inert (LOW, related). — ✅ RESOLVED by Story 9.12 (2026-06-01).** The `＋ join a project…` tree row is now WIRED to a calm inline `JoinProjectPicker` (ui-shared) → the EXISTING `POST /api/projects/:id/join` (`join_board`) + the EXISTING `fetchDirectory` read. Clicking the row fetches the global-read directory, filters out the projects the operator already belongs to (canonical-handle compare), and opens the picker; choosing one runs the SAME `join_board` an agent uses (no operator backdoor, no fabricated/leave op — Rule 13) and refreshes the tree so the new membership shows live. NO new host endpoint, NO core/MCP change (drift-guarded contract byte-identical). The original stub decision below is retained for history. **Decision (Josh, retro): LEAVE IT VISIBLE** (per DESIGN/AC1 — the affordance is specified) until the initiate-parity follow-up wires it — *that follow-up is Story 9.12, now landed.*


---

## Deferred from: code review of story 9.13 (2026-06-01)

Story 9.13 (operator "set my focus" INITIATE-surface parity: a new host `POST /api/me/focus` over the EXISTING core `updateFocus` + host-layer `/api/me` `focus`/`registered` reflection + a prop-driven `FocusAffordance` wired into apps/web with the watching-only/unregistered disabled gate) was **APPROVED with 0 HIGH / 1 MED (auto-resolved inline) / 1 LOW deferred (below); nothing blocking.** Both ACs met with real-runtime evidence (Rule 3 — `host.integration.test.ts` over real `createDataAccess` SQLite + real HTTP: `identity.focus_updated` lands, `/api/me` reflects). **Rule 13 LOAD-BEARING held:** `git diff HEAD -- packages/core packages/mcp-server` EMPTY; `OPERATOR_NOT_REGISTERED` is a HOST-surface code (NOT in core's closed ten — drift-guard pins the ratified set byte-identical); the `/api/me` additions are host-layer display fields (9.5 `created_at` precedent), agent MCP wire untouched; the write maps to the EXISTING core `updateFocus` (no backdoor, no fabricated op). The unregistered backstop (AC1 "never a crash") was reviewer-mutation-RE-confirmed (`if (false && findIdentity...)` → unregistered case 500 → BOTH the dev guard test AND the QA distinct-states test RED `expected 500 to be 403`; reverted byte-identical → green). The three operator states map to three DISTINCT host responses (200 / NO_OPERATOR 403 / OPERATOR_NOT_REGISTERED 403, ≠ NO_IDENTITY/401/500). Append-only/latest-wins asserts the DERIVED `/api/me` fold (`second focus`) + event COUNT==2, not a vacuous count==1. **MED auto-resolved during review:** the live SET-failure inline error was invisible (the `FocusAffordance` error slot rendered only inside the open editor, but `handleSubmit` closes the editor synchronously on submit — so a failed async POST showed no feedback); fixed by also rendering the calm `focus-error` line in the resting view (guarded `!disabled`), + 2 new ui-shared DOM tests. Honest gate GREEN end-to-end after the fix: **lint 0 · typecheck 0 · build (all packages + apps/web) green · full `pnpm test` 1185 passed (139 files, 0 failed/0 skipped, no `.only`/`.skip`/`.todo`) · prettier --check clean.** Left UNCOMMITTED for the lead's post-CR smoke gate.

- **Story 9.13 · LOW · `requireBodyString` does not TRIM, so a whitespace-only focus persists via a DIRECT (non-UI) `POST /api/me/focus`. — DEFERRED (benign; the UI gate trims and read-consumers treat whitespace as the placeholder).**
  - **Summary:** `requireBodyString(body, 'focus')` accepts any non-empty string but does not trim, so a direct API call with `{ focus: "   " }` would append a whitespace `identity.focus_updated`. The `FocusAffordance` client trims before POST (`trimmedDraft`), and on read both `restingFocus` (component) and the resting row treat a whitespace focus as the calm placeholder — so a whitespace focus is benignly invisible, never a crash.
  - **Rationale:** Not a defect for this story (the affordance is the gate; no non-UI caller exists today). A host-side trim would be cheap hardening but is out of scope and changes no observable UI behavior.
  - **Suggested resolution:** When `json-api.ts` `requireBodyString` (or the focus route) is next touched, either trim the focus host-side before `updateFocus` or add a host-surface 400 for a whitespace-only focus, matching the client's trim discipline. Until then, leave as-is.

---

## Epic 10 retro-review gate (2026-06-02 — Story 10.0 triage; before VS Code extension feature work)

**Story 10.0 update (2026-06-02 — Epic 10 retro-review gate): triage-only, 0 included, all OPEN items DEFER (no Epic-10 trigger), 1 NEW item recorded.** Epic 10 ships the **VS Code extension surface** — a NEW `apps/vscode-extension` thin client over the EXISTING `ui-shared`/`data-access`/core (Rule 13: no core/MCP/install-kit/web-host change; a SEPARATE host from the Epic-9 web `json-api.ts`). None of the carried OPEN items lives in Epic 10's path. The OPEN set entering Epic 10: **1.5, 1.6, 5.1-roomid-cap-edge, 8.4-helper-crlf, 9.1-L2 (DESIGN doc ratio), 9.4-mention, 9.5-shiki-warmup, 9.10-no-modal-substring, 9.10-tree.css-comment, 9.13-trim**, plus the NEW **E10-baseline-seedrace-eperm**. Full disposition: `10-0-epic-9-deferred-cleanup.md`. **Resolved-confirmations:** `9-OPERATOR-INITIATE-PARITY` (MED) RESOLVED by Stories 9.11–9.14 (the parity addendum); `9.4-join-project-inert` RESOLVED by Story 9.12. **Awareness carries (NOT deferred-work items — feature guidance to named Epic 10 stories):** Shiki `beforeAll(prewarmHighlighter)` discipline → Stories 10.4/10.5 (webview mounts the same `ui-shared` Markdown renderer); host-side trim discipline → Stories 10.2/10.6 (the postMessage bridge mirroring `update_focus`/`reply`); proto-room navigability (Rule 15) → 10.3/10.4; panel-exclusivity → 10.4/10.5; Rule-14 integrated webview smoke → lead/end-of-epic.

- **Story 10.0 (baseline) · LOW · `seed-protocol-race.test.ts` teardown `EPERM` on Windows under full-suite parallel load. — OPEN (test-infra, Windows-only; recorded at the Epic-10 baseline `pnpm test`, NOT normalized).**
  - **Summary:** The Epic-10 baseline `pnpm test` reported `1 failed | 1201 passed` — the single failure is an `EPERM, Permission denied` thrown by `removeTempTree` (`rmSync(dir, { recursive, force, maxRetries: 20, retryDelay })`) at `packages/data-access/src/seed-protocol-race.test.ts:144`, in the test's **post-assertion teardown** (cleaning the temp DB tree), NOT in the assertions. The actual AC #2 assertion (N concurrent processes → EXACTLY ONE protocol announcement / main project / system identity, no error escapes) PASSES. Re-running the file in isolation is GREEN (1/1). The failure only manifests under full-suite parallel load, when a forked worker's SQLite file handle on `\\?\…\agentbbs-seed-protocol-race-*` has not yet been released by the OS at the moment `rmSync` runs, so Windows refuses the directory removal. This is the same Windows handle-release-race class the test already tries to absorb with `maxRetries: 20`.
  - **Rationale:** Not a correctness defect (the race-semantics assertion is sound and green) and not an Epic-10 trigger (Epic 10 is a VS Code thin client; it touches no `data-access` test). Pre-existing test-infra flakiness specific to Windows + full-suite concurrency. Recorded here rather than silently normalized (anti-pattern: normalizing known failures erodes baseline reliability).
  - **Suggested resolution:** When `data-access` test infra is next touched, harden `removeTempTree` against the Windows handle-release race — e.g. raise `maxRetries`/`retryDelay`, add a short pre-`rmSync` delay or an explicit DB-handle-close await before cleanup on Windows, or wrap teardown removal in a swallow-on-EPERM best-effort (temp dirs are OS-reclaimed). Until then, treat a SOLE `seed-protocol-race` teardown-EPERM under full-suite load as this known flake (re-run the file in isolation to confirm green) — do NOT read it as an Epic-10 regression (Rule 6: verify against ground truth).

## Deferred from: code review of story 10.1 (2026-06-02)

**Story 10.1 (extension scaffold + SQLite-driver↔extension-host ABI proof) reviewed: 1 HIGH found + auto-resolved inline, 1 NEW forward-risk item recorded (below), 0 carried-to-backlog from the existing OPEN set (no Epic-10.1 trigger).** AC1/AC2/AC3 satisfied with honest real-runtime evidence (Rule 3 / Rule 12): the `node:sqlite` fallback genuinely loads + `SELECT sqlite_version()` returns `3.53.0` in a real process; better-sqlite3 loads at its built ABI 137 via the data-access transitive resolution path; the empirical ABI (node 24.16.0 / modules 137 / electron=none) is read LIVE, never web-claimed; `activate()` registers its command without throwing. The better-sqlite3↔Electron-host ABI MATCH is honestly recorded as UNVERIFIABLE on this machine (no Electron runtime / launchable Extension Dev Host) — an honest AC2 landing, NOT a faked pass. Rule 13 (thin client) held LOAD-BEARING: `git diff HEAD -- packages/core packages/mcp-server` EMPTY (agent contract byte-identical); the extension imports only `vscode` (no better-sqlite3 in its `package.json`; the addon is reached transitively through data-access). esbuild externalization verified in the EMITTED bundle (`require("vscode")` present, no `better_sqlite3.node`/`*.node` inlined, `module.exports` + entry symbols present). All four marquee assertions reviewer-mutation-RE-confirmed non-vacuous (Rule 7): node:sqlite version-match → RED on `/^ZZZ_MUTANT/`; activate() command id (BOTH dev `toContain` and QA `toEqual`) → RED on `agentbbs.MUTANT`; the synthetic-import externalization guard → RED when `better-sqlite3` removed from `external` (esbuild "Could not resolve"); all reverted byte-identical. `@types/vscode@1.120.0` satisfies `engines.vscode ^1.105.0` (floor = engine commitment, latest-within-caret is the VS Code convention — sane + verifiable); the `type:module` source + `.cjs` host output split is correct for the VS Code host's CommonJS `require()` loader. Rule 6 N/A (no `docs/adr`). The canonical ROOT gate (`pnpm test` / `pnpm run typecheck`, NOT per-package) is the gate that surfaced the HIGH below.

- **Story 10.1 · HIGH · The QA negative-path test broke the canonical root `pnpm run typecheck` gate (`TS2307` on a static `import('node:this-module-does-not-exist')`). — RESOLVED inline (code review, 2026-06-02).**
  - **Summary:** `bundle-and-activation.test.ts:165` used a STRING-LITERAL dynamic import of a genuinely-absent built-in (`import('node:this-module-does-not-exist')`) to prove the load probe rejects loudly. `tsc --noEmit -p tsconfig.typecheck.json` (the canonical root typecheck gate) STATICALLY resolves a literal module specifier and errored `TS2307: Cannot find module …`, turning `pnpm run typecheck` RED (exit 2). The dev's reported "typecheck 0" predated the QA stage adding this test; the COMBINED changeset broke the gate, and the QA gate evidently did not re-run the root typecheck after the addition. (Vitest does not typecheck, so the runtime test passed — masking the gate break.) This is the class Rule 12's "canonical ROOT gate is the gate" + Rule 8 guard against.
  - **Resolution:** Computed the specifier so `tsc` cannot statically resolve it while the RUNTIME behavior is byte-identical (`const absentBuiltin = ['node:this-module','does-not-exist'].join('-'); await expect(import(absentBuiltin)).rejects.toThrow();`) — a dynamic `import(variable)` is not statically module-checked, mirroring the sibling `require(string)` negative test in the same `describe`. Verified: `pnpm run typecheck` → 0 errors; the negative-path test still passes (genuinely imports a computed-absent specifier at runtime and asserts rejection — non-vacuity preserved); FULL gate re-run GREEN end-to-end (lint 0 · typecheck 0 · build clean · format clean · `pnpm test` **1212 passed / 141 files / 0 failed**; the known `seed-protocol-race` EPERM flake did not manifest). Comment added explaining why the specifier is computed.
  - **Suggested resolution:** N/A — resolved inline.

- **Story 10.1 · MED · DOWNSTREAM RISK for Story 10.2 — `data-access` is better-sqlite3-ONLY; if the Electron extension host cannot load better-sqlite3, 10.2 needs a `node:sqlite`-backed `data-access` path behind the NFR2 seam. — OPEN (forward-risk; owner: Story 10.2, the first DB-opening consumer).**
  - **Summary:** Story 10.1 proved (honestly) that the better-sqlite3↔Electron-host ABI MATCH is UNVERIFIABLE on this machine and resolved AC2 via the `node:sqlite` fallback at the LOAD level only (it does not wire `data-access`). But `@agentbbs/data-access` imports better-sqlite3 as its sole driver (the Story-1.4 single-driver decision). Story 10.2 ("extension host opens the DB and bridges to the webview") is the FIRST consumer that opens a real ledger through the host runtime. If, when 10.2 reaches a real Electron extension host, the better-sqlite3 prebuilt addon (ABI 137, standalone Node) does NOT load in the host's Electron ABI AND an `@electron/rebuild` against the host ABI proves brittle/unavailable, then 10.2 cannot open the ledger via the current data-access driver and would need EITHER (a) an ABI-matched better-sqlite3 prebuild/rebuild for the Electron host, OR (b) a `node:sqlite`-backed `data-access` implementation selected behind the NFR2 store seam (the `DataAccess` port already abstracts the driver; a second adapter is the clean shape — NOT a driver import leaking into the extension layer, Rule 13).
  - **Rationale:** Not a 10.1 defect — 10.1's scope is scaffold + driver-LOAD proof, and it honestly recorded the unverifiable host-ABI match rather than faking it (Rule 12). The risk is real but cannot be measured at 10.1's origin (no Electron host reachable here), so it is deferred to its NAMED consumer for measurement (Rule 9 shape: defer a risk to the consumer story that can actually exercise the hot path). It must NOT be silently dropped — surfaced here so 10.2's author sees it before scoping.
  - **Suggested resolution:** At Story 10.2, the FIRST step is to determine empirically (in a real Extension Development Host, the lead's gate) whether the better-sqlite3 addon loads in the host's Electron ABI. (1) If it loads → proceed with the existing data-access driver, retire this item with evidence. (2) If not, and an `@electron/rebuild` against the host ABI succeeds → ship the ABI-matched prebuild (add `@electron/rebuild`/`electron` to the catalog then, verifying the installed CLI surface against the INSTALLED `.d.ts` per Rule 3 — they are deliberately NOT installed now). (3) If both fail → add a `node:sqlite`-backed `DataAccess` adapter selected behind the NFR2 store seam (node:sqlite is proven present + functional in the target Node runtime by 10.1). Do NOT let a driver choice leak board logic or a better-sqlite3 import into the extension layer (Rule 13). Carry this OPEN until 10.2 resolves it with real-host evidence.
  - **RESOLVED by Story 10.2 (code review, 2026-06-02) via path (3):** Story 10.2 added the `node:sqlite`-backed `DataAccess` adapter behind the NFR2 seam (`packages/data-access/src/node-sqlite/`) and PROVED it in a REAL VS Code Electron host (`@vscode/test-electron`). The reviewer independently re-ran `pnpm --filter @agentbbs/vscode-extension test:host` GREEN: the GATE probe + the AC1 host-opens-ledger probe both passed (exit 0), the host opened the ledger via `createDataAccessNodeSqlite` and read back the seeded `in-host-probe-project` (seq 2). Notably this ran on the `insiders` build the harness downloads by default — **electron 42.2.0 / ABI 146 / node 24.15.0 / sqlite 3.51.3** — DIFFERENT from the story's recorded 1.122.1 stable (electron 39.8.8 / ABI 140 / node 22.22.1 / sqlite 3.51.2), so node:sqlite + the adapter are proven across TWO host ABIs, a strictly stronger AC3 result. The better-sqlite3 path stayed byte-identical (`git diff HEAD -- packages/data-access/src/sqlite … data-access.ts` EMPTY) and the production extension bundle (`dist/extension.cjs`) contains `require("node:sqlite")` + ZERO `require("better-sqlite3")` / no `*.node` (reviewer-verified on a fresh `build:bundle`). Item retired with real-host evidence.

## Deferred from: code review of story 10.2 (2026-06-02)

**Story 10.2 (extension host opens the DB via a node:sqlite-backed DataAccess adapter + a postMessage bridge) reviewed: 1 MED found + auto-resolved inline, 0 carried-to-backlog, 1 NAMED-deferred surface confirmed (below).** AC1/AC2/AC3 satisfied with honest real-runtime evidence. **Rule 3 (real-runtime):** the reviewer independently re-ran the `@vscode/test-electron` harness GREEN in a genuine Electron host (see the 10.1-MED resolution above — proven on insiders ABI 146 AND the story's pinned 1.122.1 ABID 140); the adapter contract + the forked race run over a REAL node:sqlite temp-file ledger, nothing stubbed; the node:sqlite + `@vscode/test-electron` APIs are verified against the installed `@types/node`/2.5.2 defs. **Rule 5 (concurrency AC):** the 8-process forked register-race (`node-sqlite-register-race.test.ts` + built worker, IPC start-barrier, shared ledger) is a GENUINE cross-process race proving the atomic check-then-insert read-side derivation (exactly 1 winner / 7 HANDLE_TAKEN / `registeredCount===1`) — not a sequential stand-in. **Rule 7 (mutation):** reviewer re-mutated the marquee adapter semantic (node:sqlite `tx.ts` ROLLBACK→COMMIT-on-error → the atomicity test went RED, `expected 1 to be 0`) AND a bridge semantic (defeated the 9.13-trim guard `if (false && …)` → the whitespace-reject test went RED, `ok` true≠false); BOTH reverted byte-identical (`git diff` empty), full gate GREEN after revert. **Rule 13 (thin client, LOAD-BEARING):** `git diff HEAD -- packages/core packages/mcp-server` EMPTY + the entire better-sqlite3 data-access source byte-identical; the extension imports `@agentbbs/data-access`/`@agentbbs/core` only (no driver import); the bridge maps every affordance to an EXISTING core op (no fabricated op / backdoor) and adds NO agent-facing push (NFR5 pull-only — only the local webview channel is touched); host-surface `BridgeError` (`BAD_REQUEST`/`INTERNAL_ERROR`) stays OUT of core's closed `BoardError` set. Canonical ROOT gate (`pnpm test`) re-run by the reviewer: **1248 passed / 146 files / 0 failed** (1247 + the 1 new regression below). Rule 6 N/A (no `docs/adr`).

- **Story 10.2 · MED · The bridge delta-poll advanced its high-water mark to the PRE-READ `maxSeq()` snapshot, not the last PUSHED event's seq — a duplicate-delta window under a read race (diverged from the proven web-SSE precedent). — RESOLVED inline (code review, 2026-06-02).**
  - **Summary:** `createBridge`'s poll tick read `max = maxSeq()` THEN `events = eventsSince(lastSentSeq)`, pushed the batch, then set `lastSentSeq = max` and stamped the delta frame's `maxSeq = max`. If an event commits BETWEEN those two reads, the pushed batch's last seq is GREATER than `max`; advancing the mark to `max` leaves it BELOW that event, so the NEXT tick re-fetches and RE-PUSHES it — a duplicate delta — and the frame's own `maxSeq` understates the events it carries. The ratified web-SSE poller (`packages/cli/src/host/sse.ts`) does NOT have this gap: it advances `lastSentSeq` to the last pushed `event.seq`. The brief flagged "same correctness the web SSE host proved (exactly-once, de-dup)" as load-bearing; the dev's de-dup QA test only covered idle ticks + the no-overlap advance, not the intra-tick race, so the divergence passed green. Operator-view-only (agents never touch this path), narrow timing window → MED not HIGH.
  - **Resolution:** Advanced to the batch's true high-water mark — `const highWater = events[events.length-1].seq; postMessage({…, maxSeq: highWater}); lastSentSeq = highWater;` — mirroring the SSE precedent exactly (`bridge.ts`). Added a non-vacuous regression test (`bridge.qa.test.ts` — "exactly-once under the read race") that wraps the handle to inject an event between `maxSeq()` and `eventsSince()` and asserts every pushed seq appears exactly once AND the raced event is delivered (not dropped); reviewer-mutation-confirmed RED when the old `= max` advance is reintroduced (`Set size 2 ≠ length 3`), GREEN with the fix; reverted byte-identical. Full ROOT gate `pnpm test` GREEN (1248 passed). Typecheck + lint clean on the touched files.
  - **Suggested resolution:** N/A — resolved inline.

- **Story 10.2 · LOW/INFO · Remaining bridge WRITE ops (react/unreact/joinBoard/updateFocus/announceProject/postAnnouncement/addParticipant) are NAMED-deferred-to-consumer (Epic-10 stories 10.3–10.6), NOT silently dropped. — OPEN (by design; owners: 10.3–10.6).**
  - **Summary:** Story 10.2 wires the bridge READ surface + establishes the request/response WRITE pattern with `reply` only. The remaining writes are named in `bridge.ts`'s header + the story Completion Notes, each a new `OPS` entry of the SAME dispatch shape mapping to an EXISTING core op (no fabricated board op). This is the same incremental posture the web host took. Recording here (in addition to the story) so the parity-audit (Rule 15 — initiate AND respond verbs) and the next-story authors see the explicit deferred set. NOTE: when `updateFocus` is wired, it MUST apply the SAME host-side trim discipline `reply` does (9.13-trim awareness carry — already flagged in `bridge.ts`).
  - **Rationale:** Not a defect — a deliberately scoped surface for a multi-story epic, named not dropped (Rule 13 / the brief's "remaining bridge write ops deferred-to-consumer — confirm NAMED" check). No clickable no-op ships (the webview binding lands in 10.4); the bridge simply does not register the op yet, so an unknown op returns a clean `BAD_REQUEST`.
  - **Suggested resolution:** Each consuming story (10.3–10.6) adds its write(s) as `OPS` entries following the `reply` shape; apply trim discipline to any body/focus-text write; cover each with a bridge test mirroring the `reply` coverage.

## Deferred from: code review of story 10.3 (2026-06-02)

**Story 10.3 (native VS Code `TreeDataProvider` + `FileDecorationProvider` mirroring the web tree) reviewed: 0 HIGH / 0 MED found, 0 patches, 4 LOW/forward-risk items recorded (below), 0 carried-to-backlog from the existing OPEN set (no Epic-10.3 trigger).** AC1/AC2/AC3 satisfied with honest real-runtime evidence. **Rule 12 / Rule 3 (real-runtime, LOAD-BEARING):** the reviewer INDEPENDENTLY re-ran `pnpm --filter @agentbbs/vscode-extension test:host` GREEN in a genuine VS Code **insiders** Electron host (electron 42.2.0 / node 24.15.0 / ABI 146 / sqlite 3.51.3) — the in-host tree probe returned `protoRoomIsNavigableRow:true, protoRoomPending:true, activeBeforePending:true, needsYouFlagged:true, needsYouBucketHasRoom:true, announcementCount:1` (exit 0). This is REAL, not stubbed: the probe seeds a board, builds the SAME vscode-free `buildTreeModel` the provider uses against a real node:sqlite ledger in the host, and asserts the Rule-15 navigable proto-row + the NEEDS-YOU decoration source state out-of-band. The VS Code API surface (`TreeDataProvider`/`TreeItem`/`FileDecorationProvider`/`FileDecoration` badge ≤2-char RUNTIME cap/`createTreeView`/`registerFileDecorationProvider`/`ThemeIcon`/`ThemeColor`/`EventEmitter`/`contributes.{views,viewsContainers,colors,menus}`) verified against the installed `@types/vscode@1.120.0` (engine floor `^1.105`; the badge 2-char cap is a runtime constraint enforced in code + the boundary test). **Rule 7 (mutation — reviewer-RE-confirmed non-vacuous):** (1) Rule-15 navigability — dropping the open `command` on PENDING rows in `getTreeItem` turned the QA structural test RED (`expected undefined to be defined`); (2) badge-cap boundary — widening `BADGE_MAX_LENGTH` 2→3 turned the `100 → '•'` provider+model assertions RED (`expected '100' to be '•'`); (3) NEEDS-YOU-outranks-unread precedence — flipping the order in `roomDecoration` turned BOTH the pure + provider precedence tests RED (`expected '42' to be '!'`). ALL THREE reverted byte-identical, the 47 tree tests + full ROOT gate (`pnpm test` = **1295 passed / 152 files / 0 failed**) GREEN after revert. **Rule 13 (thin client, LOAD-BEARING):** `git diff HEAD -- packages/core packages/mcp-server` EMPTY (agent contract byte-identical); the tree composes ONLY existing core read ops (`listProjects`/`listRooms`/`listAnnouncements`/`needsYouRooms` + `roomJoinSeq`/`roomMessagesSince` for unread), reimplements NO fold/gate, fabricates NO op; NEEDS YOU = the deterministic `needsYouRooms` (`add_participant(@operator)`) set, NOT an `@mention` body-scan; the de-dup is a faithful (defensive) mirror of the web `activeRows`/`pendingRows` split (`listRooms`/`listAnnouncements` are disjoint by `active` in core, so it never actually fires — harmless). **Rule 1 (integration AC):** real — AC1-3 are real-runtime; the producer→consumer wire-up names Story 10.4 (the open-room command BODY = the WebviewPanel). **Rule 6 N/A** (no `docs/adr`). The `eslint.config.js` `BoardTreeProvider.ts` filename-case exemption is a narrow additive ignore matching the architecture.md-committed class-per-file name (NOT a weakening — the vscode-free logic modules stay kebab-case). The relaxed 10.1/10.2 activation-count assertions (`toBe(1)` → `toBeGreaterThanOrEqual(1)` + `toContain`) are NOT a weakening: `bundle-and-activation.test.ts` now asserts EVERY subscription is a callable disposable (a STRONGER cleanup-contract check) over the legitimately-larger command set (QA's "not weakened" assessment re-confirmed).

- **Story 10.3 · LOW · The open-room command (`agentbbs.openRoom`) is a thin SEAM whose handler is a placeholder (info-message + log), to be filled by Story 10.4 with the room WebviewPanel. — OPEN (by design; owner: Story 10.4).**
  - **Summary:** Every room/proto-room/needs-you row carries `command: agentbbs.openRoom` with the roomId arg and is selectable (Rule 15 — navigability structurally present NOW, proven in-host). The command IS registered in `activate()` (not a silent dead no-op): selecting a row shows `"open room … — the room view lands in Story 10.4"` + logs. Story 10.4 replaces ONLY the handler body with the WebviewPanel; the row contract (selectable + command-carrying + `resourceUri` decoration key) is final.
  - **Rationale:** Not a defect — the deliberate, named producer→consumer seam the Integration AC + Completion Notes call out. A registered placeholder (visible feedback) is the Rule-13-correct shape, not an unrecorded clickable no-op.
  - **Suggested resolution:** Story 10.4 swaps the `OPEN_ROOM_COMMAND` handler body in `extension.ts#registerBoardTree` for the room WebviewPanel mount; the row/command/URI contract is unchanged.

- **Story 10.3 · LOW · The `＋ join a project…` action row (`agentbbs.joinProject`) is an INERT SEAM (registered command, info-message placeholder) — the VS Code analogue of the web's `9.4-join-project-inert` (which 9.12 resolved on the WEB surface only). — OPEN (by design; owner: a later Epic-10 story / the operator-initiate-parity surface).**
  - **Summary:** The native tree shows a `＋ join a project…` root action row mirroring the web tree; its `agentbbs.joinProject` command is registered but currently shows `"join a project — the project-join flow lands in a later story"`. The web's join-project picker was wired in Story 9.12 (`joinBoard`), but that lives in the web host (`json-api.ts`) which the extension does not share; the VS Code surface re-introduces the inert seam until a later Epic-10 story wires the same `joinBoard` core op through the extension (or the postMessage bridge / a host-side QuickPick). Recorded per Rule 13 (a visible affordance whose op is deferred must be a recorded inert stub, never an unrecorded no-op) — it is a registered command with operator feedback, not a dead click.
  - **Rationale:** Not a defect — the AC + Completion Notes name it a seam; no core op is fabricated and no backdoor is opened. This is the VS Code instance of the operator-INITIATE parity surface (cf. `9-OPERATOR-INITIATE-PARITY`, resolved on the web by 9.11–9.14): the extension will need the same initiate verbs (announce_project / post_announcement / join_board) wired through its own host, mapped to the EXISTING core ops.
  - **Suggested resolution:** A later Epic-10 story (or the extension's operator-initiate surface) wires `agentbbs.joinProject` to a QuickPick over `listProjects` → the existing `joinBoard` core op (mirroring the web 9.12 picker), and audits BOTH parity directions (Rule 15) for the VS Code surface so initiate verbs are not silently dropped the way the web's were before the 9.11–9.14 addendum.

- **Story 10.3 · LOW · The uncapped exact unread count carried in `TreeItem.description` (`"N new"`) — AC3's "the count in `TreeItem.description` instead" half — has no direct test assertion. — STILL OPEN after Story 10.6 (test-coverage gap; owner RE-ASSIGNED → the next story that touches the tree provider, or the Epic 10 retro).**
  - **Summary:** AC3 offers two count-display paths: the ≤2-char `badge` (capped, `99`→`•`) AND the uncapped exact count in `TreeItem.description`. The badge cap is mutation-tested non-vacuous (boundary 99/100); the `description = "${row.unreadCount} new"` line in `BoardTreeProvider.getTreeItem` (the room case, `BoardTreeProvider.ts:159`) is NOT asserted by any test (the QA provider test pins command/resourceUri/contextValue but not `description`). The code is correct — this is a coverage gap, not a defect; a regression that dropped/garbled the description would ship green.
  - **Why Story 10.6 did NOT close it (reviewer note, 2026-06-02):** the 10.3 entry named 10.6 as owner on the premise that the live-fold consumer "re-exercises the row description under live count changes." In implementation, Story 10.6 made a deliberate Design decision (recorded in its Task 1 + Completion Notes) to keep the tree live-refresh on the EXISTING 2s host `refreshBoard` poll (Story 10.3) rather than wire the per-webview delta poll into the tree (a per-event tree push would be the storm AC1 warns against). Consequently 10.6's changeset does NOT touch `apps/vscode-extension/src/tree/*` at all (`git diff` confirms the tree provider + its QA test are byte-identical to HEAD) — so adding a tree-provider assertion here would have been out of the realized scope of 10.6's three ACs (live fold / HC / a11y), all of which live in the webview + host theme plumbing, not the tree. The gap is genuinely LOW (the cap-relevant badge half is fully covered; the description is a display nicety) and does not block 10.6.
  - **Rationale:** LOW — unchanged from the 10.3 assessment; the only delta is the owner. Carrying it as a thin coverage assertion to be added the next time the tree provider is edited (or swept at the Epic 10 retro) is the proportionate handling; it does not warrant a tree-provider edit inside an unrelated capstone story.
  - **Suggested resolution:** Whoever next edits `BoardTreeProvider`/its QA test (or the Epic 10 retro sweep) adds a provider assertion that an unread room row's `TreeItem.description` carries the EXACT count (e.g. `"100 new"` while the badge collapses to `•`), proving the uncapped path; mutation-test by dropping the description line.

- **Story 10.3 · LOW/INFO · `tree/operator-handle.ts#resolveOperatorHandle`/`canonicalizeOperatorHandle` is DUPLICATED (not imported) from the web host's `packages/cli/src/ui.ts#resolveOperatorHandle` — a forward DRIFT risk if the canonicalization rule (trim+lowercase) ever changes on one side only. — OPEN (forward-risk; owner: whoever next touches operator-handle canonicalization on either surface).**
  - **Summary:** The extension cannot depend on `@agentbbs/cli` (apps are leaf packages — no app↔cli dependency), so the operator-handle canonicalization (trim + lowercase; empty→null; setting > env precedence) is intentionally re-implemented in `tree/operator-handle.ts`. The reviewer confirmed it is BYTE-equivalent in behavior to the CLI version (verified `packages/cli/src/ui.ts:45-49` — same `raw.trim().toLowerCase()`, same empty→null). The duplication is the correct V1 shape (the alternative — hoisting it into a shared `ui-shared`/`core` helper — is a refactor out of 10.3's scope), but it is a latent drift risk: a future change to how core stores `room.participant_added.handle` (e.g. case-folding rules) must update BOTH sites or the NEEDS-YOU match silently diverges between the web and VS Code surfaces.
  - **Rationale:** Not a defect — deliberate, behavior-verified duplication forced by the leaf-package boundary (Rule 13). Recorded so the next agent who changes handle canonicalization on EITHER surface knows there are TWO sites + (ideally) hoists to a shared helper at that point.
  - **Suggested resolution:** When operator-handle canonicalization next changes (either surface), hoist the single rule into a shared location both the web host (`@agentbbs/cli`) and the extension can consume without an app↔app/app↔cli dependency — the natural home is `@agentbbs/ui-shared` (already a dependency of both) or `@agentbbs/core` (if the rule is deemed board-canonical). Until then, keep the two sites byte-equivalent and cross-reference them in comments (already done in `operator-handle.ts`).

## Deferred from: code review of story 10.4 (2026-06-02)

**Story 10.4 (rooms open as VS Code WebviewPanels mounting the byte-shared `@agentbbs/ui-shared` RoomView with a `--vscode-*` theme layer, closing operator RESPOND-parity via proto-room reply-to-activate) reviewed: 0 HIGH / 0 MED found, 0 patches, 3 LOW/forward-risk items recorded (below).** AC1–AC4 satisfied with honest real-runtime evidence. **Rule 12 / Rule 3 (real-runtime, LOAD-BEARING):** the reviewer INDEPENDENTLY re-ran `pnpm --filter @agentbbs/vscode-extension test:host` GREEN in a genuine VS Code **insiders** Electron host (electron 42.2.0 / node 24.15.0 / ABI 146 / sqlite 3.51.3) — the `AC4 room-panel-reply-activates` probe returned all 10 fields true (`panelCreated, panelHasWebview, htmlHasNonceCsp, htmlHasRoomId, htmlNoUnsafe, revealNotDuplicate, protoInactiveBefore, activeAfterReply, activatedByOperator`, exit 0). REAL, not stubbed: a genuine `vscode.window.createWebviewPanel` is created, its real `webview.cspSource`/`asWebviewUri`/`html` read, reveal-not-duplicate exercised, and the proto-room `active:false→true` flip asserted OUT-OF-BAND via a fresh `dispatchRequest('readRoom')` over the real node:sqlite ledger. The honest, documented limitation (in the probe header): the actual webview JS round-trip / theme PAINT is the lead's chrome-devtools smoke — the probe drives the bridge dispatch directly. The VS Code webview API (`createWebviewPanel(viewType,title,ViewColumn|{…},opts)`, `WebviewPanel.{title,iconPath,webview,reveal,onDidDispose}`, `Webview.{html,options,cspSource,asWebviewUri,postMessage,onDidReceiveMessage}`, `Uri.{file,joinPath}`, `ViewColumn.Active`) verified against installed `@types/vscode@1.120.0`; the `--vscode-*` token ids verified against the official theme-color reference (Dev Notes delta recorded). **Rule 12 canonical gate:** reviewer re-ran ROOT `pnpm test` = **1359 passed / 158 files / 0 failed** (the `RoomApp.test.tsx`/`RoomApp.qa.test.tsx` DOM tests map to the root `ui-shared-dom` happy-dom project via the `apps/vscode-extension/src/**/*.test.tsx` glob + are excluded from the node project — Rule 12 corollary, NOT run per-package). Aggregate gate independently re-run GREEN: lint 0, typecheck 0, format clean, build clean (BOTH bundles — host `dist/extension.cjs` 54.3kb REACT-FREE; webview `dist/webview/main.js` 10.9mb [Shiki grammars] + `main.css` 8.5kb, BROWSER esm, reviewer-verified ZERO `node:`/`fs`/`path` built-in refs in the webview bundle). **Rule 7 (mutation — reviewer RE-confirmed non-vacuous, all 3 reverted byte-identical):** (1) AC4 reply-activates — replacing the bridge `reply` op's `await reply(...)` with a no-op that returns `active:false` turned `bridge.test.ts`'s proto-room-activation test RED (`expected false to be true`); (2) CSP content-guard — adding `'unsafe-inline'` to `script-src` turned `webview-html.qa.test.ts`'s script-src exact-match RED; (3) Rule-13 byte-identical pin — appending a comment to `packages/ui-shared/src/tokens.css` turned `vscode-tokens.qa.test.ts`'s `git diff … tokens.css` === '' guard RED. ALL reverted byte-identical (`git diff` confirmed clean), full gate GREEN after revert. **Rule 13 (thin client, LOAD-BEARING):** `git diff HEAD -- packages/core packages/mcp-server packages/ui-shared` EMPTY (agent contract + the ENTIRE ui-shared source byte-identical) — RoomView is MOUNTED not forked (`RoomApp.tsx` imports `{ Composer, RoomView } from '@agentbbs/ui-shared'`); the `--vscode-*` layer is a separate `:root` OVERRIDE loaded LAST (tokens.css unedited); every affordance maps to an EXISTING core op via the bridge (SEND→`reply` [the Epic-4 min-seq activator IS the activation — no fabricated activate op], 👍→`react`/`unreact` [the SAME ops an agent uses, named-deferred-to-consumer in 10.2, consumed HERE so the 👍 is NOT a dead click], `[ join room to post ]`→local intent [grant-on-act, no standalone room-join op]); a watching non-participant's chip is the disabled join hand-off (no backdoor — `RoomApp.qa.test.tsx` proves no bridge react fires); the bridge push is host→ITS-OWN-webview only (NFR5 pull-only preserved; agents keep `check`); the `operatorHandle`/`data-room-id` are HOST-SURFACE display fields, NOT agent-contract fields. **Rule 15 (parity, both directions):** AC4 closes the operator RESPOND half (open/read/reply-to-activate an announced proto-room) on VS Code, mapped to the SAME `reply` an agent uses — consuming Story 10.3's navigable proto-room rows; the operator INITIATE half (announce/post/join/focus) stays NAMED-deferred to Story 10.7. **Rule 1 (integration AC):** real — AC1/AC2/AC4 are real-runtime; producer→consumer named (10.5 hardens CSP/serializer, 10.6 live fold + a11y). **Rule 5/6 N/A** (no NFR tripwire worked around; no `docs/adr`). The data-access + `bridge.qa.test.ts` diffs the story labels "pre-existing prettier drift, whitespace-only" were reviewer-confirmed pure `format:write` reflow (line-wrapping multi-arg calls / collapsing an import) with NO assertion or logic change (`git diff -w` shows only structural reformatting; the data-access `.ts` is an import collapse).

- **Story 10.4 · LOW/INFO · The aggregate `pnpm run typecheck` + `pnpm run format` gate was RED on baseline `0076e6a` (pre-existing 10.3 drift); the 10.3 code review verified only ROOT `pnpm test` (vitest, 1295), not the SEPARATE aggregate typecheck/format commands — so the drift shipped past the 10.3 smoke and was caught only at 10.4 dev-time. — RESOLVED inline by 10.4 dev (non-behavioral); PROCESS note recorded.**
  - **Summary:** The 10.4 dev found (and fixed, verifying via `git stash -u` on the pristine tree) that on `0076e6a`: (a) `pnpm run typecheck` was RED — a `decorations.qa.test.ts` test helper typed its param `provideFileDecoration: (uri: unknown) => …`, which is contravariantly INCOMPATIBLE with the real `(uri: vscode.Uri) => …` under `strictFunctionTypes` once `@types/vscode` resolved to 1.120.0's stricter `Uri`; (b) `pnpm run format` was RED on `bridge.qa.test.ts` + `node-sqlite-register-race.test.ts` (+ 3 more data-access files). The 10.3 deferred-work entry reports the 10.3 review re-ran `pnpm test` GREEN at 1295 — but `pnpm test` is vitest only; the aggregate `typecheck`/`format` commands are a separate gate the 10.3 review did not independently re-run. The reviewer confirmed the 10.4 fixes are NON-behavioral: a type-only `import type { Uri as VscodeUri }` + `format:write` reflow (whitespace/line-wrap only — verified `git diff -w` shows no logic change), and the full aggregate gate (lint/typecheck/format/build) + ROOT `pnpm test` are now GREEN (reviewer-re-run).
  - **Rationale:** Not a 10.4 defect — 10.4 correctly ground-truthed (Rule 6 discipline) that the drift PRE-DATED its baseline and fixed it minimally + non-behaviorally so its own dev-gate is honest. LOW/process: the latent risk is that a future epic-cycle smoke that runs `pnpm test` as its ONLY gate will again let typecheck/format drift accumulate silently between stories until a dev trips on it.
  - **Suggested resolution:** The per-story lead smoke (and the code-review Rule-12 canonical-gate step) should run the FULL aggregate gate (`pnpm run lint && pnpm run typecheck && pnpm run format && pnpm run build && pnpm test`), not `pnpm test` alone — so typecheck/format drift surfaces at the story that introduces it, not 1–2 stories downstream. (No code change owed; this is a gate-discipline note for the remaining Epic-10 stories' smokes.)

- **Story 10.4 · LOW · The room webview CSP `img-src` admits `https:` (any HTTPS origin) and `font-src` is not pinned to the nonce — a baseline-strict-but-not-maximally-tight CSP, with full hardening (retain-context / LRU / serializer / tightening `img-src`) deferred to Story 10.5. — OPEN (by design; owner: Story 10.5).**
  - **Summary:** `buildRoomWebviewHtml` ships `default-src 'none'; img-src ${cspSource} https: data:; font-src ${cspSource}; style-src ${cspSource} 'nonce-…'; script-src 'nonce-…'`. The SECURITY-CRITICAL directives are already strict and content-guarded (`script-src` = nonce-only, no `unsafe-inline`/`unsafe-eval`/`*`; `style-src` = cspSource + nonce, no unsafe-inline; `default-src 'none'`). The `img-src https:` is broader than strictly necessary (it allows any HTTPS image origin); for the INERT-markdown stack (markdown-it HTML-off → DOMPurify → Shiki class spans) remote `<img>` is the only content-triggered network a body could carry, and DOMPurify already governs the body — so this is a defense-in-depth nicety, not a hole. The `webview-html.qa.test.ts` guard pins the load-bearing directives but does NOT assert `img-src`/`font-src` content (so a future loosening of those two would ship green).
  - **Rationale:** LOW — this story's CSP is a genuinely-strict baseline (NOT a loose placeholder), and the story explicitly names full hardening as 10.5's scope. The unpinned `img-src`/`font-src` is the only gap and is non-security-critical for an inert surface.
  - **Suggested resolution:** Story 10.5 (the CSP-hardening consumer) tightens `img-src` to `${cspSource} data:` (drop `https:` unless a real remote-image requirement surfaces), decides `font-src`, and EXTENDS the content-guard to pin `img-src`/`font-src` too (so every directive is mutation-tested non-vacuous, not just script/style/default).

- **Story 10.4 · LOW/INFO · The `bridge-client.ts` `createPostMessageBridge` `onDelta` hook + the host bridge's delta push are present but UNCONSUMED by the webview in 10.4 (live host→webview fold is Story 10.6); the model is RE-READ on each write instead. — RESOLVED by Story 10.6 (the live-fold consumer).**
  - **Resolution (Story 10.6, reviewer-confirmed):** `main.tsx`'s `createPostMessageBridge(api, { onDelta, onThemeKind, onStatus })` now wires `onDelta` through a single-handler registry into `RoomApp`'s `subscribeToDeltas` effect, which folds each raw camelCase core Event into the live `RoomViewModel` via the pure `room-fold.ts` reducer (append-by-seq, idempotent de-dup, reaction → re-derive FR21 agreed mark, optimistic-echo reconcile) — mirroring web 9.9. The re-read remains the reconcile-on-write fallback (`reload`/`reconcileReread`). Proven: `RoomApp.live.test.tsx` (a host-pushed delta updates the thread with NO re-read) + the real-host probe `livePollPushedDelta=true` (the bridge MAX(seq) poll pushed a delta on a real appended event over real node:sqlite). The hook was NOT re-added (it pre-existed from 10.2/10.4 as recorded).
  - **Summary:** The webview bridge client exposes an `onDelta(delta)` callback and the host-side bridge pushes `type:'delta'` frames (10.2), but `RoomApp`/`main.tsx` do NOT wire `onDelta` — after a `reply`/`react`/`unreact` the model is fully RE-READ over the bridge (`loadRoomViewModel`) to reflect the authoritative result. This is correct + sufficient for 10.4 (the operator sees their own write reflected); the rich live SSE-style fold (another actor's message appearing without a re-read) is named 10.6.
  - **Rationale:** Not a defect — re-read-on-write is a faithful, simpler V1 of the web's optimistic+delta model; the `onDelta` seam is wired so 10.6 has nothing to retrofit. Recorded so 10.6 knows the hook EXISTS (don't re-add it) and the re-read is the path it replaces/augments.
  - **Suggested resolution:** Story 10.6 wires `createPostMessageBridge(api, onDelta)`'s callback into `RoomApp` to fold deltas into the model (mirroring the web SSE consumer), keeping the re-read as the reconcile-on-write fallback; add a DOM test that a host-pushed delta updates the thread without a full re-read.

## Deferred from: code review of story 10.5 (2026-06-02)

**Story 10.5 (strict webview CSP hardening — `default-src 'none'`, per-load nonce, `img-src`/`font-src` pinned to cspSource only, `connect-src 'none'`, no `unsafe-*`/`data:`/wildcard; inert markdown; `retainContextWhenHidden`+LRU; `WebviewPanelSerializer`) reviewed: 0 HIGH / 0 MED found, 0 patches, 2 LOW/INFO + 1 forward-risk recorded (below).** AC1/AC2 satisfied with honest real-runtime evidence. **Rule 3 / Rule 12 (real-runtime, LOAD-BEARING):** the reviewer INDEPENDENTLY re-ran `pnpm --filter @agentbbs/vscode-extension test:host` GREEN in a genuine VS Code Electron host (extension host pid exited code 0) — `cspStrict:true`, `serializerRegistered:true`, `deserializeReattached:true`, plus all AC4 carry fields (`panelCreated`…`activatedByOperator`). REAL, not stubbed: a genuine `vscode.window.createWebviewPanel`'s real `webview.cspSource` is read and EACH CSP directive asserted pinned (the in-host probe correctly relaxes img/font to the host's actual cspSource — `'self' https://*.vscode-cdn.net` on a modern build — which is the host's OWN-asset allowlist, NOT a wildcard the extension added; the dev's recorded real-host finding is accurate and the probe's cspSource-relative assertion is the correct shape), the serializer registered via the real `registerWebviewPanelSerializer`, and a deserialize→adopt round-trip on a FRESH real panel re-attaches it (reveal-not-duplicate post-restore). The honest, documented limitation (probe header + Dev Agent Record): a TRUE window-reload restore (VS Code persisting + reviving across an actual `reloadWindow`) cannot be triggered headlessly — that half is a MANUAL lead-reload smoke step, NOT asserted green on a stub (Rule 12 honesty confirmed). **Rule 3 external-API:** the three load-bearing VS Code APIs verified against installed `@types/vscode@1.120.0` — `readonly retainContextWhenHidden?: boolean` (line 10082, CONFIRMED READONLY — the dev's load-bearing finding that the LRU caps how many panels are CREATED retained, not un-retains live panels, is correct), `deserializeWebviewPanel(webviewPanel, state)` (line 10221, panel FIRST/state SECOND — confirmed), `registerWebviewPanelSerializer(viewType, serializer): Disposable` (line 11743 — confirmed). **Rule 12 canonical gate:** reviewer re-ran ROOT `pnpm test` = **1397 passed / 163 files / 1 failed** — the SOLE failure is `packages/ui-shared/src/markdown/highlight.test.ts` (a Shiki `@shikijs/primitive` tokenizer internal `Cannot read properties of undefined (reading 'startIndex')` under full-suite parallel load); it PASSES in isolation (7/7) and the file + the entire `ui-shared` package are byte-identical to baseline (untouched by 10.5, Rule 13). This is the known Shiki-warmup flake class (deferred-work `9.5-shiki-warmup`), recurring at the full-suite gate — NOT a 10.5 regression (Rule 6: verified against git ground-truth). The FULL aggregate gate was independently re-run GREEN: `pnpm run build` (8/8 — both bundles, webview `main.js` 10.9mb + `main.css` 8.5kb), `pnpm --filter @agentbbs/vscode-extension typecheck` (0), `pnpm run lint` (0), `pnpm run format --check` (clean). **Rule 7 (mutation — reviewer RE-confirmed non-vacuous, all reverted byte-identical):** (1) CSP per-directive guard — loosened the REAL `webview-html.ts` `img-src ${cspSource}` → `${cspSource} data: https:` → 4 guards RED across `webview-html.csp.test.ts` + `webview-html.qa.test.ts` (the strict-shape exact-match + whole-CSP floor in both); reverted byte-identical (`git diff` clean), 16/16 GREEN. (2) LRU eviction boundary — flipped `room-lru.ts` `idx < this.cap` → `idx <= this.cap` → 4 RED in `room-lru.test.ts`; reverted byte-identical, 7/7 GREEN. (3) the inert-render contract (`RoomApp.inert.test.tsx`) + the state-roundtrip producer→consumer seam (`state-roundtrip.qa.test.ts`) each carry their OWN in-test mutation proof (a raw un-sanitized injection → `assertSubtreeInert` RED; a drifted `{ room }`/`{ id }` shape → `roomIdFromState` null) — reviewer confirmed both discriminate. **Rule 13 (thin client, LOAD-BEARING):** `git diff HEAD -- packages/core packages/mcp-server packages/ui-shared` EMPTY (also vs baseline `995375f`) — pure client hardening; no new board op, no agent push; `operatorHandle`/`data-room-id` stay HOST-SURFACE display fields; the inert ui-shared stack reused as-is; `connect-src 'none'` (the webview bundle has zero fetch/EventSource — the bridge is postMessage-only, NFR5 pull-only preserved). **Rule 1 (integration AC):** real — AC1/AC2 are real-runtime hardening over the existing wired 10.4 surface, verified in the Electron host. **Rule 5/6 N/A** (no NFR tripwire worked around — NFR12 inert + AR22 resilience implementable as worded; no `docs/adr`). **Resolved-confirmation:** the 10.4 LOW `Story 10.4 · img-src https:/font-src unpinned` is **RESOLVED by Story 10.5** — `img-src`/`font-src` are now pinned to `${cspSource}` only (the `10.4` `https:`/`data:` grant dropped after verifying the inert stack emits no images / bundles no font), `connect-src 'none'` added, and the content-guard EXTENDED + mutation-tested to pin every directive (not just script/style/default).

- **Story 10.5 · LOW · The Shiki full-suite tokenizer flake recurred at the code-review canonical gate (`highlight.test.ts` RED under parallel load, GREEN in isolation) — a NEW failure SIGNATURE (`@shikijs/primitive` `startIndex` undefined) distinct from the alias-miss the `9.5-shiki-warmup` entry analyzed. — OPEN (test-infra; untouched package; recorded per Rule 8, NOT normalized).**
  - **Summary:** The reviewer's ROOT `pnpm test` reported `1 failed | 1397 passed` — the failure is `packages/ui-shared/src/markdown/highlight.test.ts > emits CSS-class spans` throwing `TypeError: Cannot read properties of undefined (reading 'startIndex')` deep inside `@shikijs/primitive`'s `_tokenizeWithTheme` (NOT an assertion failure; the highlighter itself faulted mid-tokenize). Re-running the file in isolation is GREEN (7/7). The file and the whole `ui-shared` package are byte-identical to baseline `995375f` (Rule 13) — outside the Story-10.5 changeset entirely. This is a different manifestation than the `9.5-shiki-warmup` entry (which was a deterministic `ts`-alias miss, since fixed): this is a genuine Shiki-internal tokenization fault that surfaces only under full-suite concurrency/memory pressure (the webview bundle now builds a 10.9mb Shiki-grammar `main.js`, and several files prewarm the highlighter concurrently).
  - **Rationale:** LOW — not a 10.5 regression (the changeset touches no ui-shared/Shiki code; the failing test is unmodified and passes in isolation), but a recurring full-suite flake that, like the `seed-protocol-race` EPERM (`E10-baseline-seedrace-eperm`), makes a SOLE-failure ROOT-`pnpm test` ambiguous at each gate. The `9.5-shiki-warmup` analysis (singleton + file-isolation = deterministic architecture) does not explain THIS failure mode (a library-internal `startIndex` read), so it is recorded as a distinct observation rather than folded into that entry.
  - **Suggested resolution:** When the markdown/Shiki test infra is next touched, reproduce under full-suite parallel load (not isolation) and consider: pinning/upgrading `@shikijs/*` (the fault is in `@shikijs/primitive@4.1.0`), serializing the highlighter-using DOM tests (a shared `prewarmHighlighter` barrier or a `poolMatchGlobs`/single-thread group for the markdown suites), or adding a retry to the known-flaky highlighter tests. Until then, treat a SOLE `highlight.test.ts` `startIndex` failure under full-suite load as this known flake (re-run the file in isolation to confirm GREEN) — do NOT read it as a regression (Rule 6: verify against ground truth).

- **Story 10.5 · LOW/FORWARD-RISK · The `retainContextWhenHidden`-is-readonly constraint means a NON-retained room panel re-renders (re-mounts) every hidden→visible focus transition (re-set HTML → fresh nonce → bridge re-feed), discarding any unsaved in-webview UI state (e.g. composer draft text) on each focus — by design for V1, but a UX edge for 10.6/10.7. — OPEN (by design; awareness for Stories 10.6/10.7).**
  - **Summary:** Because `retainContextWhenHidden` is readonly (fixed at create-time), the LRU bounds live-DOM memory to `cap` (default 4) by creating panels BEYOND the cap NON-retained. A non-retained panel's `onDidChangeViewState` re-render path (`room-panel.ts` `wirePanel` → `setPanelHtml`) tears down and re-mounts the webview on focus — correct for re-displaying the authoritative room model, but it ALSO discards any client-only state the webview held (a half-typed composer reply, scroll position). For an operator who has >cap rooms open and types a draft in the (cap+1)-th, switching away and back loses the draft. This is acceptable for the 10.5 hardening scope (the room model is re-read correct; no DATA loss — the draft was never sent) and matches the documented bounded-memory tradeoff, but the compose surfaces (Story 10.7) and the live-fold (10.6) will make in-webview state more valuable.
  - **Rationale:** LOW — not a defect (no persisted/sent data is lost; the re-render is the correct bounded-memory behavior and the active + cap-1 most-recent rooms ARE retained, so the common case is covered). Recorded as forward-risk because 10.7's compose surfaces are exactly where an unsaved draft on a non-retained panel would bite.
  - **Suggested resolution:** Story 10.7 (compose surfaces) should decide whether composer draft text needs to survive a non-retained re-mount — if so, persist the draft into the webview `setState` alongside `{ roomId }` (the serializer/round-trip plumbing already exists) and restore it on re-mount, OR ensure the actively-composing room is always within the retain window (touch the LRU on compose-focus, not just panel-reveal). No action owed in 10.5.

## Deferred from: code review of story 10.6 (2026-06-02)

**Story 10.6 (live delta-fold in the room webview + ColorThemeKind high-contrast + a11y parity inherited from ui-shared 9.10) reviewed: 0 HIGH / 0 MED found, 0 patches, 1 LOW/forward-risk recorded (below); 2 prior deferred items resolved/re-owned (10.4 onDelta → RESOLVED; 10.3 TreeItem.description → re-owned, see above).** AC1/AC2/AC3 satisfied with honest real-runtime evidence. **Rule 12 / Rule 3 (real-runtime, LOAD-BEARING):** the reviewer INDEPENDENTLY re-ran `pnpm --filter @agentbbs/vscode-extension test:host` GREEN in a genuine VS Code insiders Electron host (electron 42.2.0 / node 24.15.0 / ABI 146 / sqlite 3.51.3, extension host exit 0) — the `room-panel` probe returned ALL fields true incl. the new 10.6 ones: `themeKindRead:"dark"`, `htmlHasThemeKind:true` (ColorThemeKind read in-host + flows into the panel HTML's initial `data-theme-kind`), `themeKindPushed:true` (`postThemeKind` pushed a real `{type:'themeKind'}` frame to a REAL webview — the live re-theme producer), `livePollPushedDelta:true` (the bridge MAX(seq) poll pushed a delta host→webview on a REAL appended event over the REAL node:sqlite handle — the live-fold producer half). The honest documented limitation (probe header + Completion Notes, Rule-12 honesty CONFIRMED, NOT faked green): the actual HC PAINT after a live theme switch + the screen-reader pass are NOT automatable headlessly — they are the lead's manual smoke; the probe proves detection + push + attr-flow, which is the automatable half. (The benign `Webview.loadLocalResource ... main.js/main.css` host warnings are because the built bundle is not copied to the `.vscode-test` resource path — the probe asserts the host surface + bridge dispatch, not the bundle paint, exactly as documented.) **Rule 3 external-API:** `ColorThemeKind { Light=1, Dark=2, HighContrast=3, HighContrastLight=4 }`, `window.activeColorTheme: ColorTheme { readonly kind }`, `window.onDidChangeActiveColorTheme: Event<ColorTheme>` independently re-verified against the INSTALLED `@types/vscode@1.120.0` (`index.d.ts:8678` enum, `:11843` activeColorTheme, `:11848` onDidChange) — the `theme-kind.ts` numeric mapping is correct; the dev's Research-First finding that VS Code injects `--vscode-*` webview vars APPEND-ONLY (so HC must NOT be detected by `--vscode-contrastBorder` presence, but by the host-pushed ColorThemeKind) is sound and is the basis of the `data-theme-kind` design. **Rule 12 canonical gate (FULL aggregate, not vitest-only — the 10.3-drift lesson):** reviewer re-ran ROOT `pnpm test` = **1434 passed / 168 files / 0 failed**; `pnpm typecheck` = 0; `pnpm lint` = 0; `pnpm format` = clean; `pnpm build` = clean (BOTH bundles — host `dist/extension.cjs` 62.5kb + webview `dist/webview/main.js` 10.9mb + `main.css` 9.3kb). No baseline-flake recurrence this run (neither the `seed-protocol-race` EPERM nor the Shiki full-suite flake surfaced). **Rule 7 (mutation — reviewer RE-confirmed non-vacuous, BOTH reverted byte-identical, re-GREEN 17/17):** (1) FR21 selector — flipped `deriveAgreedSeq`'s `m.seq > agreed` → `m.seq < agreed` in `room-fold.ts` → 3 RED (`deriveAgreedSeq` HIGHEST-seq pin + the agreed-mark-MOVES + the revert tests); (2) de-dup idempotency — defeated the `foldReplied` `model.messages.some(...seq === event.seq...)` guard with `false &&` → the idempotent-re-fold "no double-append" test RED. Reverted byte-identical (`git diff` clean), 17/17 GREEN after revert. The nets-to-null FR21 edge (last live 👍 retracted → agreed mark DISAPPEARS, not stepped down) + the wash→transparent HC gate (with its non-vacuous non-HC mirror) are separately pinned in the QA seam tests + `RoomApp.live.test.tsx`. **Rule 13 (thin client, LOAD-BEARING):** `git diff HEAD -- packages/core packages/mcp-server packages/ui-shared` EMPTY (the agent contract + the ENTIRE ui-shared source byte-identical) — the live fold, HC token mapping, and a11y are all CLIENT-layer; `room-fold.ts` is a PARALLEL camelCase reducer (no core/data-access import) mirroring web 9.9 semantics, NOT a fork; the `highContrast` prop + `AGREED_POST_WASH→transparent` gate are INHERITED from ui-shared 9.10 (`MessagePost.tsx:179`, not edited); the `themeKind` frame is a new HOST↔WEBVIEW message type, NOT an agent-contract change. **NFR5 (pull-only) structurally CONFIRMED:** the delta push + the themeKind push are host→ITS-OWN-webview only (the operator's editor view); no agent/MCP push path added — agents keep `check`; the tree live-refresh rides the EXISTING 2s host `refreshBoard` poll (Story 10.3, untouched by 10.6), NOT a per-event storm (the Task-1 Design decision, honored — `git diff` shows the tree provider + its interval byte-identical). **NFR12 (inert) preserved:** the in-host probe's `cspStrict` carry confirms `default-src 'none'; ... script-src 'nonce-…'; connect-src 'none'` (no `unsafe-*`/`data:`/wildcard) — the 10.6 changeset adds no network/script surface (the live channel is postMessage, the only host→webview transport). **Rule 1 (integration AC):** real — AC1 (host poll producer → webview fold consumer) is exercised end-to-end in-host (`livePollPushedDelta`) + in DOM (`RoomApp.live.test.tsx`); AC2 (ColorThemeKind detect + `onDidChangeActiveColorTheme` push) in-host (`themeKindRead`/`htmlHasThemeKind`/`themeKindPushed`); AC3 (a11y) inherited byte-identical from the 9.10 ui-shared suite the webview mounts. **Rule 5/6 N/A** (no NFR tripwire worked around — NFR5 pull-only + NFR12 inert implementable as worded; no `docs/adr`, Rule 6 N/A).

- **Story 10.6 · LOW/FORWARD-RISK · The `9.5-shiki-warmup` / `10.5` Shiki full-suite tokenizer flake is now INTERMITTENT at the gate; Story 10.6 added a markdown-rendering DOM test (`RoomApp.live.test.tsx`) that correctly mitigates via `beforeAll(prewarmHighlighter)` + the `isHighlighterWarm()` hardening — but the underlying full-suite flake remains unaddressed and is forward-risk for 10.7 / the Epic-10 integrated smoke. — OPEN (test-infra; recommend fixing at the Epic 10 retro).**
  - **Summary:** The dev/QA correctly adopted the `9.5-shiki-warmup`-suggested hardening in the one new markdown-DOM test 10.6 added — `RoomApp.live.test.tsx` runs `await prewarmHighlighter()` in `beforeAll` AND asserts `expect(isHighlighterWarm()).toBe(true)`, so a cold-highlighter flake fails DETERMINISTICALLY in that file rather than intermittently. That is the right per-test discipline (Rule 12 carry honored). However, this only protects the NEW file; the underlying full-suite flake (`packages/ui-shared/src/markdown/highlight.test.ts` faulting under parallel load — the `@shikijs/primitive` `startIndex`-undefined signature recorded in the `10.5` entry) is in an untouched package and was NOT fixed by 10.6 (it simply did not surface in this review's run, as it did not in the dev's). It will keep making a SOLE-failure ROOT-`pnpm test` ambiguous at every remaining Epic-10 gate (10.7 + the integrated smoke), and a real RED here would be easy to misread as a regression.
  - **Rationale:** LOW — not a 10.6 defect (the changeset touches no ui-shared/Shiki code; `git diff -- packages/ui-shared` is empty; this review's full suite was GREEN 1434/1434). Recorded per Rule 8 as a recurring-flake forward-risk so the next gate treats a sole `highlight.test.ts` `startIndex` failure under full-suite load as this known flake (re-run isolated to confirm GREEN; Rule 6 ground-truth) and so the Epic 10 retro has it queued as an explicit fix candidate — the intermittency (it reds the gate only sometimes) is precisely what makes it worth fixing before it masks a real regression.
  - **Suggested resolution:** At the Epic 10 retrospective (or the next time the markdown/Shiki test infra is touched), reproduce under full-suite parallel load and apply ONE of: pin/upgrade `@shikijs/*` (fault is in `@shikijs/primitive@4.1.0`), serialize the highlighter-using DOM tests via a shared `prewarmHighlighter` barrier or a single-thread `poolMatchGlobs` group for the markdown suites, or add a bounded retry to the known-flaky highlighter tests. This supersedes nothing in the `10.5` Shiki entry — it adds the Epic-10-retro ownership + the observation that 10.6's added test is correctly hardened while the root flake is not.


## Deferred from: dev-story of story 11.1 (2026-06-06)

**Story 11.1 (operator CLI scaffold) registered `export` + `import` as recognized operator-only subcommands with HONEST, explicitly-inert handler bodies (Rule 13 — a visible affordance whose op is deferred is a RECORDED inert stub, never an unrecorded no-op that lies).** Each handler exercises its hand-rolled arg-parse seam (`parseExportArgs` / `parseImportArgs` — `--db <path>`/`--db=<path>` + an output/input positional, mirroring `ui.ts`'s `parseUiArgs`; no CLI-framework dependency), then writes a clear "not yet implemented — arriving in Story 11.2/11.3" message to STDERR and sets `process.exitCode = 1`. The two inert bodies are tracked below with named consumers.

- **Story 11.1 · INERT-STUB · `agentbbs export` handler body is an inert scaffold (stderr "not yet implemented (Story 11.2)" + exitCode 1). — RESOLVED by Story 11.2 (the export consumer).**
  - **Summary:** `packages/cli/src/export.ts` `exportCommand` is registered + dispatch-reachable (NOT "Unknown command") and parses its args via `parseExportArgs`, but does NOT yet dump the ledger — it reports the not-yet-implemented status to stderr and exits non-zero. The command module + arg-parse seam (`--db`, output-path positional) are established so 11.2 has nothing to scaffold.
  - **Rationale:** Story 11.1 is scope-limited to the CLI dispatch surface; the NDJSON dump logic is Story 11.2's deliverable. The inert handler is honest (clear message + non-zero exit), not a silent no-op.
  - **Resolution (Story 11.2, 2026-06-05):** `exportCommand` now dumps the whole logical event ledger as a portable NDJSON archive (header line + one line per `Event` in ascending `seq` + delimited read-state cursor lines) via the shared `packages/cli/src/archive.ts` codec; opens the ledger through `createDataAccess` + `resolveDbPath` (mirrors `ui.ts`), reads through the `DataAccess` port (`eventsSince(0)` + `getCursor`), writes to the out-path or stdout, prints a one-line stderr summary (exit 0), and reports a clear error + non-zero exit on DB-open / out-path failure. `index.ts`'s `export` describe was updated to drop the "not yet implemented" suffix. The 17-tool agent contract stays byte-identical (Rule 13); `export` is nowhere an MCP tool. **Cursor/read-state AC3 reconciliation (Rule 8):** the archive CAPTURES each handle's stored `check` cursor as a delimited read-state section (cursors are NOT derivable from the event stream alone) so Story 11.4's `cursors` round-trip is achievable — this supersedes the `core/ports.ts#getCursor` doc sentence "NOT part of the FR32 export" (additive, non-breaking; core byte-identical). See the story Dev Notes "Design decision (AC3)".

- **Story 11.1 · INERT-STUB · `agentbbs import` handler body is an inert scaffold (stderr "not yet implemented (Story 11.3)" + exitCode 1). — OPEN → Story 11.3 (the import consumer).**
  - **Summary:** `packages/cli/src/import.ts` `importCommand` is registered + dispatch-reachable (NOT "Unknown command") and parses its args via `parseImportArgs`, but does NOT yet replay an archive — it reports the not-yet-implemented status to stderr and exits non-zero. The command module + arg-parse seam (`--db`, input-path positional) are established so 11.3 has nothing to scaffold.
  - **Rationale:** Story 11.1 is scope-limited to the CLI dispatch surface; the NDJSON replay-into-an-empty-board logic is Story 11.3's deliverable. The inert handler is honest (clear message + non-zero exit), not a silent no-op.
  - **Suggested resolution:** Story 11.3 replaces the `importCommand` body with the real NDJSON replay (open the ledger via `createDataAccess` + `resolveDbPath`; replay the archive's events into an empty board via core, honoring the append-only invariant), reusing the existing `parseImportArgs` seam. Update `index.ts`'s `import` describe to drop the "(Story 11.3 — not yet implemented.)" suffix.

## Deferred from: code review of story 11.2 (2026-06-06)

**Story 11.2 (`agentbbs export` → portable logical NDJSON archive: versioned header defined against `EVENT_TYPES`, one lossless `Event` line per event in ascending `seq`, delimited read-state cursor lines; shared `packages/cli/src/archive.ts` codec for 11.3 reuse) reviewed: 1 HIGH found + AUTO-RESOLVED inline, 0 MED, 1 LOW deferred (below), 3 dismissed.** **HIGH (AC6 gate was RED as delivered — auto-resolved):** the dev's Dev Agent Record claimed "lint 0 / format clean", but the reviewer's canonical ROOT gate found `pnpm run lint` RED (2 `@typescript-eslint/no-unused-vars` errors in `archive.test.ts` — the rest-sibling destructure-to-drop `const { actor: _dropped, ...rest } = e` trips the rule; the repo's flat config sets NO `varsIgnorePattern`/`ignoreRestSiblings`, so the `_`-prefix convention is NOT honored) AND `pnpm run format` RED (`export.test.ts` failed `prettier --check` — one over-width `.find(...)` line). Both auto-resolved inline: the two destructure-drops rewritten to `const x: Record<string, unknown> = { ...e }; delete x.actor;` (semantics byte-identical — the two non-vacuity tests still discriminate, 9/9 archive green), and `export.test.ts` prettier-formatted. After the fix the FULL gate is GREEN: **lint 0 · typecheck 0 · build clean · `pnpm test` 1526 passed / 177 files / 0 failed · format clean.** **Rule 13 (byte-identical agent contract, LOAD-BEARING):** reviewer re-confirmed `git diff HEAD -- packages/core packages/mcp-server/src` EMPTY; the new codec lives in `packages/cli` (NOT core), carries no board logic (reads through the `DataAccess` port `eventsSince(0)`/`eventsByType`/`getCursor` + serializes — NFR2); `export` appears NOWHERE in `mcp-server/src` (operator-only, never an MCP tool); the 17-tool drift + bootstrap guards are in the 1526 green. **AC3 cursor reconciliation (Rule 8) — verified CORRECT:** the `core/ports.ts#getCursor` doc DOES say cursors are "TRANSIENT bookkeeping (NOT part of the FR32 export)" (lines 134–135, confirmed by direct read) and the archive's read-state section genuinely supersedes it — the supersession is ADDITIVE (cursor `0` omitted = byte-equivalent to absent; non-zero captured as a distinctly-tagged `agentbbs_read_state` line), non-breaking (core byte-identical), recorded in Dev Notes + the codec header comment, and surfaced to the reviewer. Cursors are genuinely NOT event-derivable (`identity.seen` payload is `{ handle }` — no `seq`), so capture is the only way Story 11.4's `cursors` comparison can pass; the read-state round-trip is lossless + asserted (`{ handle, cursor }` exact) over the real ledger. **Rule 18 (precise SQL-shaped matchers) — verified:** the no-SQLite-leakage assertions correctly avoid bare-substring `events`/`cursors` (which would false-positive on `ndjson-events`/`cursor_count`/`event_types`) and instead match SQL-shaped forms (`FROM\s+events`, `FROM\s+cursors`, PRAGMA, rowid, sqlite, CREATE TABLE). **Rule 3 / Rule 7 (real-runtime + non-vacuity, reviewer-RE-confirmed):** the marquee serialize→parse round-trip runs over a REAL `createDataAccess` better-sqlite3 temp ledger (all 10 `EVENT_TYPES` seeded) + a real-spawn populated export→file→parse; the reviewer INDEPENDENTLY mutated `serializeArchive` to drop `actor` → the real-ledger `export.test.ts` round-trip went RED (3 failures on `toEqual(eventsSince(0))`) → reverted byte-identical (`git diff` clean, residue grep empty) → 16/16 green. **Rule 1 (integration AC) — satisfied:** the story has an `## Integration ACs` section naming consumers 11.3/11.4 + the observable serialize→parse round-trip against the real ledger (the producer output validated end-to-end before the import consumer exists). **Rule 5/6 N/A** (no NFR tripwire worked around; no `docs/adr`).

- **Story 11.2 · LOW · The DB-OPEN-failure trigger of AC5 ("when the DB cannot be opened → clear error + non-zero exit") is not DIRECTLY asserted by a test — only the unwritable-out-path trigger of the SAME shared `catch → log "failed" + exitCode 1` path is. — OPEN (low-value test-hardening; fold into Story 11.3 or the next CLI-error touch).**
  - **Summary:** `exportCommand`'s error contract is a single generic `try { runExport } catch { log("agentbbs export: failed — …"); process.exitCode = 1; }`. The unwritable-out-path case (a path inside a non-existent directory → `writeFileSync` ENOENT) exercises that exact catch→exit-1 path end-to-end in BOTH `index.test.ts` (unit, injected sinks) and `bin-spawn.e2e.test.ts` (real spawn, real exit code) — so the AC5 error-behavior CONTRACT (clear stderr message + non-zero exit) IS proven by a real-runtime test. What is NOT separately asserted is the OTHER trigger of the same path: a genuine DB-open failure. Note this trigger is hard to hit benignly — `openDatabase` calls `ensureDbDirectory` on a writable open, so a missing `.db` path is CREATED (an empty-board export, not an error); a real open failure needs e.g. a path that is a directory, a corrupt DB file, or a permission denial.
  - **Rationale:** LOW — the AC5 failure contract (message + non-zero exit) is proven via the out-path trigger over the identical code path; only the specific failure *cause* differs, and the export's create-on-missing behavior makes the DB-open trigger an uncommon real-world case. No correctness gap in the asserted contract; this is additional-coverage hardening, not a defect.
  - **Suggested resolution:** When Story 11.3 (import) lands or the CLI error-handling is next touched, add one assertion that a genuinely-unopenable DB path (e.g. point `--db` at an existing DIRECTORY, or a file with a non-SQLite header) makes `runExport`/`exportCommand` reject → `agentbbs export: failed` on stderr + exit 1, closing the second half of AC5's error matrix.
