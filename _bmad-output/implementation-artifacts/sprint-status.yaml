# generated: 2026-05-31
# last_updated: 2026-05-31
# project: AgentBBS
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts
# story 1-6 code-review: done (2026-05-30)

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog -> in-progress: Automatically when first story is created (via create-story)
#   - in-progress -> done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-31
last_updated: 2026-05-31 (story 7-2 code-review: done — APPROVED; 0 HIGH / 0 MED / 0 LOW / 0 decision-needed / 0 deferred / 0 dismissed. Adversarial layers (Blind/Edge/Auditor) + independent gate re-run all clean. 8 directed checks verified: (1) APPEND INVARIANT held — seed APPENDS only (no UPDATE/DELETE; main project/agentbbs identity/announcement are events), surfacing READS, check stays pull-only, core imports only the DataAccess port, cursors table untouched by the seed; (2) AC#1/#2 idempotent — existence-check no-op on re-run + QA forked seed-protocol-race GENUINELY proves 8 real OS processes converge to EXACTLY ONE announcement/main project/agentbbs identity, every seed resolves, worker resolves core via dist+build-if-stale, and the NON-uniqueness fault re-throws (only code==='UNIQUENESS_CONFLICT' swallowed, the exact data-access discriminant); (3) AC#3 first-check surfacing detected pre-recordSeen, floor/cursor-independent, additive (no cursor advance), per-identity, robust-if-unseeded, late-bootstrap ordering proven; (4) AC#4 join surfacing at the tool layer via readProtocolAnnouncement, joinBoard's Project return unchanged; (5) AC#5 real Client↔McpServer↔SQLite genuine (idempotent + first/second check + join + OPEN-read by a non-member, FR9); (6) additive envelope inferred-literal (5.3 gotcha handled, build+typecheck green, existing 6.1/3.3 tests updated not broken); (7) Rule 8 reserved-main reconciliation SOUND (a user announcing 'Main' → PROJECT_EXISTS on the project_id guard, documented/intended); (8) honest gate GREEN end-to-end: lint 0 / build 7-7 / typecheck 0 / test 638 (93 files, 0 failed/0 skipped, no .only/.skip/.todo) / format --check clean — count 638/93 matches QA. Source facts re-verified (Rule 4 — payload shapes, at-rest room_id, projections, roomToWire body). No files modified beyond the story Review Findings + tracking docs. Left UNCOMMITTED (no dist) for the lead's post-CR smoke gate. ;; story 7-2 → review; dev complete — SEED the permanent main-board protocol announcement (idempotent by existence-check, AR16/AC#2; appendGuarded on the at-rest room_id makes a concurrent bootstrap race safe — loser's guard trips + is swallowed) on a RESERVED `main` project (Design dec.1 — projectId-scoped model; OPEN-readable FR9) + SURFACE it ADDITIVELY on first `check` (AC#3 — `protocol?` field, detected from the pre-recordSeen stream, bypasses the floors, robust if unseeded) and on `join_board` (AC#4 — tool-layer via new core read `readProtocolAnnouncement`, keeps joinBoard's Project return + the thin tool thin). NO new event type/error code (EVENT_TYPES 10 + BOARD_ERROR_CODES unchanged); THE APPEND INVARIANT held (seed APPENDS idempotently, surfacing READS, core imports only the DataAccess port). Seed runs at bootstrap in main.ts (NOT createBoardServer — existing integration tests unaffected). Build CAUGHT a structuredContent index-signature type error the vitest src-alias hid (Story 5.3 gotcha) — envelopes built as inferred literals w/ the optional protocol spread in. AC#5 Integration: real Client↔McpServer↔SQLite (nothing mocked) — seed twice→exactly ONE announcement (+1 system identity +1 main project), first check surfaces (body=four moves + docs/negotiation-protocol.md pointer), second does NOT, join_board surfaces, OPEN-readable by NON-member via read_room/list_announcements (reader∉list_members, agentbbs∈). Honest gate GREEN: lint 0 / build 7-7 / typecheck 0 / test 636 (92 files, 0 failed/0 skipped, no .only/.skip/.todo) = 621 + 15 (9 seed-unit, 4 check-surface, 1 integration; existing-test edits = assertions not new it()) / format --check clean (after --write on 3 files + affected re-run). Left UNCOMMITTED for the lead's post-CR smoke gate; story 7-1 → review; Negotiation Protocol documented — docs/negotiation-protocol.md (four moves → reply/react/unreact/read_contract, escalation, convention-not-enforced) + content-guard test (3) + README/tool-contract cross-links; gate green: lint 0 / build 7-7 / typecheck 0 / test 621 (90 files, 0 failed/0 skipped) = 618+3 / format --check clean; NO production code, NO new tool/event/error code, APPEND INVARIANT untouched; left UNCOMMITTED for the lead's post-CR smoke gate)
# story 6-2 code-review: done (2026-05-31) — APPROVED; EPIC 6 COMPLETE. 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog; 1 LOW (doc tool-count "12 tools" → count-agnostic in docs/pull-only-delivery.md §1+§5; actual registered count is 17; the same 4.5-tool-label/E3-tool-names drift, →Epic 7) auto-resolved inline in the doc + story Review Findings + deferred-work.md (6.2 clean-review entry). Proportionate review of a docs + tests-only changeset — ZERO production-source change CONFIRMED (git diff --stat vs baseline 0da782d: only *.test.ts under mcp-server/src + docs/pull-only-delivery.md + README + tracking docs; check.ts core+tool, server.ts, register-tool.ts all unchanged). All 4 ACs re-confirmed with real-runtime evidence: (1) BOUNDED genuine + load-bearing (NOT vacuous) — per-BOARD floor (announcements: 400 back-history + 3 delta → first check returns EXACTLY 3, toEqual(newRoomIds), none of the 400 leak, length<maxSeq/10) AND per-ROOM floor (QA bounded-messages: 400 reply history + late add_participant → EXACTLY post-add delta, exercises the room.participant_added floor branch the announcement test never touches), both + SCALE-INVARIANCE (same delta count at 10 vs 400 on one shared ledger, large.maxSeq>small.maxSeq); a broken floor returns back_history+delta → fails toHaveLength(DELTA), so the assertion is the thing under test. (2) NO-PUSH genuine — STRUCTURAL grep of ALL production *.ts in mcp-server/src for push CALL forms (sendNotification(/.notification(/sendLoggingMessage(/createMessage(/elicitInput(/SSEServerTransport/EventSource)→0 (independently reproduced: only ServerNotification hits are register-tool.ts:37 type-import + :44 RequestHandlerExtra type-alias, ZERO calls), non-vacuity-guarded (files>15, server.ts+check.ts present); BEHAVIOURAL real client gets 0 notifications across register→check via fallbackNotificationHandler (verified present on INSTALLED sdk@1.29.0 protocol.d.ts:265) + check returns CallToolResult. (3) DEAD-LETTER genuine — Z registers+joins (need IS in scope) but workflow ENDS, Z NEVER checks (asserted 0 identity.seen), announcement.posted stays in ledger (out-of-band eventsByType==1) + returned by OPEN reads (list_announcements subject + read_room body verbatim) to a NON-member operator (operator∉list_members) = durability-without-delivery. (4) all over real createDataAccess SQLite (Rule 3, nothing mocked). DOC ACCURACY — every load-bearing claim verified vs code: seq>max(cursor,joinFloor) matches check.ts:189(board)+:205(room); cursors-table carve-out matches cursors.ts:83-84 (INSERT…ON CONFLICT(handle) DO UPDATE SET seq=excluded.seq); maxReturned-not-maxSeq() matches check.ts:215-228; FR9 open reads identity-not-membership (list-announcements.ts + read-room.ts, locked by board-read-open.fr9.integration.test.ts); README cross-refs exist (line 20 "nobody is ever pushed to", line 168 out-of-scope "push notifications (pull-only)"). The ONLY inaccuracy was "12 tools" (fixed). Rule 5 honest — query-cost smoke (checkMs<2000, file ~3s) confirms check cheap at V1, no NFR amendment, no paper-over. Adversarial: Blind Hunter 0 correctness, Edge Case Hunter 0 unhandled (QA messages test closes the per-room-floor coverage gap), Acceptance Auditor 0 AC violations. Rule 1 N/A; Rule 3 satisfied; Rule 4 re-verified; Rule 6 N/A (no docs/adr); Rule 8 — 4 *.test.ts files default-suite-discovered + confirmed-RUN (9 tests pass by path filter, real-SQLite 700-1040ms each). APPEND INVARIANT held (nothing appended/mutated; dead-letter persists BECAUSE append-only; check PUSHES nothing). Honest gate GREEN end-to-end: lint 0 / build 7-7 / typecheck 0 / test 615 (88 files, 0 failed/0 skipped, no .only/.skip/.todo) = 606 + 7 dev + 2 QA / format --check clean (incl. edited doc). NO new deferred items; carried-forward OPEN for the Epic 6 retro: 1.5, 1.6, 5.1-roomid-cap-edge, E3-tool-names(→Epic 7), 4.5-tool-label(cosmetic). Findings written to story Review Findings + deferred-work.md. Left UNCOMMITTED (incl. no dist) for the lead's post-CR smoke gate. Status review→done.
# story 6-2: review (2026-05-31) — dev complete; the Epic-6-closing story (documents + reaffirms the pull-only/bounded/dead-letter contract Story 6.1's check already implements). ZERO new production code (no new MCP tool/event/error code/push surface — adding a push surface would violate NFR5; check.ts core+tool and server.ts unchanged). DELIVERED: (1) docs/pull-only-delivery.md (NFR5/NFR8/NFR11, open-source-ready) — PULL-ONLY (board never pushes/notifies/interrupts; agents dial in; structural property, not just prose), BOUNDED (response = delta since max(cursor,floor), indexed seq read; back-history never transmitted, browsed on demand), the per-identity cursor + per-scope floors composition (4.6/6.1) incl. the cursors-table APPEND-INVARIANT carve-out, and the ACCEPTED pull-only DEAD-LETTER (durability-WITHOUT-delivery-guarantee: append-only persists, FR9 open-read + operator global-read/escalation backstop, V2 push deferred behind the data-access seam); cross-linked from README (check/cursor Key-concepts row + check MCP-tool-surface row). (2) 3 reaffirming test files (+7 tests): check.bounded.integration.test.ts (real SQLite — ABSOLUTE: 400-event pre-floor back-history + 3-item post-floor delta → first check returns EXACTLY 3, no pre-join leak, response < maxSeq/10; SCALE-INVARIANCE: same delta count at 10 vs 400 back-history on one shared ledger → response doesn't grow with ledger; + Rule-5 query-cost smoke green ~3s well under 2000ms ceiling, the QUERY is acceptable at V1 scale, NOT a tripwire); no-push.contract.test.ts (STRUCTURAL grep-style scan of all production *.ts in mcp-server/src for push CALL forms sendNotification(/.notification(/sendLoggingMessage(/createMessage(/elicitInput(/SSEServerTransport/EventSource → 0 hits, the lone ServerNotification token pinned to its RequestHandlerExtra TYPE position in register-tool.ts; BEHAVIOURAL real-client zero-notification across register→check via fallbackNotificationHandler [verified on installed sdk@1.29.0 protocol.d.ts] + check returns CallToolResult); dead-letter.integration.test.ts (real SQLite — Z joins board [need IS in Z's scope] but workflow ENDS, Z NEVER checks [0 identity.seen], announcement.posted remains in ledger [out-of-band eventsByType==1] + returned by OPEN reads list_announcements + read_room (body verbatim) to a NON-member operator)). Rule 1 N/A (not service-introducing); Rule 3 satisfied (bounded + dead-letter + no-push-behavioural over real createDataAccess SQLite, nothing mocked); Rule 4 source-facts re-verified vs repo (check bounded delta, no push surface, FR9 open reads, snake_case→camelCase read mapping); Rule 5 honest — no NFR amendment (response bounded + query cheap); Rule 6 N/A (no docs/adr); Rule 8 — all 3 files *.test.ts under packages/mcp-server/src/** → default-suite-discovered; APPEND INVARIANT held (nothing new appended/mutated; dead-letter persists BECAUSE append-only; check PUSHES nothing). Honest gate GREEN: lint 0 / build 7-7 / typecheck 0 / test 613 (87 files, 0 failed/0 skipped, no .only/.skip/.todo) = 606 + 7 (bounded 2, no-push 4, dead-letter 1) / format --check clean (after --write on the 3 new test files, full suite re-run green on the formatted tree). Left UNCOMMITTED (incl. no dist) for the lead's post-CR smoke gate.
# story 6-1 code-review: done (2026-05-31) — APPROVED. 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog; 1 LOW (test-count doc drift 604→606, QA tests_added 3→actual 2: check-race + multiscope) auto-resolved inline in the story Validation/Change Log + this note. The marquee Epic 6 capability: `check` (pull-only dial-in) + the STORED per-identity cursor (`cursors` table) + per-scope floors. All 5 ACs + all 9 directive points + the marquee APPEND INVARIANT re-confirmed: (1) APPEND INVARIANT held — no UPDATE/DELETE events anywhere (grep-clean; the only `events` tokens near UPDATE/DELETE are in cursors.ts comments explaining the guard is not tripped); the ONLY mutation is the `cursors` UPSERT (INSERT…ON CONFLICT(handle) DO UPDATE SET seq=excluded.seq) against the SEPARATE architecture-sanctioned bookkeeping table (line 253); lint GREEN proves the guard regex (needs literal `events` after UPDATE/DELETE) does not flag `DO UPDATE SET seq`; cursors.test.ts proves setCursor appends 0 events rows; check/recordSeen append ONLY identity.seen. (2) AC#1/#3 cursor→maxReturned NOT maxSeq (check.ts:218-224) — the forked cross-process check-race.test.ts GENUINELY proves AC#3 (8 real OS appender processes vs 1 looping checker): no in-scope message skipped (union of returns ∪ quiescent drain == landed replies, none lost/phantom/dup), cursor monotonic (never backward), UPSERT+recordSeen consistent under contention (final stored cursor==max in-scope seq, 1 identity.seen/check); worker forks BUILT dist/check-race-worker.js (core via exports→dist) with beforeAll build-if-stale. (3) AC#4 floors: announcements seq>max(cursor,boardFloor), messages seq>max(cursor,roomFloor); non-participant room gated on Map membership NEVER `?? 0`; boardJoinSeq=MIN board.joined seq, non-member→undefined; confirmed members⟺board.joined (foldProjects derives members only from board.joined; check.ts inline boardFloors folds the same set) so the `?? cursor` fallback at check.ts:189 is unreachable. (4) AC#2 empty-delta cursor unchanged; recordSeen UNCONDITIONAL (check.ts:233) so presence pings even on empty check (last_seen advances every dial-in). (5) scope: announcements by MEMBERSHIP, messages by PARTICIPATION; multiscope.test.ts proves the positive 2-board+2-room union seq-merged WHILE a 3rd board+room excluded. (6) ~21 fakes gained getCursor/setCursor (Map-backed / reject-stubs), typecheck green, SQLite adapter is the real impl (satisfies DataAccess). (7) Task 6 optimization behaviour-IDENTICAL (same MIN floors, strict >, scope) — all check tests pass; measurement sound (real SQLite, busy actor, 50 iters median+p95); 3.0-b + 4.6-a closed-with-evidence justified; deferred-work reconciliation accurate. (8) AC#5 real Client↔McpServer↔SQLite (nothing mocked): first-check-no-flood, delta, empty-delta-unchanged-cursor, last_seen advances (list_members + identity.seen count cross-check), NO_IDENTITY, no-params surface; check PUSHES nothing (grep: no notification/sendLoggingMessage/createMessage/elicitInput — only array .push + the "no push" comment); NO new event type/error code. (9) honest gate GREEN: lint 0 / build 7-7 / typecheck 0 / test 606 (84 files, 0 failed/0 skipped, no .only/.skip/.todo) = 575 baseline + 31 (check 9, multiscope 1, boardJoinSeq 10, cursors 7, check-race 1, check.integration 3) / format --check clean; check is the 14th registered tool + in the bootstrap exhaustive sorted tool-list. Adversarial: Blind Hunter 0 correctness, Edge Case Hunter 0 unhandled, Acceptance Auditor 0 AC violations. Rule 1+3 satisfied (real-runtime AC#5); Rule 4 source-facts re-verified; Rule 5 N/A (no NFR amendment); Rule 6 N/A (no docs/adr). NO new deferred items; OPEN set unchanged (1.5, 1.6, 5.1-roomid-cap-edge, E3-tool-names→Epic 7, 4.5-tool-label cosmetic). Findings written to story Review Findings. Left UNCOMMITTED (incl. no dist) for the lead's post-CR smoke gate. Status review→done.
# story 5-3 code-review: done (2026-05-31) — APPROVED; EPIC 5 COMPLETE. 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog; 1 LOW (Dev-Agent-Record test-count drift 572→575, breakdown 16+5+6→19+4+6) auto-resolved inline in the story file + this sprint-status note (QA appended 3 cases to contract.test.ts — multi-level ≥3 reversion + 2 highest-seq-not-most-reactors — after the dev draft, 16→19; op file is 4 it() not 5; net +29 over the post-5.2 baseline of 546 = 575). All 5 ACs + all 9 directive points re-confirmed: (1) APPEND INVARIANT + module boundary lint-clean — contract DERIVED by query every call, NO stored contract column/flag/event (data-access grep: only the word "contract" in comments), currentContract/readContract only READ+fold (no eventsSince/append in contract.ts; read-contract.ts reads eventsSince(0) once, never appends), order by seq, core→DataAccess-port only; (2) AC#1 highest-seq live-👍 wins — reuses roomMessages' live reactions (does NOT re-fold liveReactors), walks max-seq→down to first reactions.length>0, COUNT irrelevant; QA highest-seq-not-most-reactors test (seq2=3 reactors loses to seq3=1) genuinely pins it; (3) AC#2 reversion automatic — no stored previous/revert branch, fresh fold each call; QA multi-level ≥3 test walks M3→M2→M1→null skipping 2 consecutive dead messages; (4) AC#3 null when no live 👍 (never-reacted + 👍'd-then-un-👍'd); (5) AC#4 OPEN read — tool gates only NO_IDENTITY (NO isMember/requireMembership/isParticipant in the tool), core readContract findRoom→ROOM_NOT_FOUND DISTINCT from contract:null; (6) AC#5 real Client↔McpServer↔SQLite nothing-mocked (createDataAccess=real better-sqlite3) — M1→M2→M1→null progression (seqs from read_room), no-reactions→null, NON-member open read, ROOM_NOT_FOUND, NO_IDENTITY, discovery-surface room_id; returns full contract MESSAGE incl body (asserts env.contract.body); (7) thin client — read_contract tool delegates to core.readContract, no eventsSince/board-logic; existence+ROOM_NOT_FOUND moved to core readContract mirroring readRoom; pure currentContract stays existence-agnostic; (8) NO new event/error code (EVENT_TYPES still 10, ROOM_NOT_FOUND/NO_IDENTITY reused); strict-typecheck cast fix GENUINE — type ContractEnvelope alias (anonymous object-literal, carries index signature) NOT a named interface (grep: zero interface ContractEnvelope); pnpm run typecheck GREEN exit 0 (the vitest src-alias had hidden the TS2352); (9) honest gate GREEN end-to-end: lint 0 / build 7-7 / typecheck 0 / test 575 (78 files, 0 failed/0 skipped, no .only/.skip/.todo) / format --check clean; read_contract registered in server.ts + in the bootstrap exhaustive sorted 18-name tool-list. Adversarial: Blind Hunter 0 correctness (messages[i]! safe — i∈[0,len-1]; null-vs-message envelope maps to JSON null per convention), Edge Case Hunter 0 unhandled (empty/unknown-room→ROOM_NOT_FOUND; unique-seq no ties; cross-room isolation; re-react re-instates; order-independence; announcement #1/proto-room CAN be contract; integration connect() shares ONE module-level dataAccess so the progression is a genuine single-ledger sequence), Acceptance Auditor 0 AC violations. Rule 1+3 satisfied (real-runtime AC #5, nothing mocked); Rule 4 source-facts (roomMessages.reactions / liveReactors / messageToWire / findRoom) independently re-verified vs repo, no deltas; Rule 5/6 N/A (no NFR amendment; no docs/adr). NO new deferred items introduced. Carried-forward OPEN for the Epic 5 retro: 1.5, 1.6, 3.0-b(→6.1), 4.6-a(→6.1), 5.1-roomid-cap-edge, E3-tool-names(→Epic 7), 4.5-tool-label(cosmetic). Findings written to story Review Findings + deferred-work.md (story 5.3 clean-review entry). Left UNCOMMITTED (incl. no dist) for the lead's post-CR smoke gate. Status review→done.
# story 5-3: review (2026-05-31) — dev complete; FR21, the marquee Epic 5 capability (current agreed contract). currentContract(events, roomId): RoomMessage|null pure projection = of roomMessages (which carry live 👍 reactions via Story 5.2 liveReactors), the HIGHEST-seq message holding ≥1 live 👍, else null — reuses roomMessages (does NOT re-fold reactions), walks max-seq→down. COMPUTED by query every call, NEVER stored (THE APPEND INVARIANT) — reversion-on-retract needs NO special logic (fresh query yields next-highest live-👍'd or null). readContract(dataAccess, roomId) thin core read op (readRoom mirror): one stream read → findRoom → ROOM_NOT_FOUND → currentContract; keeps existence-check + ROOM_NOT_FOUND in core (thin-client rule — no eventsSince/board-logic in mcp-server) while the pure projection stays existence-agnostic (null for unknown/empty). read_contract MCP tool: { room_id } Zod, NO_IDENTITY gate (OPEN read, NO membership — FR9 "computed by ANY reader"), delegate, { room_id, contract: messageToWire(msg)|null }. Unknown room → ROOM_NOT_FOUND DISTINCT from contract:null (known room, no live 👍 yet); announcement (#1) CAN be the contract. NO new event type / error code (ROOM_NOT_FOUND/NO_IDENTITY reused). Registered in server.ts; bootstrap exhaustive tool-list extended (read_contract). Real Client↔McpServer↔SQLite integration (Rule 1+3, nothing mocked): seqs captured from read_room, participant bob drives 👍M1→M1, 👍M2→M2, retract M2→reverts M1, retract M1→null; no-reactions room→null; non-member open read; ROOM_NOT_FOUND; NO_IDENTITY; discovery-surface room_id. Rule 4 source-facts (roomMessages.reactions / liveReactors / messageToWire / findRoom) re-verified vs repo, no deltas. Rule 5/6 N/A. NOTE: strict tsconfig.typecheck.json rejected casting structuredContent to a NAMED interface (TS2352 — no index signature); fixed via a `type` alias (anonymous object-literal type, the inline-cast idiom) — vitest src-alias hid it, build/typecheck gate caught it. +27 tests (16 unit projection + 5 core op + 6 integration). Honest gate green: lint 0 / build 7-7 / typecheck 0 / test 572 (78 files, 0 failed/0 skipped, no .only/.skip/.todo) = 545 + 27 / format (prettier --check) clean. Left UNCOMMITTED for the lead's post-CR smoke gate.
# story 5-0 code-review: done (2026-05-31) — APPROVED; clean review, 0 decision-needed / 0 patch / 0 defer / 0 dismissed. Reviewed proportionately (test-only changeset: 3 *.integration.test.ts + 3 tracking docs; NO production source — git diff confirms zero non-test .ts under packages/*/src/). All 5 directive verification points PASS: (1) NO production-source change; (2) negative-path tests GENUINE — each malformed case asserts isError===true AND readErrorPayload===undefined (the Zod-boundary discriminator, traced to register-tool.ts: SDK validateToolInput throws InvalidParams BEFORE the handler so mapErrorToResult is never called → no closed {code,message}, distinct from ROOM_NOT_FOUND/HANDLE_NOT_FOUND core paths which DO carry a code) AND the event count is anchored to a genuinely-established prior ===1 (a real reply/add appended first) + maxSeq unchanged, not ===0; 3 suites re-run green 28/28 (boundary-filtered 3 passed/25 skipped); (3) deferred-work.md reconciliation accurate — 4.3-a RESOLVED w/ evidence, 4.3-b→5.1, 1.5/1.6/3.0-b(→6.1)/4.6-a(→6.1)/E3-tool-names(→Epic 7) OPEN, ledger-snapshot consistent; (4) Rule 4 applied — bodySchema→announcementBodySchema delta caught+recorded, no stale source-fact coded to, schemas re-confirmed byte-exact; (5) full honest gate re-run GREEN: lint 0 / build 7-7 / typecheck 0 / test 478 (69 files, 0 failed/0 skipped, no .only/.skip/.todo) = 475+3 no new files / format --check clean. Rule 1 N/A (strengthens existing tools' Rule 3 coverage), Rule 3 satisfied (real Client↔McpServer↔SQLite), Rule 5/6/8 N/A or satisfied. No deferred items introduced. Findings written to story Review Findings. Left UNCOMMITTED for the lead's post-CR smoke gate. Status review→done.
# story 5-0: review (2026-05-31) — dev complete; Epic 4 deferred cleanup (LEAN test-hardening / housekeeping). Closes deferred-work 4.3-a: added 3 negative-path Zod-boundary-rejection integration cases over the real Client↔McpServer↔SQLite stack (nothing mocked) proving a malformed input is rejected BEFORE the delegate with NOTHING appended — reply (room_id ∈ ['','   ','Bad Room!','UPPER','--leading','a..b'], establishes a real room.replied=1 first then proves each malformed reply leaves it at 1), add_participant (malformed room_id AND malformed handle ∈ ['','   ','Bad Handle!','has space','UPPER!'], real room.participant_added=1 untouched), read_room (READ-side parity: malformed room_id → isError, NOT ROOM_NOT_FOUND). Assertion shape mirrors announce-project's boundary-rejection test: isError===true + readErrorPayload(result)===undefined (Zod rejection, no closed board code — the discriminator vs a core BoardError) + maxSeq unchanged + event count unchanged. NO production-source change, NO new tool/event/error code (Rule 1 N/A — strengthens existing tools' Rule 3 boundary coverage; Rule 5/6 N/A). Rule 4 (Epic 4 retro) source-facts verified vs repo before coding: roomIdSchema=.min(1).max(200)+slug regex, handleSchema=.min(1).max(128)+^[a-z0-9._@-]+$, body validator is announcementBodySchema (story said bodySchema — name delta noted, not load-bearing; body cap untouched, Story 5.1 owns it). deferred-work.md reconciled: 4.3-a RESOLVED (this story, evidence=the 3 cases); 4.3-b OPEN → OWNED BY Story 5.1 (cap-edge — 5.1 replaces .max(16000) char cap with formal 256 KB/BODY_TOO_LARGE and tests the edge there); ledger snapshot re-reconciled (now "by Story 5.0"); carried-forward block gained explicit OPEN bodies for 4.6-a→6.1 + cosmetic 4.5-tool-label; 1.5/1.6/3.0-b(→6.1)/E3-tool-names(→Epic 7) stay OPEN rationale intact. Honest gate green: lint / build 7-7 / typecheck / test 478 (69 files) = 475 + 3 added cases (no new files, 0 failed/0 skipped, no .only/.skip/.todo) / format (prettier --check .) — note: prettier --write applied to the 2 write-tool test files (bad-input arrays wrapped multi-line) then --check re-run green + full suite re-run green on formatted tree. Build-before-test observed (forked workers resolve core via dist; this story adds no core export). Left UNCOMMITTED for the lead's post-CR smoke gate.
# story 4-6 code-review: done (2026-05-31) — APPROVED; 0 HIGH / 0 MED / 0 decision-needed / 0 defer-to-backlog; 2 LOW dismissed (recorded in story Review Findings + deferred-work.md): (1) roomJoinSeq re-implements participants.ts's participant-event filter inline — by-design (the two projections return a SET vs a MIN-seq scalar; a shared helper adds indirection for no behaviour gain); (2) non-participant roomJoinSeq===undefined is a FORWARD-CONTRACT note for Story 6.1 — 6.1's check MUST gate per-room on !==undefined and NOT `?? 0` a non-participant (that would flood), per Design decision 5; the `number` signature already prevents the in-process misuse, so not a 4.6 defect. All 6 ACs + all 8 directive verification points re-confirmed: append-invariant/module-boundary lint-clean (DERIVED — folds every call, NO cursor table/stored position/mutation/UPDATE/DELETE; core→DataAccess-port only; ports.ts UNCHANGED — no getCursor/setCursor, only pre-existing maxSeq) (V1); roomJoinSeq = MIN seq over the handle's room.replied ∪ the room.participant_added naming them, both kinds handled, earliest wins both directions — runtime-proven vs built core/dist (V2/AC#1,#3,#4); non-participant→undefined + roomMessagesSince(sinceSeq:number) type-guards the composition (V3/AC#5); no-flood — roomMessagesSince(roomId, joinSeq) excludes pre-join, read_room/roomMessages NOT cursor-scoped, runtime-proven (V4/AC#2); per-(identity,room) floor NOT a mutated global cursor, reply/add_participant UNCHANGED, QA combined-floor preview seq>max(checkCursor,roomJoinSeq) correct in BOTH directions (V5/Design-dec.1); AC#6 real createDataAccess SQLite ledger nothing-mocked, escape-clause legit (producer, first consumer Story 6.1, mirrors 3.5 forward-declaration), Rule 1+3 (V6); barrel adds ONLY roomJoinSeq/roomMessagesSince, server.ts unchanged, EVENT_TYPES(10)/BOARD_ERROR_CODES UNCHANGED (V7); honest gate re-run GREEN (V8). Adversarial: Blind Hunter 0 correctness, Edge Case Hunter 0 unhandled (empty/unknown-room/announcer-only/non-participant→undefined; added↔replied both orders→earliest; adder-vs-added; out-of-order→MIN; cross-room isolation; strict-> boundary; added-after-last→[]; announcer-of-#1-joins-late does NOT see own seed as new — all pinned), Acceptance Auditor 0 AC violations. Rule 5/6 N/A. Count reconciliation: test 475 (69 files) − 26 (the two new 4.6 files) = 449 = documented post-4.5 baseline (dev-stage was 470; QA appended 5: unit "many participants" 4 + integration "combined floor" 1). EPIC 4 COMPLETE — whole suite green end-to-end (all 12 V1 tools + projections). Honest gate: lint 0 / build 7 / typecheck 0 / test 475 (69 files, 0 failed/0 skipped, no .only/.skip/.todo) / format (prettier --check .) 0. Status review→done.
# story 4-6: review (2026-05-31) — dev complete; per-(identity, room) JOIN-CURSOR projection (the floor Story 6.1 check consumes). roomJoinSeq(events, roomId, handle) = MIN seq over the handle's room.replied for the room ∪ the room.participant_added naming them (undefined for a non-participant); roomMessagesSince = roomMessages (4.4) filtered STRICT seq > sinceSeq. DERIVED, append-invariant-clean (no cursor table/mutation; core→DataAccess-port only) — the join-event seq IS the ledger position at the instant of join, so "set the room cursor to max seq on join" needs no stored cursor. Per-(identity, room) not a single per-identity position advanced to maxSeq on join (that would skip unread deltas in OTHER rooms). Earliest-participating-event wins (add-then-reply → the add seq). NO new MCP tool/event type/error code/stored cursor; NO change to reply/add_participant (Rule 1 escape clause — first consumer is Story 6.1 check; AC #6 real-ledger proof over createDataAccess, nothing mocked, Rule 3 satisfied by library-invocation evidence). Rule 5/6 N/A. +21 tests (17 core unit incl. all AC branches + 4 real-createDataAccess integration: join-by-reply, join-by-add, added-then-reply→earliest, non-participant→undefined — each asserting cursor == joining-event seq + back-history excluded + full history still available on demand). Honest gate green: lint / build 7 / typecheck / test 470 (69 files) = 449 + 21 / format (prettier --check .). Left UNCOMMITTED for the lead's post-CR smoke gate.
# story 4-5 code-review: done (2026-05-31) — APPROVED; 0 HIGH, 0 MED, 1 LOW deferred (doc-only "12th vs 13th tool" label drift), 1 dismissed (HANDLE_NOT_FOUND additive code-add — verified correct: appended last in BOARD_ERROR_CODES with no reorder/rename, union derived from the runtime tuple so they can't drift, error-map routes any BoardError.code generically so it surfaces as {code:HANDLE_NOT_FOUND,message} with no client change, and it is distinct from NOT_A_MEMBER per gate order room→participation→target). All 6 ACs + all 8 directive verification points re-confirmed (append-invariant/module-boundary lint-clean, core→DataAccess-port only; AC#1 target auto-join atomic in ONE append with correct adder-vs-target actor attribution + conditional board.joined skip when already a member; AC#4 gate = room PARTICIPATION not board membership — announcer-who-never-replied rejected; AC#5 idempotent no-op + AC#2/#3 checks precede any append, nothing appended; AC#4 genuine N=8 forked cross-process WAL race, passes 4/4 deterministic ~6s; AC#6 real Client↔McpServer↔SQLite nothing-mocked Rule 1+3; room.participant_added does NOT activate a proto-room; bootstrap exhaustive tool-list incl add_participant). NOTE: a transient corrupted tool-result briefly suggested an append.ts DEFERRED-transaction concurrency defect; investigated and DISPROVEN against git (append.ts unchanged from b337c9b, plain db.transaction as committed) + the race passing deterministically — NO code change made by the reviewer, working tree is exactly the dev/QA changeset. Reviewer re-ran the honest gate green: lint / build 7-7 / typecheck / test 449 (67 files) / format --check. Status review→done.
# story 4-5: review (2026-05-31) — dev complete; the 12th/final V1 tool (add_participant MCP tool + addParticipant core op + room-participants projection). roomParticipants/isParticipant derive a room's participants (room.replied actors ∪ room.participant_added handles, de-duped first-seen seq order; the announcer-who-never-replied is NOT one — proto-rooms have zero participants, so the gate naturally enforces "a negotiation I'm part of"). addParticipant gates: ROOM_NOT_FOUND (unknown room) → NOT_A_MEMBER (actor not a participant) → HANDLE_NOT_FOUND (target unregistered) → idempotent no-op (target already a participant) → PLAIN-append room.participant_added (actor=adder, payload.handle=target) + conditional board.joined (actor=TARGET — the pulled-in peer joins the sub-board, mirroring reply's auto-join but for the target) in ONE tx. Returns { room, participants }; participation DERIVED never stored. add_participant tool: NO_IDENTITY gate, { room_id: roomIdSchema, handle: handleSchema } (both reused), { room, participants } envelope. DEVIATION (surfaced for CR): HANDLE_NOT_FOUND did NOT pre-exist — the story's Source-fact "already in the closed set at errors.ts:29" was WRONG (line 29 = BOARD_NOT_FOUND; repo-wide grep found HANDLE_NOT_FOUND only in the story file). AC #2/#6 NAME it distinctly from NOT_A_MEMBER, so ADDED to BOARD_ERROR_CODES additively (non-breaking, architecture-sanctioned) — the smallest faithful change; error-map routes any BoardError.code generically. No new event type (room.participant_added + its wire mapping pre-existed). Real Client↔McpServer↔SQLite integration (Rule 1+3, nothing mocked): B(replier/participant) pulls in C(registered non-member) → C in list_members + can read_room full history + ledger room.participant_added(actor=B)/board.joined(actor=C); HANDLE_NOT_FOUND; ROOM_NOT_FOUND; non-participant announcer → NOT_A_MEMBER; bystander → NOT_A_MEMBER; idempotent re-add; NO_IDENTITY; discovery-surface params. +32 tests (11 projection + 13 core op + 8 integration). Honest gate green: lint / build 7-7 / typecheck / test 448 (66 files) / format --check. Left UNCOMMITTED for the lead's post-CR smoke gate.
# story 4-4 code-review: done (2026-05-31) — APPROVED; 0 HIGH/MED/decision/defer; 1 LOW auto-resolved inline (test-count drift: docs said +22/416/7-integration → corrected to the measured +24/418/9-integration; the integration file gained 2 QA-addition tests after the +7 draft — interleaved A/B isolation + per-message seq==event-seq referenceability; code unaffected) + 1 dismissed (directive "11th tool" off-by-one — surface is 12 real tools, read_room the 12th; bootstrap exhaustive assertion lists all 14 names and is green). All 6 ACs + all 9 directive verifications independently re-confirmed: append-invariant/module-boundary lint-enforced (read-only pure fold, sort by seq never created_at, core→DataAccess-port-only grep-clean) (Verif#1); announcement=message#1 (body, kind=announcement) then replies by seq (kind=reply), each RoomMessage.seq==event.seq incl non-activator reply#3 (Epic5 messageSeq alignment), proto→single message+active=false, subject NOT in MessageWire (Verif#2/AC#1,#5); open read — only NO_IDENTITY gate, zero membership call (grep-clean), non-member reader succeeds (Verif#3/AC#2/FR9); ROOM_NOT_FOUND via findRoom (in closed set, error-map preserves code) (Verif#4/AC#3); no-truncation genuine — one real createDataAccess handle, reply lands on same ledger, re-read slice==earlier+appended (Verif#5/AC#4); NFR6 hostile markdown/CJK/RTL/ZWJ body byte-identical round-trip (toBe+.length), no transform anywhere (Verif#6); interleaved A/B real-ledger isolation strictly seq-ordered+disjoint (Verif#7); no new error code/event type, read_room 12th real tool, bootstrap assertion exhaustive+green (Verif#8); honest gate re-run by reviewer green (Verif#9). Adversarial: Blind Hunter 0 correctness (readRoom double-pass = intentional Design-decision-5 V1 cost); Edge Case Hunter 0 unhandled (empty→ROOM_NOT_FOUND, orphan-reply→ROOM_NOT_FOUND via findRoom gate, duplicate-announcement unreachable via room_id appendGuarded uniqueness, oversize body capped at write boundary). Real Client↔McpServer↔SQLite nothing-mocked (createDataAccess=real better-sqlite3) (Rule 1+3). Rule 5/6 N/A (no NFR amendment; no docs/adr). Honest gate green: lint / build 7-7 / typecheck / test 418 (63 files) / format --check; no .skip/.only/.todo. Status review→done.
# story 4-4: review (2026-05-31) — dev complete; read_room MCP tool + readRoom core op + room message-history projection (roomMessages). A room's COMPLETE ordered history: the seeding announcement.posted as message #1 (kind=announcement, body=announcement body), then every room.replied by seq (kind=reply); a "message" is identified by its seq (Epic 5 react/contract consume these). OPEN read (FR9 — NO membership; NO_IDENTITY the only precondition), ROOM_NOT_FOUND for unknown room, NEVER truncated (append-only pure fold). No new event type / error code (announcement.posted/room.replied + ROOM_NOT_FOUND/NO_IDENTITY reused). Real Client↔McpServer↔SQLite integration (Rule 1+3): non-member 3rd identity reads [announcement,reply,reply] byte-identical by seq; ROOM_NOT_FOUND; re-read after another reply = no truncation; proto-room → single announcement message; NO_IDENTITY; NFR6 hostile-body verbatim; discovery-surface room_id. +24 tests (9 projection + 6 core op + 9 integration; count corrected at code review from a stale +22/7-integration — the integration file carries 9 tests incl. 2 QA additions). Honest gate green: lint / build 7-7 (caught+fixed structuredContent index-signature type error the vitest src-alias hid) / typecheck / test 418 (63 test files) / format --check. Left UNCOMMITTED for the lead's post-CR smoke gate.
# story 4-3 code-review: done (2026-05-31) — APPROVED; 0 HIGH/MED/decision/patch; 2 LOW deferred (4.3 malformed-room_id boundary-rejection test gap + room_id/body max-length cap-edge test gap — both test-hardening, consistent with codebase-wide precedent, validator correct by parity with projectIdSchema; not 4.3 defects). All 5 ACs + all 8 directive verifications independently re-confirmed: activator min-seq set-once-never-clobbered + undefined-on-proto (AC#1/Verif#2); announcement=message#1 seq-ordering proven via QA history test (AC#1); acting=joining atomic one-append + already-member idempotent skip + NO requireMembership gate (AC#2/Verif#3); ROOM_NOT_FOUND nothing-appended + read-back fail-loud (AC#3/Verif#4); PLAIN append not appendGuarded + QA forked 8-proc race (distinct→N unique increasing seqs+1 min-seq activator+converge; same-actor→benign double board.joined dedups to 1 member) genuine cross-process WAL, build-if-stale dist (AC#4/Verif#2,#5); real Client↔McpServer↔SQLite nothing-mocked (AC#5/Verif#6, Rule 1+3); append-invariant+module-boundary lint-enforced, core→port only grep-clean (Verif#1); no new error code/event type, roomToWire omits activator on proto (Verif#7); Rule 5/6 N/A (body cap = plain Zod .max interim → Epic 5; no docs/adr). Blind Hunter 0 correctness findings (phantom-project board.joined unreachable + harmless). Honest gate green: lint / build 7-7 / typecheck / test 394 (60 files) / format --check. 394/60 = dev 390/59 + QA (1 forked-race file/2 cases + 2 integration cases); no .skip/.only/.todo
# story 4-3: review (2026-05-31) — dev complete; the keystone (reply MCP tool + reply core op: a posted need → a live multi-party room). Rooms projection activator (min-seq room.replied → derived activatedBy/activatedAtSeq, set once never overwritten, undefined for proto-rooms) + reply core op (plain append: room.replied ALWAYS + conditional idempotent board.joined auto-join — "acting = joining" FR10 — in ONE transaction; ROOM_NOT_FOUND for unknown room nothing appended; activator = read-side min-seq derivation, no lock) + reply MCP tool (NO_IDENTITY gate, GRANTS not gates membership, { room_id, body }, { room } envelope, roomToWire +activated_by/+activated_at_seq omitted on proto). Real Client↔McpServer↔SQLite integration (Rule 1+3): non-member reply moves room list_announcements→list_rooms + auto-joins (list_members) + activator=them; ROOM_NOT_FOUND; already-member 2nd reply → board.joined count stays 1; two replies → sequential seqs + activator=lower seq. No new error code/event type (ROOM_NOT_FOUND/NO_IDENTITY/room.replied reused). +15 tests (4 projection + 5 core op + 6 integration). Honest gate green: lint / build 7-7 / typecheck / test 390 (59 files) / format --check. AC#4 forked-race deferred to QA (plain append, no appendGuarded contention to stress)
# story 4-2 code-review: done (2026-05-31) — APPROVED; 0 HIGH/MED/defer; 1 LOW auto-resolved inline (stale RoomWire.active doc comment in room-shared.ts — rewrote to the 4.2 activation read-model; comment-only). All 4 ACs + all 8 directive items independently re-verified: append-invariant/module-boundary lint-enforced (reads only, active DERIVED in foldRooms never stored, seq-order, core→port only); AC#1 disjoint split proven via QA interleaved-ledger test (active subset not a ledger suffix; annIds∩roomIds=∅, all rooms once); AC#2 open read (no membership call leaked — grep-clean; non-member sees full list over real surface); AC#3 BOARD_NOT_FOUND both layers + distinguished from announced-but-empty (→empty arrays, both core+real-surface); AC#4 real Client↔McpServer↔SQLite (nothing mocked; port-seeded room.replied = legit read-tool testing per Design decision 4, not a missing-consumer gap); activation read-model idempotent (multi-reply→1 room emitted once) + unknown-roomId ignored (no phantom) + min-seq activator correctly deferred to 4.3/4.4; no new error code/event type (BOARD_NOT_FOUND/NO_IDENTITY/room.replied reused). Honest gate green: lint / build 7-7 / typecheck / test 375 (57 files) / format --check. QA-reported 375/57 confirmed (dev 371 + QA 4)
# story 4-2: review (2026-05-31) — dev complete; the two browse tools (list_announcements/list_rooms) + the rooms activation read-model. foldRooms room.replied→active=true branch (existence-of-reply, idempotent) + listAnnouncements/listRooms core read ops (findProject existence-check + fold/filter/seq-sort, no append, session-agnostic) + the two MCP tools (NO_IDENTITY gate, NO membership — open read FR9, BOARD_NOT_FOUND for unknown board, roomToWire envelopes). 375 tests (57 files) post-QA+CR; no new tool-registration-count error/event/error-code beyond the two tools; reuses BOARD_NOT_FOUND/NO_IDENTITY/room.replied. Full gate green
# story 4-1 code-review: done (2026-05-31) — CLEAN; 0 HIGH/MED/defer; 5 dismissed. All 6 ACs independently re-verified against the changeset: gate-first membership (AC#2/#3 nothing-appended), exactly-one append.posted (AC#1), disambiguator 1-based ordinal + sound 10k bound (AC#4), room fallback (AC#5), real MCP↔SQLite round-trip + snake_case at-rest (AC#6, Rule 1+3 satisfied). AC#4 cross-process race GENUINE (8 forked OS procs, IPC start barrier, exact {base..base-8} set, distinct seqs/bodies, retry-under-real-contention — not sequential). projectId additive (both mapping dirs + EventPayloadMap totality + all-10 round-trip byte-identical, project_id on disk); no new error code (NOT_A_MEMBER/BOARD_NOT_FOUND reused; 16k cap = plain Zod .max, not BODY_TOO_LARGE; Rule 5 = planned Epic-5 boundary). Append invariant + module boundary lint-enforced + meta-tested. Honest gate green: lint / build 7-7 / typecheck / test 353 (55 files) / format --check
# story 4-1: review (2026-05-31) — dev complete; first Epic 4 service surface (post_announcement) — post_announcement MCP tool + postAnnouncement core op + rooms projection + room-id helper; projectId added to announcement.posted (additive, never-written). 353 tests (55 files) post-QA+CR (dev 347/54 + QA cross-process race test/worker + integration gap-fills); no new error code (NOT_A_MEMBER/BOARD_NOT_FOUND reused); 256KB body cap deferred to Epic 5. Full gate green
# story 4-0 code-review: done (2026-05-31) — CLEAN; 0 HIGH/MED/defer; 0 dismissed. All 4 ACs independently re-verified: no-dist typecheck GREEN + traceResolution→src (AC#1); paths typecheck-config-only (not in base/pkg configs); no-baseUrl validated vs installed TS 6.0.3 (TS5101/TS5090 probe, Rule 3); sentinel grep clean + no-dist pnpm test 322 GREEN + 0 in core/dist (AC#2); deferred-work reconciled exact (AC#3); honest gate lint/build7-7/typecheck/test322/format all 0, −2 = retired 2 alias-proof cases only (AC#4). QA 0-tests SOUND (no runtime surface; AC#1 untestable in Vitest by design)
# story 4-0: review (2026-05-31) — dev complete; Epic 3 deferred cleanup. tsconfig.typecheck.json paths mapping (E3-typecheck-dist RESOLVED, no-dist typecheck green) + retired alias-proof fixture/test (3.0-a RESOLVED, no-dist pnpm test green); deferred-work.md reconciled. Full gate green; 322 tests (50 files) = 324−2 retired alias-proof cases; no new tool/event/error-code; no docs/adr
# story 3-5 code-review: done (2026-05-31) — CLEAN; 0 HIGH/MED/defer; 2 dismissed; Rule 1 escape clause genuine (Epic 4 4.1/4.3/4.5 named, no Epic-3 post tool), Rule 3 real-runtime gate-vs-real-ledger green; touched-file vitest 2 files/9 tests + typecheck clean
# story 3-5: review (2026-05-31) — dev complete, 323 tests green (+8: 6 core unit + 2 integration); membership write-gate primitive (isMember/requireMembership) + FR9 read-open lock-in; no new tool/event/error-code; AC #4 + post consumer forward-declared for Epic 4 (Rule 1 escape clause)
# story 3-4 code-review: done (2026-05-31) — CLEAN; 0 HIGH/MED/defer; 3 dismissed; Rule 1+3 integration green (membership ⋈ identity round-trip); build 7/7 + targeted 14 tests green
# story 3-3 code-review: done (2026-05-31) — CLEAN; 0 HIGH/MED/defer; 1 dismissed; Rule 3 integration green; full gate 307 tests
# story 3-3: review (2026-05-31) — dev complete, 307 tests green (+9: 6 core unit + 3 integration)
# story 3-2 code-review: done (2026-05-31) — CLEAN; 0 HIGH/MED/defer; 4 dismissed; Rule 3 integration re-run green
# story 3-2: review (2026-05-31) — dev complete, 298 tests green
# story 3-1 code-review: done (2026-05-31) — 0 HIGH; 1 MED resolved inline (empty-slug title guard); 0 deferred
# story 3-1: review (2026-05-31) — dev complete, 287 tests green
# story 3-0 code-review: done (2026-05-31) — 0 HIGH/MED; 2 LOW deferred (3.0-a, 3.0-b)
# story 3-0: ready-for-dev (2026-05-31) — epic-cycle retro-review gate
# story 2-5 code-review: done (2026-05-31)
# story 2-5: review (2026-05-31)
# story 2-4: review (2026-05-31)
# story 2-1 code-review: done (2026-05-31)
# story 1-7 code-review: done (2026-05-31)
# story 1-7: review (2026-05-31)
# story 1-6: review (2026-05-30)
project: AgentBBS
# story 1-5 code-review: done (2026-05-30)
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Foundation - monorepo, append-only ledger & shared core
  epic-1: done
  1-1-scaffold-the-pnpm-workspace-and-package-skeleton: done
  1-2-shared-toolchain-and-boundary-enforcement: done
  1-3-event-vocabulary-dataaccess-port-and-error-model: done
  1-4-sqlite-connection-concurrency-mode-and-db-discovery: done
  1-5-append-only-events-table-with-authoritative-sequence: done
  1-6-read-query-path-and-wire-internal-mapping: done
  1-7-multi-process-concurrency-verification: done
  epic-1-retrospective: done

  # Epic 2: Agent identity & presence (MCP)
  epic-2: done
  2-0-epic-1-deferred-cleanup: done
  2-1-mcp-server-bootstrap-with-validation-and-error-mapping: done
  2-2-register-a-durable-unique-identity: done
  2-3-re-establish-identity-with-login: done
  2-4-update-current-focus: done
  2-5-last-seen-presence-tracking: done
  epic-2-retrospective: done

  # Epic 3: Projects & sub-boards (MCP)
  epic-3: done
  3-0-epic-2-deferred-cleanup: done
  3-1-announce-a-project-and-create-its-sub-board: done
  3-2-browse-the-main-board: done
  3-3-join-a-sub-board-single-and-multiple: done
  3-4-sub-board-directory-with-focus-and-last-seen: done
  3-5-board-wide-read-with-join-to-post-gating: done
  epic-3-retrospective: done

  # Epic 4: Announcements, rooms & multi-party negotiation (MCP)
  epic-4: done
  4-0-epic-3-deferred-cleanup: done
  4-1-post-an-announcement-proto-room: done
  4-2-list-announcements-and-rooms: done
  4-3-first-reply-activates-the-room: done
  4-4-read-a-rooms-full-ordered-history: done
  4-5-pull-a-participant-into-a-room: done
  4-6-joining-sets-the-cursor-no-back-history-flood: done
  epic-4-retrospective: done

  # Epic 5: Messaging, reactions & the computed contract (MCP)
  epic-5: done
  5-0-epic-4-deferred-cleanup: done
  5-1-post-a-verbatim-message-with-a-size-cap: done
  5-2-react-and-retract-a-thumbs-up: done
  5-3-compute-the-current-agreed-contract: done
  epic-5-retrospective: done

  # Epic 6: Discovery - pull-only check & cursors (MCP)
  epic-6: done
  6-0-epic-5-deferred-triage: done
  6-1-check-returns-my-delta-and-advances-my-cursor: done
  6-2-bounded-pull-only-delivery-with-documented-dead-letter: done
  epic-6-retrospective: done

  # Epic 7: Negotiation Protocol & agent guidance
  epic-7: done
  7-0-ratify-mcp-tool-contract: done
  7-1-document-the-negotiation-protocol: done
  7-2-seed-and-surface-the-protocol-announcement: done
  7-3-ship-the-agent-prompt-snippet: done
  epic-7-retrospective: done

  # Epic 8: BMad integration, identity & installation kit
  epic-8: in-progress
  8-0-epic-7-deferred-triage: done
  8-1-identity-bootstrap-workflow: backlog
  8-2-post-step-board-review-cadence-hook: backlog
  8-3-skill-customizations-and-skill-rules-registry: backlog
  8-4-single-self-contained-installation-kit: backlog
  epic-8-retrospective: optional

  # Epic 9: Operator UI - shared core & web control room
  epic-9: backlog
  9-1-ui-shared-scaffold-and-design-token-system: backlog
  9-2-inert-markdown-renderer-and-code-block: backlog
  9-3-on-demand-web-host-with-json-api-and-sse: backlog
  9-4-board-navigation-tree-with-decorations-and-needs-you-queue: backlog
  9-5-room-thread-with-breadcrumb-and-joined-row: backlog
  9-6-reaction-chip-and-computed-agreed-mark: backlog
  9-7-join-gate-composer-and-participate-as-peer: backlog
  9-8-rooms-as-editor-tabs: backlog
  9-9-live-updates-optimistic-posting-and-reconciliation: backlog
  9-10-calm-states-connection-footer-voice-and-accessibility-floor: backlog
  epic-9-retrospective: optional

  # Epic 10: Operator UI - VS Code extension surface
  epic-10: backlog
  10-1-extension-scaffold-and-better-sqlite3-electron-abi-proof: backlog
  10-2-extension-host-opens-the-db-and-bridges-to-the-webview: backlog
  10-3-native-treeview-navigation-with-decorations: backlog
  10-4-rooms-as-webviewpanels-mounting-ui-shared-with-theme-inheritance: backlog
  10-5-webview-csp-hardening-and-state-serialization: backlog
  10-6-live-updates-high-contrast-and-accessibility-parity: backlog
  epic-10-retrospective: optional

  # Epic 11: Backup, restore & open-source readiness
  epic-11: backlog
  11-1-operator-cli-scaffold: backlog
  11-2-export-the-ledger-to-a-logical-archive: backlog
  11-3-import-by-replaying-into-an-empty-board: backlog
  11-4-round-trip-fidelity-test: backlog
  11-5-distribution-and-open-source-stand-up-docs: backlog
  epic-11-retrospective: optional
