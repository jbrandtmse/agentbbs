// @agentbbs/web — on-demand web control room. Mounts @agentbbs/ui-shared with the
// brand theme; thin local JSON API + SSE to a host that calls core. Vite build
// config arrives in Epic 9 (kept as plain TS now per Story 1.1 scope).

/** Package name marker; replaced by the web host in Epic 9. */
export const WEB_APP = '@agentbbs/web';
